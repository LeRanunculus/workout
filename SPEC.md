# 구현 명세

**순서대로 할 것.** 특히 0단계를 건너뛰면 잘못된 날짜로 데이터가 쌓인다.
3단계(아이폰 검증)를 통과하기 전에 4단계로 넘어가지 말 것.

| 단계 | 내용 | 왜 이 순서인가 |
|---|---|---|
| **0** | 알려진 버그 4건 수정 | 잘못된 데이터가 쌓이기 전에 |
| **1** | 저장 계층 + SCHEMA 구조 + 자동 저장 | 이 프로젝트의 본체 |
| **2** | manifest · sw · **즉시 배포** | 완성 후 배포 금지 (CLAUDE.md) |
| **3** | 아이폰 실기기 검증 ← **관문** | 여기까지가 1차 완료 기준 |
| **4** | RIR 칩 · 세션 노트 · 코칭 프롬프트 복사 | 배포된 기반 위에서 |
| **5** | 프로그램 편집 | 없는 기구를 매 세션 다시 고르지 않게 |
| **6** | 러닝 탭 | 독립적이고 소급 입력이 가능해 급하지 않다 |
| **7** | GitHub 동기화 | 프로그램까지 확정된 뒤에 올린다 |

`index.html`의 앱 로직(타이머·프리필·대체종목·증량판정)은 구조가 완성돼 있다.
**프레임워크로 새로 짜지 말 것.** 아래 명시된 것만 고친다.

`SCHEMA.md`가 데이터 계약이다. **먼저 읽을 것.** 특히 「시각 규칙」.

---

## 최종 파일 구조

```
workout/
├── index.html          # 앱 본체 (이미 있음, 수정만)
├── manifest.json       # 신규
├── sw.js               # 신규
├── icons/              # 이미 있음 · 검증 완료
│   ├── icon-192.png              192×192
│   ├── icon-512.png              512×512
│   ├── icon-512-maskable.png     512×512 · 불투명 배경 ✓
│   └── apple-touch-icon-180.png  180×180 · 불투명 #2B2836 ✓
├── CLAUDE.md
├── SCHEMA.md
├── SPEC.md
├── PROGRAM.md
└── HANDOFF.md          # 세션 인계 · 매번 갱신
```

**아이콘은 실측으로 검증됐다** (2026-08-11). 치수·투명도 모두 정상이므로
함정 ②는 이미 해결된 상태다. 아이콘을 다시 만들지 말 것.

---

# 0단계 — 알려진 버그

전부 실제 코드에서 확인된 것이다. 줄 번호는 검토 시점(2026-08-11) 기준.

## 0-1. 날짜가 UTC라 아침 세션이 전날로 기록된다 ★ 최우선

```js
today = () => new Date().toISOString().slice(0,10)   // :360
```

한국은 UTC+9다. **오전 9시 이전에 저장하면 어제 날짜가 찍힌다.**
PROGRAM.md는 사용자가 "아침 또는 낮"에 운동한다고 전제한다 — 일상적으로 터진다.

화면 상단 날짜(`:807`)는 `getMonth()/getDate()`로 로컬을 쓰기 때문에
**화면은 8/11, 저장은 8/10**이 된다. 두 값이 다르다는 것으로 재현을 확인할 수 있다.

SCHEMA.md 「시각 규칙」의 `localDate()`로 교체한다.
`toISOString()`을 쓰는 곳이 더 있는지 **파일 전체를 검색할 것.**

덧붙여 **시각을 저장하는 필드를 새로 만들지 말 것** — 날짜만 남긴다
(SCHEMA.md 「시각을 저장하지 않는다」). 소요 시간은 `durationSec`, 변경 추적은 `rev`다.

## 0-2. `probeStorage()`가 정의돼 있지 않다

`:808`에서 호출하는데 함수 본체가 파일에 없다. ReferenceError가 나서
**바로 뒤의 `envCheck()`가 실행되지 않는다.** 하단 환경 표시줄이 늘 비어 있는 이유다.

덧붙여 `storageOK`는 `:370`에서 `null`로 선언된 뒤 **어디에서도 대입되지 않는다.**
`:628` `:664` `:671`의 `storageOK===false` 분기는 영원히 거짓이다.

즉 **저장 실패 배너와 백업 유도 UI는 처음부터 동작한 적이 없다.**
1단계에서 저장 어댑터를 만들 때 `probeStorage`를 함께 만들고 `storageOK`를 세팅한다.

## 0-3. 반복 수 없이 ✓를 누르면 그 세트가 조용히 사라진다

```js
const arr = (entries[i]||[]).filter(x => x && x.done && x.r);   // :657
```

저장 시 `r`이 없는 세트를 버린다. 그런데 진행률은 `done`만 센다(`:498`).
**화면엔 "3/3 세트", 저장된 건 0세트**가 되고 아무 경고도 없다.

CLAUDE.md 원칙 3("저장 실패는 조용히 넘어가지 말 것")에 걸린다.

**고치는 방식** — 반복 칸이 비어 있으면 ✓를 완료 처리하지 않고 **반복 칸으로 포커스를
옮긴다.** 저장 시점에 경고하면 이미 45분이 지난 뒤라 되돌리기 어렵다.
프리필 덕에 반복 칸이 실제로 비는 건 각 Day 첫 세션뿐이라 마찰이 거의 없다.

## 0-4. 세션 도중 앱이 죽으면 전부 날아간다

```js
const KEY="workout-log", DRAFT="workout-draft"   // :359  DRAFT는 선언만
function saveDraft(){ dirty=true; markUnsaved(); }   // :371  「저장하지 않음」
```

`DRAFT` 키는 한 번도 쓰이지 않는다. `saveDraft()`는 이름과 달리 아무것도 저장하지 않고
"미저장" 배지만 켠다. 지금은 저장소가 죽어 있어 티가 안 났지만,
**PWA로 옮기면 가장 자주 겪을 사고**가 된다 — 45분 기록하다 iOS가 앱을 정리하면 소실.

1단계의 자동 저장으로 해결한다. **기존 코드를 살리는 게 아니라 새로 만드는 작업이다.**

---

# 1단계 — 저장 계층과 데이터 구조

**여기가 이 프로젝트의 본체다.** 어댑터만 끼우면 끝나는 작업이 아니다.

## 1-1. 저장 어댑터

`window.storage` 호출은 **3곳**이다.

| 위치 | 현재 호출 |
|---|---|
| `load()` `:365` | `window.storage.get(KEY,false)` |
| `setRetry()` `:380` | `window.storage.set(key,val,false)` |
| `envCheck()` `:797` | `window.storage.list("",false)` |

어댑터 하나를 만들고 위 3곳이 그것을 쓰게 한다. **호출부 구조는 유지할 것** —
재시도와 백업 UI가 그 위에 얹혀 있다.

```js
const DB = {
  get(k){ const v = localStorage.getItem(k); return v===null ? null : {value:v}; },
  set(k,v){ localStorage.setItem(k,v); },
  del(k){ localStorage.removeItem(k); },
  probe(){ const t="__probe"; localStorage.setItem(t,"1"); localStorage.removeItem(t); }
};
```

- 전부 `try/catch`로 감싼다. 사파리 프라이빗 모드에서는 `localStorage`가 예외를 던진다
- **`probeStorage()`를 새로 만들고 `storageOK`를 대입한다** (0-2)
- 실패하면 기존 배너와 내보내기 경로가 뜨게 둔다

## 1-2. 데이터 구조 교체 ← 가장 큰 작업

현재 구조와 SCHEMA.md 요구가 근본적으로 다르다.

| | 현재 코드 | SCHEMA.md |
|---|---|---|
| 키 | `workout-log` 하나 | `wk:index` · `wk:2026-08` · `wk:program` · `wk:draft` |
| 종목 | **객체** `rec.ex[이름]` `:659` | **배열** `exercises: [{name, planned, …}]` |
| 이력 | `store.history` + 60개 컷 | 월별 파일 분산, 상한 없음 |
| Day 순환 | `history[0].day`에서 유도 `:369` | `index.rotation` |
| 프로그램 | 코드 상수 `DAYS` `:253` | `wk:program` — 사용자가 편집한다 |

**마지막 줄이 함정이다.** 기록이 월별로 쪼개지면 `nextDay()`가 깨진다 —
월초에 앱을 켜면 그 달 파일이 비어 있어 항상 Day 1로 돌아간다.
반드시 `rotation`에서 읽도록 바꿀 것.

연쇄로 손대야 하는 함수는 **7개**다:
`load` `finish` `logText` `parseBackup` `applyRestore` `renderBrief` `nextDay`

대체 종목 프리필은 SCHEMA.md 「대체 종목 프리필 — 읽기 전용 규칙」을 그대로 따른다.
**읽기 규칙과 쓰기 규칙을 섞지 말 것.**

## 1-3. 자동 저장을 되살린다

localStorage는 호출 비용이 없다. "세션 저장 버튼을 눌러야만 저장"에서 되돌린다.

- 입력 이벤트마다 저장하되 **1초 디바운스**
- 세트 완료(✓) 시 즉시 저장
- 화면 복귀(`visibilitychange`) 시 진행 중이던 세션 복원 (0-4)
- `beforeunload` 경고 제거 (`:390`) — 더 이상 잃을 게 없다

## 1-4. wk:program 자리를 지금 잡는다

편집 UI는 5단계지만 **데이터 자리는 여기서 만든다.**
앱이 처음 켜질 때 코드의 `DAYS`를 그대로 복사해 `wk:program`에 넣고,
이후 모든 화면이 **코드 상수가 아니라 이 키를 읽게** 한다.

나중에 넣으면 이미 쌓인 데이터를 이사시켜야 하고, 마이그레이션이 하나 더 생긴다.
지금은 복사 한 번이면 끝난다. 코드의 `DAYS`는 **되돌리기용 원본**으로 남는다.

---

# 2단계 — PWA 껍데기와 배포

**여기서 배포한다. 완성한 뒤에 배포하지 않는다.**
3단계의 아이폰 검증을 시작하려면 배포된 HTTPS 주소가 필요하기 때문이다.

## 2-1. manifest.json

```json
{
  "name": "운동 세션",
  "short_name": "운동",
  "start_url": "./",
  "scope": "./",
  "display": "standalone",
  "orientation": "portrait",
  "background_color": "#2B2836",
  "theme_color": "#2B2836",
  "lang": "ko",
  "icons": [
    { "src": "icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "icons/icon-512.png", "sizes": "512x512", "type": "image/png" },
    { "src": "icons/icon-512-maskable.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable" }
  ]
}
```

`start_url`·`scope`를 **상대경로 `./`로 둘 것** (함정 ①).

## 2-2. index.html `<head>`

```html
<link rel="manifest" href="manifest.json">
<link rel="apple-touch-icon" href="icons/apple-touch-icon-180.png">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="운동">
<meta name="theme-color" content="#2B2836">
```

`viewport-fit=cover`는 이미 들어 있다. 유지할 것.
`<title>`이 `운동 세션 · 최종 3`(`:6`)으로 돼 있다 — `운동 세션`으로 정리한다.

## 2-3. sw.js

- 캐시 이름에 **버전 포함**: `const CACHE = "workout-v1"` — 배포마다 올린다 (함정 ③)
- `install`에서 `index.html`, `manifest.json`, 아이콘 4개를 캐싱
- `activate`에서 이름이 다른 옛 캐시 삭제
- `skipWaiting()` + `clients.claim()`
- `fetch` 전략: **network-first + 2초 타임아웃 → 캐시**

타임아웃이 핵심이다. 신호가 **없는** 것보다 **약한** 것이 나쁘다 —
완전 오프라인이면 `fetch`가 즉시 실패해 캐시로 넘어가지만, 신호가 1칸이면
요청이 10초 넘게 매달렸다가 실패한다. 헬스장 지하에서 앱을 열 때마다 흰 화면을
그만큼 본다. `Promise.race`로 2초를 걸고 캐시로 넘긴다.

cache-first로 하지 않는 이유는 수정해도 옛 화면이 계속 뜨기 때문이다.

**캐시 버전을 화면 하단 환경 표시줄에 띄운다.** 함정 ③("고쳤는데 왜 안 바뀌지")을
폰에서 눈으로 즉시 확인할 수 있다.

### 등록 코드 (index.html 맨 아래)

```js
if ("serviceWorker" in navigator) {
  window.addEventListener("load", () => {
    navigator.serviceWorker.register("sw.js").catch(()=>{});
  });
}
```

`"sw.js"` 상대경로로 등록할 것. `"/sw.js"`는 서브경로에서 404가 난다 (함정 ①).

## 2-4. 배포

**저장소는 이미 있다** — https://github.com/LeRanunculus/workout (public, `main`).
`git init` 이나 `gh repo create` 를 다시 하지 말 것.

```bash
git add -A && git commit -m "PWA 껍데기" && git push
```

**Pages는 아직 꺼져 있다.** 여기서 켠다 — Settings → Pages → Branch: `main` / root.
`gh api -X POST repos/LeRanunculus/workout/pages -f 'source[branch]=main' -f 'source[path]=/'`
로도 된다. 반영까지 1~2분.

켠 직후 **서브경로부터 확인할 것** (함정 ①) —
`https://leranunculus.github.io/workout/manifest.json` 이 200으로 뜨는가.

---

# 3단계 — 아이폰 검증 ← 관문

**여기를 통과해야 1차가 끝난 것이다.** 아래 검증 체크리스트의 "아이폰" 절을
실기기에서 직접 확인한다. 추정으로 체크하지 말 것.

특히 이 세 가지가 0단계·1단계 수정의 회귀 테스트다.

- **오전 8시에 저장해도 오늘 날짜로 기록되는가** (0-1)
- **세트 입력 → 앱 완전 종료 → 재실행 → 기록이 남아 있는가** (0-4)
- **앱을 나갔다 30초 뒤 돌아와도 타이머가 맞는가** (4-4)

## 여기서 멈추고 한동안 써볼 것

3단계를 통과하면 **1차가 끝난 것이고, 그것만으로 지금보다 압도적으로 낫다.**
곧바로 4단계로 넘어가지 말고 **실제로 몇 주 써본다.**

4~7단계는 각각 만만치 않은 작업이다. 한 번에 다 만들려다 **아무것도 쓸 수 없는
상태가 길어지는 것이 최악**이다. 써보면서 불편한 것부터 붙이면 순서도 저절로 정해진다.

4단계 순서(RIR·노트·타이머·되돌리기)는 권고이지 고정이 아니다.
실제로 겪은 불편이 우선한다.

---

# 4단계 — 입력 항목과 AI 경로

현재 앱은 종목별 `{중량, 반복}`만 받는다. 분석에 필요한 것이 빠져 있다.
**추가하되 헬스장에서의 탭 수를 최소로** 설계한다.

## 4-1. RIR 칩 — 종목당 1탭

세트마다 묻지 않는다. 한 종목의 세트를 다 마치면 카드 하단에 칩 3개가 뜬다.

```
여유 있었음  ·  딱 맞았음  ·  한계였음
```

→ `easy` / `ontarget` / `limit`. 안 고르면 `null`로 저장하고 넘어간다.
세트마다 받으면 17~21탭이지만 이렇게 하면 6~7탭이다.

**왜 필요한가** — 이게 없으면 정체 원인을 구분할 수 없다.
"벤치 7.5kg 10회 정체"가 무게 문제(RIR 0)인지 덜 민 것(RIR 3)인지 알 방법이 없다.

## 4-2. 세션 노트 · 웜업 · 소요 시간

- **노트** — 세션 저장 화면에 한 줄 입력. 비워도 된다. "어깨 뻐근", "잠 못 잠" 수준
- **웜업·마무리** — 이미 체크박스로 받고 있다(`blocks`). **최종 기록에 포함만 시키면 된다.**
  현재는 저장 시 버려진다. 추가 입력 0
- **소요 시간** — 첫 세트 완료부터 저장까지를 재서 `durationSec` 하나로 저장한다.
  **시각은 남기지 않는다** (SCHEMA.md 「시각을 저장하지 않는다」). 자동이라 입력 0

## 4-3. AI 기능 — 앱 안에서 호출하지 않는다

`ask()`(`:399`)가 `api.anthropic.com`을 **키 없이** 호출한다. 아티팩트 환경이 인증을
대신 해줘서 되던 것이고, GitHub Pages에서는 CORS·401로 100% 실패한다.
CLAUDE.md 절대원칙 1과도 충돌한다.

| 기능 | 처리 |
|---|---|
| 세션 코치 `:680` | 버튼을 **「코칭 프롬프트 복사」**로 바꾼다 |
| 대체 종목 더 찾기 `:596` | **버튼 삭제.** 종목당 2개가 `DAYS`의 `e[10]`에 이미 있다 |

`ask()` 함수 자체를 제거한다.

**코칭 프롬프트 복사** — 담을 항목은 SCHEMA.md 「코칭 프롬프트」 표를 그대로 따른다.
오늘 세션 · **오늘 Day의 목표** · 같은 Day 이전 2회 · **최근 러닝 3회** ·
**주차와 디로드 여부** · 사용자 상황 · 답변 형식.

지금 프롬프트(`:692`)에는 **러닝과 주차가 없다.** 러닝이 빠지면 하체 정체의 원인을
영영 무게 문제로만 보게 된다. 목표를 프로그램에서 읽는 것도 중요하다 —
사용자가 종목을 편집했으면 기본값을 가정하면 안 된다.

**버튼 위치** — 세션을 저장하면 **바로 손에 닿는 자리**에 나와야 한다.
지금은 저장 후 백업 섹션이 스크롤돼서 나타나는 구조인데, 매 세션 쓰는 버튼이
두 번째 스크롤에 있으면 쓰지 않게 된다.

⚠️ **iOS 클립보드 주의** — `navigator.clipboard.writeText()`는 사용자 제스처 안에서
**즉시** 호출돼야 한다. 앞에 다른 `await`가 끼면 권한이 만료돼 조용히 실패한다.
문자열을 동기적으로 다 만든 뒤 `writeText`를 **첫 `await`**로 둘 것.
기존 백업 복사 버튼(`:756`)이 이미 그 형태다.

## 4-4. 휴식 타이머 — 백그라운드에서 멈춘다

```js
timer = setInterval(tick, 1000);  …  left--;   // :639, :647
```

카운터를 1초마다 1씩 빼는 방식이다. iOS는 앱이 백그라운드로 가면 `setInterval`을
정지시킨다. 세트 사이 쉬면서 카톡을 확인하고 돌아오면 **멈춰 있던 만큼 늦게 울린다.**
Wake Lock은 화면이 켜져 있을 때만 유효하므로 이걸 막지 못한다.

`Date.now()` 기준 종료 시각을 잡고 남은 시간을 매번 계산한다.

⚠️ **고치면 알람이 안 울리게 된다.** 현재 조건이 `if(left===0)`(`:646`)인데,
종료 시각 방식에서는 백그라운드에서 돌아온 순간 `3 → -12`로 건너뛰어
**정확히 0인 순간이 없다.** 조건을 "0 이하 + 아직 안 울림"의 **엣지 트리거**로 바꾼다.
덧붙여 **30초 넘게 지나서 돌아왔으면 울리지 않는다** — 이미 지난 휴식에 뒤늦게
삑 소리가 나면 혼란만 준다.

## 4-5. 주차 표시

브리핑에 `5주차 · 4바퀴`처럼 **둘 다** 표시한다.

**디로드 판정은 바퀴 수 기준이다** — `rotation.cycles`가 6 또는 12에 도달하면 배지.
달력 주차가 아니다. PROGRAM.md의 "6주차"는 같은 문서의 "주 5회 = 정확히 1주에 한 바퀴"에
비춰 보면 **처음부터 6바퀴를 뜻하고 있었다.** 빈도가 흔들리면 둘이 벌어지고,
누적 부하는 세션 수를 따라간다.

배지는 **권고에 그친다.** 세트 수를 자동으로 줄이지 않는다.
계산 규칙은 SCHEMA.md 「디로드는 사이클 기준이다」에 있다.

## 4-6. 문구 정리

- `:239` "세션이 끝나면 **한 번에 복사하기를 꼭 누르세요**" — 저장소가 죽어 있던 시절의
  문구다. localStorage가 살아나면 불필요한 불안을 만든다. "백업용 · 선택"으로 낮춘다
- `:798` `envCheck`의 **"진동" 표시등을 제거**한다. iOS는 `navigator.vibrate`를
  지원하지 않아 아이폰에서 항상 ○다. 매번 꺼진 등을 보여줄 이유가 없다
- 대신 그 자리에 **캐시 버전**을 표시한다 (2-3)

## 4-7. 유지할 것

**내보내기(기록 백업) 버튼은 그대로 둔다.** 두 가지 이유로 필요하다.

1. 폰 교체·사파리 데이터 삭제 대비 백업 — **7단계 전까지 유일한 복구 수단이다**
2. 사용자가 이 텍스트를 Claude 채팅에 붙여넣어 분석을 받는다 — 주 사용 경로다

포맷은 SCHEMA.md 「클립보드 텍스트 — 두 번째 계약」을 따른다.
**세션 이력 전체를 포함하고 최신을 맨 위에 둔다.**
5단계 이후에는 **편집된 프로그램도 함께 들어간다.**

## 4-8. 대체 종목을 기억한다

지금 `swaps`는 메모리에만 있어서 **세션마다 초기화된다.** 헬스장에 아예 없는 기구가
있으면 갈 때마다 같은 대체를 다시 고르게 된다.

`wk:index`에 종목별 마지막 대체를 기억해두고, 다음에 그 자리에 오면
**대체 종목이 이미 선택된 상태로** 띄운다. 원래 종목으로 되돌리는 버튼은 그대로 둔다.

**이것이 5단계(프로그램 편집)의 부담을 크게 줄인다.** 없는 기구 문제의 대부분은
여기서 해결되고, 편집은 "미리 지정된 대체 2개도 없을 때"를 위해 남는다.
구현은 몇 줄이고 5단계를 기다릴 이유가 없다.

## 4-9. 잘못 입력한 기록을 되돌린다 ★ 지금 빠져 있는 것

5kg을 50kg으로 잘못 넣고 저장하면 **고칠 방법이 전혀 없다.**
`lastByExercise`가 50kg을 기억하고, 다음 세션 프리필이 50kg으로 뜨고,
`allTop`이면 거기서 또 증량을 제안한다. 잘못된 값이 계속 굴러간다.

프로그램은 편집할 수 있는데 기록은 못 고치는 상태다. 두 겹으로 막는다.

**① 입력 시점 — 비정상 값 경고 (두 종류)**

| 검사 | 기준 | 잡히는 예 |
|---|---|---|
| 중량 | 지난 기록의 **3배** 초과 | `5kg → 50kg` (10배) |
| 반복 | 목표 범위를 **크게 벗어남** | 목표 `8-12`인데 `2` |

중량만 검사하면 `12회 → 2회` 같은 실수를 놓친다.
**목표 반복 범위는 프로그램에 이미 있으므로 공짜다.**

**② 저장 직후 — 되돌리기**
저장 화면에 `되돌리기` 버튼을 둔다. 누르면 그 세션이 취소되고
`lastByExercise`와 `rotation`이 이전 상태로 복구된다.
오타는 대개 그 자리에서 알아채므로 이것으로 대부분 커버된다.

**전체 기록 편집기는 만들지 않는다.** 지난 세션을 목록에서 골라 고치는 화면은
범위가 크다. 위 두 가지로 실제 사고의 대부분이 막히고,
**7단계가 끝나면 원격 JSON이 곧 편집기가 된다** (SCHEMA.md 「원격에서 직접 고치기」).
웹에서 값을 고치고 앱에서 복원을 누르면 된다 — 단, **`rev`를 1 올려야** 한다.

그 전까지는 백업 텍스트를 손으로 고쳐 복원하는 경로가 있다.

---

# 5단계 — 프로그램 편집

**데이터 자리는 1-4에서 이미 잡혀 있다.** 여기서는 그것을 고치는 화면만 만든다.
규칙은 SCHEMA.md 「프로그램 — wk:program」을 그대로 따른다.

## 왜 필요한가

헬스장에 그 기구가 **아예 없을 수 있다.** 매번 대체 종목을 고르는 것보다
프로그램을 한 번 고치는 게 맞다. 부상으로 빼야 할 때도, 4주 재검토에서
조정할 때도 마찬가지다.

PROGRAM.md의 "임의로 바꾸지 말 것"은 **AI에게 하는 말**이다.
사용자가 못 바꾼다는 뜻이 아니다.

## 할 수 있어야 하는 것

| 조작 | 비고 |
|---|---|
| 종목 이름 변경 | **기록이 이어져야 한다** — `was`에 옛 이름을 남긴다 |
| 종목 추가 | **6개만 묻는다** — 이름·세트·반복범위·휴식·증량단위·맨몸여부 |
| 종목 제거 | 프로그램에서만 뺀다. **기록은 지우지 않는다** |
| 순서 변경 | 배열 순서 = 화면 순서 |
| 세트·반복·휴식·증량 단위 수정 | `top`은 `"8-12"`에서 자동으로 뽑는다 |
| Day 추가·삭제 | `days` 길이로 순환이 정해진다. `nextDay()`가 따라간다 |
| **기본 프로그램으로 되돌리기** | 없으면 망가뜨렸을 때 앱을 지우는 수밖에 없다 |

## 어디에 두나

**총정리 화면 아래.** 헬스장에서 하는 조작이 아니라 집에서 가끔 하는 일이다.
**세션 화면의 탭 수는 1탭도 늘리지 않는다.**

세션 중 일회성 교체는 이미 있는 **대체 종목** 기능이 맡는다. 둘은 다른 도구다 —
대체는 오늘만이고 `planned`에 원래 종목이 남지만, 편집은 다음부터 계속이다.

## 보여줄 것 / 보여주지 않을 것

그 Day의 **총 세트 수 변화**만 표시한다 (`17세트 → 20세트`).
근육군별 분할 볼륨은 계산하지 않는다 — PROGRAM.md의 표가 근거인데
앱이 그걸 다시 계산하면 문서와 어긋날 때 어느 쪽이 맞는지 알 수 없게 된다.

**편집한 프로그램은 백업 텍스트에 포함된다** (SCHEMA.md 「백업 내보내기」).
그래야 폰이 죽었을 때 커스텀 프로그램을 복구할 수 있고, PROGRAM.md도 갱신할 수 있다.

---

# 6단계 — 러닝 탭

상단에 축 전환을 둔다. `[ 웨이트 ] [ 러닝 ]`

웨이트는 Day 순환, 러닝은 **날짜별 독립 기록**이다.
같은 날 둘 다 있을 수도, 러닝만 있을 수도 있다.

**시안에서 아래 내용이 전부 확정됐다 (2026-08-17).**
실물 프로토타입 — https://claude.ai/code/artifact/7e919a59-339c-4c4c-a7cc-66a9c0cfd9b4
**시안과 이 문서가 어긋나면 이 문서가 기준이다.**

## 입력 (30초 안에 끝나야 한다)

| 항목 | 방식 |
|---|---|
| 거리 | 한 칸. **숫자만** 치면 뒤 2자리가 소수 — `533` → 5.33 |
| 시간 | 한 칸. **숫자만** 치면 뒤 2자리가 초 — `2631` → 26:31 |
| 종류 | 칩 — 이지 / 평소 / 롱런 / 인터벌 (**넷 다 둔다**) |
| 평균 심박 | 접힘. 선택 |
| 케이던스 | 접힘. 선택 |
| 메모 | 접힘. 선택 |

- **소수점도 콜론도 치지 않는다.** 두 칸 다 `inputmode="numeric"`.
  형식과 경계값은 SCHEMA.md 「입력 형식」에 있다 — 그대로 따를 것
- **페이스는 입력받지도 저장하지도 않는다.** 거리·시간에서 계산해 **표시만** 한다
- ⚠ **거리 프리필은 「직전 값」이 아니라 `5.00` 고정이다** (2026-08-17 정정).
  밤 러닝이 5km 고정이 되면서 직전 값 프리필이 어긋났다 — 롱런 10.9km 다음 날
  10.9 가 떠서 매번 지워야 한다. **회색(ghost)으로** 띄우고 탭하면 풀린다
  (앱의 중량 칸과 같은 규칙). 안 건드리면 5.00 으로 저장된다
- 심박·케이던스는 기본 접어둔다. 매번 강요하면 기록 자체를 안 하게 된다
- **입력 규칙 안내(힌트) 줄은 두지 않는다.** 혼자 쓰는 앱이라 몇 번이면 손에 붙는다

## 목록 화면

```
08.17 월 ~ 08.23 일              목표 25km
10.1 km
━━━━━━━━━━━━━━━━━━━━━━━━━
2026-W34 · 2회        지난주 30.9km · 5회

[ + 오늘 러닝 기록 ]

── 이번 주 ──────────────
08.17 (월)  5.20km  28:04   평소   5:24/km
            ♥152  🦶176
── 2026-W33 ─────────────
08.16 (일)  4.87km  26:31   이지   5:27/km
```

주간 누적을 상단에 둔다. 목표 25km 대비 위치가 바로 보여야 한다.
**주는 ISO 8601 — 월요일 시작, 일요일 끝이다** (SCHEMA.md 「시각 규칙」).
`getDay()`(일요일=0)를 그대로 쓰면 하루 어긋난다.

**주 범위를 날짜로 적는다** (`08.17 월 ~ 08.23 일`). `2026-W34` 만으로는 어디부터
어디까지인지 안 보인다. 주차 번호는 아래줄로 내린다.
**목록도 주 경계에서 구분선으로 가른다** — 일요일에 뛴 것이 지난주로 가는 게 눈에 보여야 한다.

**축 전환은 상단에 `[ 웨이트 ] [ 러닝 ]`.** 웨이트 화면의 탭 수는 **1탭도 늘리지 않는다.**

**애플 건강 연동은 하지 않는다.** 걷기 거리와 종일 심박이 섞여 데이터 품질이 떨어진다.
자동화 요청이 와도 거부할 것 — 의도된 결정이다.

## 다 되면 — 코칭 프롬프트 계약을 채운다

SCHEMA 「코칭 프롬프트」 7항목 중 마지막 하나가 **「최근 러닝 3회」**다.
러닝 탭이 들어가면 그 블록을 프롬프트에 더해 **계약을 완성한다.**
`COACH-BRIEF.md` 3절 「러닝 데이터는 아직 안 온다」도 **같이 고칠 것.**

---


# 7단계 — GitHub 동기화

**SCHEMA.md의 「동기화 규칙」을 그대로 구현한다.** 요약하면:

- 폰이 원본, GitHub는 복제본. **단순 덮어쓰기 금지** — 폰 재설치 시 원격이 지워진다
- id 기준 병합. **원격에만 있는 기록은 보존한다**
- 동기화는 **버튼을 눌렀을 때만.** 자동 업로드 시도 없음
- 상단에 상태 한 줄: `● 미전송 3세션  [동기화]`
- 실패해도 로컬 데이터는 그대로. 미전송 카운트 유지

## 설정 화면

GitHub fine-grained PAT + 리포 정보를 입력받아 `wk:settings`에 저장한다.
**토큰을 코드에 하드코딩하지 말 것.** 리포는 public이다.
**`wk:settings`는 절대 동기화하지 않는다** — 토큰이 들어 있다.

권한 안내를 화면에 표시한다: 해당 리포 **하나만**, Contents 쓰기만.

## 착수 전에 정할 것 — 기록을 어디에 둘 것인가

**앱 코드는 Pages 때문에 public이어야 하지만 기록은 분리할 수 있다.**
후보 비교와 확인 사항은 SCHEMA.md 「기록을 어디에 올릴 것인가」에 있다.
**secret gist가 유력하다** — Claude가 raw URL로 읽으면서 검색·프로필에는 안 뜬다.

착수 첫 작업은 **토큰 확인**이다. gist API가 fine-grained PAT로 되는지,
classic PAT의 `gist` 스코프가 필요한지 먼저 확인할 것. 여기서 갈린다.

## 이 단계가 끝나면 복붙이 필요 없어진다 ← 사용자가 원하는 것

기록이 고정 주소에 놓인다. 같은 public 리포에 둔다면 이런 형태다.

```
https://raw.githubusercontent.com/<사용자>/workout/main/data/2026/08.json
```

secret gist라면 `gist.githubusercontent.com/<사용자>/<해시>/raw/2026-08.json`이다.
**버전 없는 주소를 쓸 것** — 커밋 해시가 붙은 주소는 수정할 때마다 바뀐다.

Claude 채팅에 **이 주소를 한 번 알려주면** 이후로는 붙여넣기 없이
"이번 달 벤치 정체된 것 같은데 봐줘"만으로 분석을 받을 수 있다.
동기화 버튼만 누르면 된다.

**설정 화면에 주소를 그대로 표시하고 복사 버튼을 붙인다.**
사용자가 주소를 직접 조립하게 두면 쓰지 않게 된다.

### 앱이 Claude를 직접 부르는 것은 여전히 안 한다

원한다 해도 할 수 없다 — API 키를 넣으면 **public 리포에 그대로 공개된다.**
키를 숨기려면 서버가 필요한데 CLAUDE.md가 백엔드를 금지한다.
게다가 API를 직접 쓰면 구독과 별개로 요금이 나가고, 헬스장 지하에서
AI 응답을 기다릴 이유도 없다. 위의 두 경로가 최선이다.

---

# 함정 5가지

## ① 서브경로 — 가장 흔한 실패

Pages는 `https://<user>.github.io/workout/`로 서비스된다.
`start_url: "/"`, `scope: "/"`, `register("/sw.js")`처럼 절대경로를 쓰면
**서비스워커 등록이 실패하고 PWA가 아예 성립하지 않는다.**
전부 상대경로(`./`, `sw.js`, `icons/…`)로 통일할 것.

## ② apple-touch-icon — **이미 해결됨**

manifest의 아이콘만으로는 iOS 홈 화면에 회색 스크린샷이 뜬다.
`<link rel="apple-touch-icon">`으로 180×180을 별도 지정해야 한다.

**현재 파일은 검증 완료다** — 180×180, 모서리까지 불투명 `#2B2836`.
maskable도 배경이 채워져 있다. **아이콘을 다시 만들지 말 것.**
`<link>` 태그만 넣으면 된다 (2-2).

## ③ 캐시 버전

`CACHE` 이름을 안 올리면 수정 후 배포해도 폰에는 옛 화면이 그대로 뜬다.
"고쳤는데 왜 안 바뀌지"의 99%가 이것이다. **배포할 때마다 v2, v3로 올린다.**
버전을 화면에 표시해두면 폰에서 즉시 확인할 수 있다 (2-3).

## ④ 사파리와 홈 화면 앱은 저장소가 분리된다

사파리에서 테스트하며 넣은 기록은 홈 화면 앱에 안 보인다. 반대도 마찬가지.
버그가 아니다. **검증은 반드시 홈 화면 앱에서** 한다.

## ⑤ iOS PWA에 없는 것

- 백그라운드 동기화 없음 — 앱이 열려 있을 때만 동작
- **`setInterval`이 백그라운드에서 멈춘다** — 타이머를 종료 시각 기준으로 (4-4)
- `navigator.vibrate` 미지원 — 알림음으로 대체돼 있다. 진동 코드를 되살리지 말 것
- 홈 화면 앱은 7일 저장소 삭제 대상이 아니다 (사파리 탭은 대상). 주 4~6회면 안전

---

# 검증 체크리스트

## PC (localhost) — 빠른 반복

- [ ] 콘솔 에러 0건 (`probeStorage` ReferenceError 포함)
- [ ] 세트 입력 → 새로고침 → 값이 남아 있음
- [ ] Day 전환, 대체 종목, 타이머 동작
- [ ] 지난 기록 불러오기(붙여넣기) 동작 — **v1 옛 텍스트도 읽히는가**
- [ ] `manifest.json` 200 응답, 아이콘 4개 200 응답

## 데이터

- [ ] 저장된 JSON이 SCHEMA.md 구조와 정확히 일치
- [ ] **PC 시계를 오전 8시로 바꾸고 저장 → 그날 날짜로 기록됨** (0-1 회귀)
- [ ] 대체 종목 사용 시 `planned`에 원래 이름이 남음
- [ ] **폴백 프리필에 "(다른 종목)" 표시가 뜨고 증량 배지는 안 뜸**
- [ ] **반복 칸을 비운 채 ✓ → 완료되지 않고 반복 칸으로 포커스** (0-3 회귀)
- [ ] RIR 칩 미선택 시 `null`로 저장되고 오류 없음
- [ ] 월이 바뀌면 새 월 키가 생기고 `index.json`의 `months`에 추가됨
- [ ] **월이 바뀌어도 Day 순환이 이어짐** (Day 1로 리셋되지 않음)
- [ ] 러닝 페이스 계산이 맞음 (5.2km / 1752초 = 5:37/km)
- [ ] 주간 러닝 누적이 **월요일**에 리셋됨

## 되돌리기 · 대체 기억 (4단계)

- [ ] 지난 기록의 3배 넘는 무게를 입력하면 **저장 전 확인**이 뜸
- [ ] 목표 `8-12`인데 반복을 `2`로 넣으면 **저장 전 확인**이 뜸
- [ ] 저장된 JSON에 **시각이 하나도 없음** — `id`·`durationSec`·`rev`만 (날짜는 있음)
- [ ] 저장 직후 **되돌리기** → 세션이 취소되고 `lastByExercise`가 이전 값으로 복구됨
- [ ] 되돌린 뒤 `rotation`도 이전 상태 (다음 Day가 되돌아감)
- [ ] 대체 종목을 고른 뒤 다음 세션에 **그 대체가 이미 선택된 상태**로 뜸
- [ ] 원래 종목으로 되돌리는 버튼이 여전히 동작함

## 디로드 · 마이그레이션

- [ ] 6바퀴를 완주하면 **디로드 배지**가 뜸 (달력 6주가 아니라)
- [ ] 브리핑에 `n주차 · n바퀴`가 둘 다 표시됨
- [ ] 배지가 떠도 **세트 수는 그대로**
- [ ] `schema` 번호를 올린 데이터를 넣으면 마이그레이션이 돌고 **백업 키가 생김**
- [ ] 마이그레이션을 일부러 실패시키면 **원본이 복구되고 배너가 뜸**

## 프로그램 편집 (5단계)

- [ ] 종목 이름을 바꿔도 **지난 기록과 프리필이 이어짐** — `was`에 옛 이름이 남음
- [ ] 종목을 제거해도 `lastByExercise`의 기록은 남아 있음
- [ ] 다시 추가하면 옛 기록이 되살아남
- [ ] 종목 추가 시 **6개만 묻고** 나머지는 기본값으로 채워짐
- [ ] `"8-12"`를 입력하면 `top`이 12로 자동 설정됨
- [ ] Day를 추가하면 순환이 그 길이를 따라감 (Day 6 다음이 Day 1)
- [ ] **기본 프로그램으로 되돌리기**가 동작함
- [ ] 편집한 프로그램이 **백업 텍스트에 포함됨**

## 클립보드 텍스트

- [ ] 코칭 프롬프트에 **RIR · 러닝 3회 · 주차**가 들어감
- [ ] 목표 반복·RIR을 **편집된 프로그램에서** 읽어옴 (기본값 가정 안 함)
- [ ] 대체한 종목이 `대체(원래 종목)` 형태로 표시됨
- [ ] 백업 텍스트를 다시 붙여넣으면 그대로 복원됨 (왕복 검증)

## 아이폰 (배포 후 홈 화면 앱) — 진짜 검증

- [ ] 홈 화면 아이콘이 회색이 아니라 제대로 뜸 (함정 ②)
- [ ] 실행 시 사파리 주소창 없이 전체화면
- [ ] **비행기 모드에서 열림**
- [ ] **신호가 약한 곳에서도 2초 안에 뜸** (sw 타임아웃, 2-3)
- [ ] 세트 입력 → 앱 완전 종료(멀티태스킹에서 스와이프) → 재실행 → **기록 남아 있음**
- [ ] **세션 도중 홈으로 나갔다 돌아와도 입력 중이던 값이 살아 있음** (0-4)
- [ ] **앱을 나갔다 30초 뒤 돌아와도 타이머가 맞음** (4-4)
- [ ] 2분 타이머 도는 동안 **화면이 안 꺼짐**
- [ ] 타이머 0에서 **소리가 남** (무음 모드 해제 상태)
- [ ] **오전에 세션을 저장하고 날짜를 확인** (0-1 실기기 회귀)
- [ ] 노치·홈바 영역에 UI가 안 잘림
- [ ] 숫자 입력칸 탭 시 **숫자 키패드**가 뜸
- [ ] 내보내기 → 복사 → 메모 앱에 붙여넣기 성공
- [ ] **코칭 프롬프트 복사 → Claude 앱에 붙여넣기 성공** (4-3)
- [ ] 복사 버튼이 **저장 직후 스크롤 없이 손에 닿는 자리**에 있음
- [ ] 7단계 후 — Claude에 리포 주소만 주고 **붙여넣기 없이** 분석을 받음

**아이폰 항목은 실기기에서 직접 확인한 것만 체크할 것.** 추정으로 체크하지 말 것.

---

# 완료 기준

**1차 — PWA (0~3단계)**
> 헬스장에서 비행기 모드로 앱을 열어 한 세션을 기록하고,
> 앱을 완전히 종료했다가 다음 날 다시 열었을 때 지난 중량이 그대로 채워져 있다.
> 오전에 기록해도 날짜가 맞다.

**2차 — 확장과 동기화 (4~7단계)**
> 동기화 버튼을 누르면 GitHub의 `data/2026/08.json`에 그 세션이 나타나고,
> 앱을 지웠다 다시 설치해도 복원으로 기록이 돌아온다.
> 기구가 없는 종목을 프로그램에서 바꿔도 지난 기록이 이어지고,
> Claude 채팅에 리포 주소만 주면 붙여넣기 없이 분석을 받는다.

여기까지가 이 프로젝트의 범위다. 그 이상은 하지 않는다.
