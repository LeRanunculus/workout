# 데이터 스키마

앱·리포·분석이 공유하는 계약이다. **이 문서와 다르게 구현하지 말 것.**
스키마를 바꿔야 하면 `schema` 번호를 올리고 이 문서를 먼저 고친다.

---

## 시각 규칙 ← 새 시각 필드를 만들 때마다 볼 것

**`toISOString()`을 쓰지 않는다.** UTC를 반환하므로 한국(UTC+9)에서는
**오전 9시 이전 세션이 전날 날짜로 기록된다.** 사용자는 아침에 운동한다.

| 형태 | 규칙 | 예 |
|---|---|---|
| 날짜 | 로컬 연·월·일을 직접 조립 | `2026-08-11` |
| 타임스탬프 | 로컬 시각 + **오프셋 포함** | `2026-08-11T08:32:10+09:00` |
| id | `w-`/`r-` + 로컬 시각 분 단위 | `w-2026-08-11T08:32` |

```js
const pad=n=>String(n).padStart(2,"0");
function localDate(d){ d=d||new Date();
  return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`; }
function localStamp(d){ d=d||new Date();
  const o=-d.getTimezoneOffset(), s=o<0?"-":"+", a=Math.abs(o);
  return `${localDate(d)}T${pad(d.getHours())}:${pad(d.getMinutes())}:${pad(d.getSeconds())}`
       + `${s}${pad(Math.floor(a/60))}:${pad(a%60)}`; }
```

이 규칙이 깨지면 **월별 파일 분기·주간 러닝 집계·경과일 계산이 동시에 어긋난다.**
날짜 하나를 잘못 잡으면 세 곳이 조용히 틀린다.

**주(week)는 ISO 8601 — 월요일 시작이다.** `2026-W33` 표기가 ISO이므로 다른 선택은
표기와 모순된다. `getDay()`(일요일=0)를 그대로 쓰지 말 것.

---

## 파일 구조

```
data/
├── index.json          # 요약 + 현재 상태 — 자주 읽힌다. 작게 유지
└── 2026/
    ├── 08.json         # 그 달의 세션 + 러닝
    └── 09.json
```

**왜 월별인가**
- 동기화할 때 그 달 파일 하나만 쓴다 → 전송량이 작고 충돌 위험이 낮다
- 분석할 때 필요한 달만 읽으면 된다
- 4주 단위 프로그램 재검토 주기와 경계가 맞는다
- 일별로 쪼개면 파일이 수백 개가 되고, 연별로 묶으면 매번 전체를 다시 쓴다

---

## index.json

가장 자주 읽히는 파일. **"오늘 뭐 해?"에 답하는 데 필요한 전부**가 여기 있어야
월별 파일을 안 열어도 된다.

```json
{
  "schema": 1,
  "updatedAt": "2026-08-11T22:14:03+09:00",
  "programStart": "2026-08-11",
  "months": ["2026-08"],
  "counts": { "sessions": 1, "runs": 0 },
  "rotation": {
    "lastDay": 1,
    "lastDate": "2026-08-11",
    "nextDay": 2,
    "cycles": 0
  },
  "lastByExercise": {
    "덤벨 벤치프레스": {
      "w": 5, "reps": [12, 12, 12], "rir": "ontarget",
      "allTop": true, "date": "2026-08-11"
    }
  },
  "recentRunWeeks": [
    { "week": "2026-W33", "km": 23.4, "runs": 4 }
  ]
}
```

`rotation`이 Day 순환의 **단일 진실**이다. 옛 코드는 `history[0].day`에서 유도했는데,
기록이 월별 파일로 흩어지면 그 방식은 월초에 깨진다. 반드시 여기서 읽을 것.

### 디로드는 사이클 기준이다 ← 달력 주차가 아니다

`rotation.cycles`는 **완주한 바퀴 수**다. 마지막 Day를 끝내고 Day 1로 돌아갈 때 +1.
`programStart`는 첫 세션의 날짜이고 화면 표시용으로만 쓴다.

```
디로드 = cycles 가 6 또는 12에 도달했을 때
```

**왜 달력 주차가 아닌가.** PROGRAM.md는 "6주차·12주차 디로드"라고 적었지만,
같은 문서의 순환 표가 **주 5회 = 정확히 1주에 한 바퀴**라고 말한다.
즉 "6주차"는 처음부터 **6바퀴를 뜻하고 있었다.**

빈도가 흔들리면 둘이 벌어진다. 주 4회면 한 바퀴가 8~9일이라 6바퀴에 7~8주가 걸리고,
주 6회면 6일이라 5주면 끝난다. **누적 부하는 세션 수에 비례하지 달력에 비례하지 않는다.**
달력으로 세면 주 4회인 사람은 덜 지쳤는데 디로드하고, 주 6회인 사람은 이미 지쳤는데
아직 안 한다.

### 화면 표시

브리핑에 **둘 다** 보여준다 — `5주차 · 4바퀴`.
사용자가 자기 페이스를 가늠할 재료가 되고, 둘이 벌어지는 걸 보면 빈도를 조절하게 된다.

디로드 도달 시 배지를 띄우되 **권고에 그친다.**
**세트 수를 자동으로 줄이지 않는다** — 화면과 PROGRAM.md가 달라지면 어느 쪽이 맞는지
헷갈리고, 컨디션이 좋은데 강제로 줄이면 역효과다. 판단은 사용자가 한다.

### lastByExercise

앱의 중량 프리필과 증량 판정에 그대로 쓰인다.
**종목이 바뀌어도 옛 키를 지우지 말 것** — 나중에 다시 그 종목을 하면 이어져야 한다.

| 필드 | 규칙 |
|---|---|
| 키 | **실제 수행한 종목명.** 대체했으면 대체 종목 이름으로 쓴다 |
| `date` | **반드시 `YYYY-MM-DD`.** `"복원"` 같은 문자열을 넣지 말 것 |
| `rir` | 그 종목 마지막 세션의 RIR 칩. 없으면 `null` |
| `was` | 이름을 바꾼 적이 있으면 옛 이름들. 없으면 생략 |

### 이름을 바꿨을 때 — `was`

**종목 이름이 곧 기록의 키다.** 그래서 이름을 바꾸면 과거 기록이 고아가 된다.
사용자가 프로그램에서 종목 이름을 고치면 앱이 이렇게 처리한다.

```json
"EZ바 컬": { "w": 20, "reps": [12,12,10], "date": "2026-08-11",
             "was": ["케이블 바 컬"] }
```

1. `lastByExercise`의 **키를 새 이름으로 옮긴다** — 프리필이 끊기지 않는다
2. 옛 이름을 `was` 배열에 덧붙인다. 여러 번 바꾸면 순서대로 쌓인다
3. **이미 저장된 세션 파일의 `name`은 고치지 않는다** — 그날 실제로 그렇게 불렀다.
   과거를 소급해 바꾸면 그때의 기록이 아니게 된다
4. 분석할 때는 `was` 사슬을 따라 옛 이름의 기록을 이어붙인다

고유 ID를 부여하는 방식이 정석이지만 스키마를 2로 올리는 대공사다.
이름 변경은 드문 이벤트라 이 방식으로 충분하다고 판단했다. 2026-08-11 결정.

### 대체 종목 프리필 — 읽기 전용 규칙

레그프레스 자리에서 핵스쿼트를 한 뒤, 다음번 화면을 채울 때:

1. **실제 종목 기록이 있으면 그것을 쓴다** (핵스쿼트 칸 → 핵스쿼트 기록)
2. 없으면 **그 자리의 원래 종목 기록으로 폴백**한다
3. 폴백일 때는 **화면에 출처를 밝힌다** — `지난 기록 · 레그프레스 100kg (다른 종목)`
   조용히 뜨면 그대로 들다가 다친다
4. 폴백 기록으로는 **증량 판정을 하지 않는다** — `allTop`을 무시하고 `↑ +2.5kg` 배지를
   띄우지 않는다. 다른 종목에서 목표를 채웠다고 이 종목 무게를 올릴 근거는 없다

**이 규칙은 읽기에만 적용한다.** 저장할 때 `lastByExercise`에 쓰는 키는 **언제나 실제
수행 종목**이다. 폴백을 만들다 원래 종목의 기록을 덮어쓰면, 다음에 레그프레스를 할 때
핵스쿼트 무게가 뜬다. 읽기 규칙과 쓰기 규칙을 섞지 말 것.

---

## 월별 파일 — data/YYYY/MM.json

```json
{
  "schema": 1,
  "month": "2026-08",
  "sessions": [ ... ],
  "runs": [ ... ]
}
```

### 세션 (웨이트)

```json
{
  "id": "w-2026-08-11T14:32",
  "date": "2026-08-11",
  "startedAt": "2026-08-11T14:32:10+09:00",
  "endedAt": "2026-08-11T15:36:44+09:00",
  "day": 1,
  "title": "상체 밀기",
  "note": "어깨 뻐근했음",
  "warmup": { "foamRoll": true, "treadmillMin": 7, "warmupSets": true },
  "finisher": { "name": "천국의 계단", "min": 12 },
  "exercises": [
    {
      "name": "덤벨 벤치프레스",
      "planned": "덤벨 벤치프레스",
      "targetReps": "8-12",
      "targetRir": "2",
      "rir": "ontarget",
      "sets": [
        { "w": 5, "r": 12 },
        { "w": 5, "r": 12 },
        { "w": 5, "r": 12 }
      ]
    }
  ],
  "updatedAt": "2026-08-11T15:36:44+09:00"
}
```

**필드 규칙**

| 필드 | 규칙 |
|---|---|
| `id` | `w-` + 시작 시각. 한 번 정하면 절대 안 바뀐다. 병합의 기준 |
| `day` | 1~5. Day 순환 위치 |
| `note` | 선택. 컨디션 한 줄. 비어도 됨 |
| `exercises` | **배열이다.** 객체로 하면 순서가 사라지고 같은 종목 중복이 불가능해진다 |
| `name` | 실제 수행한 종목 |
| `planned` | 프로그램상 원래 종목. **대체했을 때 원본이 여기 남는다** |
| `rir` | `easy` / `ontarget` / `limit` 중 하나. 종목 단위로 한 번만 |
| `sets` | 완료 표시한 세트만. 중량 `w`, 반복 `r` |
| `w` | 맨몸 종목은 `0` |

**`planned`가 왜 중요한가** — 레그프레스가 차서 핵스쿼트로 바꾼 날이 생기면,
`name`만 저장할 경우 그날부터 별개 종목이 되어 중량 추이가 끊긴다.
`planned`가 있으면 "레그프레스 자리에서 무엇을 했는가"로 계속 추적된다.

**RIR 매핑**

| 값 | 의미 | 대략 |
|---|---|---|
| `easy` | 여유 있었음 | RIR 3 이상 |
| `ontarget` | 딱 맞았음 | RIR 2 |
| `limit` | 한계였음 | RIR 0~1 |

세트마다 묻지 않는다. **한 종목을 끝내면 칩 하나를 탭한다.** 안 고르면 `null`.

### 러닝

```json
{
  "id": "r-2026-08-11T21:40",
  "date": "2026-08-11",
  "km": 5.2,
  "sec": 1752,
  "type": "easy",
  "hr": 152,
  "cadence": 176,
  "note": "",
  "updatedAt": "2026-08-11T21:45:02+09:00"
}
```

| 필드 | 규칙 |
|---|---|
| `km` | 소수 1자리 |
| `sec` | 총 소요 초 |
| `type` | `easy` / `normal` / `long` / `interval` |
| `hr` | 평균 심박. 선택 |
| `cadence` | 평균 케이던스(spm). 선택 |

**페이스는 저장하지 않는다.** `sec / km`로 언제든 계산된다. 중복 저장은 불일치의 원인이다.

---

## 프로그램 — wk:program

`index.html`의 `DAYS` 배열은 **코드 상수가 아니라 데이터다.** 사용자가 종목을 바꿀 수
있어야 하기 때문이다 — 헬스장에 그 기구가 아예 없거나, 부상으로 빼야 하거나,
4주 재검토에서 조정할 때.

```json
{
  "schema": 1,
  "updatedAt": "2026-08-11T22:14:03+09:00",
  "basedOn": "2026-08-11",
  "days": [
    {
      "n": 1, "title": "상체 밀기", "sub": "가슴 · 어깨 · 삼두", "leg": false,
      "run": "6~7km 평소 페이스",
      "ex": [
        { "name": "덤벨 벤치프레스", "sets": 3, "reps": "8-12", "top": 12,
          "rir": "2", "rest": 120, "inc": 2.5, "mode": "kg",
          "target": "가슴", "kind": "대근육", "equip": "프리",
          "cue": "견갑을 벤치에 고정하고…",
          "alts": [["체스트프레스 머신","궤적 고정으로 안전하게"]] }
      ]
    }
  ]
}
```

**앱이 처음 켜질 때** 코드의 기본 프로그램을 그대로 복사해 `wk:program`을 만든다.
이후로는 이 키가 진실이다. 코드의 상수는 **되돌리기용 원본**으로만 남는다.

### 편집 규칙

| 조작 | 규칙 |
|---|---|
| 이름 변경 | `lastByExercise` 키를 옮기고 `was`에 옛 이름을 남긴다 (위 참조) |
| 종목 추가 | **6개만 받는다** — 이름·세트·반복범위·휴식·증량단위·맨몸여부. 나머지는 기본값 |
| 종목 제거 | 프로그램에서만 뺀다. **`lastByExercise`의 기록은 지우지 않는다** |
| 순서 변경 | 배열 순서가 곧 화면 순서다 |
| Day 추가·삭제 | `days` 배열 길이로 순환이 정해진다. `nextDay()`가 자동으로 따라간다 |

`top`(반복 범위 상단)은 `reps` 문자열에서 자동으로 뽑는다 — `"8-12"` → `12`.
사용자에게 따로 묻지 않는다.

**되돌리기를 반드시 제공한다.** 코드의 기본 프로그램으로 복구하는 경로가 없으면
한 번 망가뜨렸을 때 앱을 지우는 것 말고 방법이 없다.

### 편집은 헬스장에서 하는 조작이 아니다

집에서 가끔 하는 일이다. 총정리 화면 아래에 두고, **세션 화면의 탭 수는 1탭도
늘리지 않는다.** 세션 중에 필요한 일회성 교체는 이미 있는 **대체 종목** 기능이 맡는다.

| | 대체 종목 (기존) | 프로그램 편집 (신규) |
|---|---|---|
| 언제 | 오늘 그 기구가 차 있음 | 그 기구가 아예 없음 · 프로그램 조정 |
| 범위 | 이번 세션만 | 다음부터 계속 |
| 기록 | `planned`에 원래 종목이 남음 | 프로그램 자체가 바뀜 |

**Day 구성을 바꾸면 근육군별 볼륨 균형이 달라진다.** PROGRAM.md의 분할 세트 표가
근거인데 앱은 그 계산을 하지 않는다. 화면에는 **그 Day의 총 세트 수 변화만** 보여준다
(`17세트 → 20세트`). 그 이상의 판단은 사용자와 분석이 맡는다.

---

## 동기화 규칙

### 원칙

**폰이 원본, GitHub는 복제본.** 다만 단순 덮어쓰기는 하지 않는다 —
폰을 재설치하면 빈 데이터가 GitHub를 지워버린다.

### 방식: id 기준 병합

동기화 버튼을 누르면 대상 월마다 이렇게 한다.

1. GitHub에서 해당 월 파일과 `sha`를 가져온다 (없으면 빈 파일로 시작)
2. **id 기준으로 병합한다**
   - 폰에만 있는 기록 → 추가
   - **원격에만 있는 기록 → 그대로 둔다** ← 데이터 보호의 핵심
   - 양쪽에 있으면 `updatedAt`이 최신인 쪽을 채택
3. `date` 오름차순 정렬 후 커밋
4. `index.json`을 다시 계산해서 커밋
5. 성공한 기록만 로컬에서 `synced: true`

**`synced` 플래그는 폰에만 둔다.** 커밋되는 파일에는 넣지 않는다 — 리포에서는 의미가 없다.

### 실패 처리

| 상황 | 처리 |
|---|---|
| 네트워크 실패 | 아무것도 안 바뀜. `synced: false` 유지, 미전송 카운트 그대로 |
| sha 충돌(409) | 다시 가져와 병합 후 1회 재시도. 그래도 실패하면 중단하고 사용자에게 알림 |
| 토큰 만료/권한 오류 | 명확한 문구로 표시. 조용히 넘어가지 말 것 |

**부분 성공을 허용한다.** 8월은 성공하고 9월이 실패했으면 8월분만 `synced` 처리한다.

### 복원 (폰 교체·재설치)

동기화의 역방향. `index.json`의 `months`를 읽어 전 월 파일을 내려받고,
로컬 데이터와 같은 규칙으로 병합한다. **로컬을 지우고 덮어쓰지 않는다.**

---

## 저장 위치

| | 무엇이 | 언제 |
|---|---|---|
| 폰 localStorage | 전체 기록 (원본) | 항상, 자동 |
| GitHub | 전송한 것까지 | 동기화 눌렀을 때만 |

**앱은 자동으로 업로드를 시도하지 않는다.** 헬스장에서 신호를 잡느라 버벅이면 안 된다.

로컬 키 구조도 파일과 맞춘다.
```
wk:index          → index.json과 동일 구조
wk:2026-08        → 월별 파일과 동일 구조 + 각 기록에 synced 플래그
wk:program        → 편집된 운동 프로그램 (동기화 대상)
wk:draft          → 진행 중인 세션 스냅샷 (동기화 대상 아님)
wk:settings       → GitHub 토큰, 리포 정보 (동기화 대상 아님 · 토큰이 들어 있다)
```

`wk:program`은 **동기화한다.** 폰을 바꿨을 때 커스텀 프로그램이 함께 돌아와야 한다.
리포에는 `data/program.json`으로 올린다.
`wk:settings`는 절대 올리지 않는다 — 토큰이 들어 있고 리포는 public이다.

이렇게 하면 동기화가 **로컬 객체를 거의 그대로 올리는 일**이 된다. 변환 로직이 줄면 버그도 준다.

---

## 클립보드 텍스트 — 두 번째 계약

JSON만 계약이 아니다. **클립보드로 나가는 텍스트도 계약이다.**
용도가 다른 두 가지가 있고, 담는 내용도 다르다.

| | 백업 내보내기 | 코칭 프롬프트 |
|---|---|---|
| 언제 | 가끔 · 백업할 때 | **세션 끝날 때마다** |
| 범위 | 전체 이력 + 프로그램 | 오늘 + 직전 2회 + 맥락 |
| 받는 쪽 | 메모 앱 · 복원 붙여넣기 | Claude 채팅 |
| 길이 | 길어도 됨 | 짧을수록 좋음 |

### 백업 내보내기

**세션 이력 전체를 포함**하고 **최신 세션을 맨 위**에 둔다.
7단계가 끝나기 전까지 이것이 **유일한 복구 수단**이다.

```
WORKOUT-EXPORT v2

--- 프로그램 (2026-08-11 수정됨) ---
Day 1 · 상체 밀기 · 6종목 17세트
1. 덤벨 벤치프레스 | 3x8-12 | RIR 2 | 휴식 120 | +2.5kg | 프리
2. 인클라인 체스트프레스 머신 | 3x10-12 | RIR 2 | 휴식 90 | +5kg | 머신
Day 2 · 하체 앞쪽 · 6종목 16세트
…

--- 기록 ---
[2026-08-11] Day 1 · 상체 밀기 · 3주차
덤벨 벤치프레스: 5kg x 12, 5kg x 12, 5kg x 12 · 딱맞았음
핵스쿼트: 60kg x 10, 60kg x 10 · 한계였음 · 대체(레그 프레스)
메모: 어깨 뻐근했음
웜업: 폼롤러, 트레드밀 7분 / 마무리: 천국의 계단 12분

[2026-08-10] 러닝: 5.2km 29:12 이지 · 심박 152 · 케이던스 176

--- 누적 기록 (다음 세션에 이 텍스트 전체를 붙여넣으세요) ---
덤벨 벤치프레스: 5kg x 12, 5kg x 12, 5kg x 12
EZ바 컬: 20kg x 12, 20kg x 12 (옛 이름: 케이블 바 컬)
```

**프로그램을 포함하는 이유** — 사용자가 종목을 바꾸면 그 프로그램이 곧 원본이 된다.
텍스트에 없으면 폰이 죽었을 때 커스텀 프로그램을 복구할 방법이 사라진다.
`was`가 있는 종목은 누적 기록 줄에 옛 이름을 괄호로 남긴다.

### 코칭 프롬프트

세션을 저장하면 **바로 손에 닿는 자리**에 복사 버튼이 나온다. 매 세션 쓰는 버튼이
두 번째 스크롤에 있으면 쓰지 않게 된다.

담을 것 — **이 중 하나라도 빠지면 코칭이 어긋난다.**

| 항목 | 왜 필요한가 |
|---|---|
| 오늘 세션 (종목·세트·**RIR**·노트) | RIR이 없으면 정체 원인을 구분할 수 없다 |
| **오늘 Day의 목표** (반복 범위·RIR·증량 단위) | 프로그램이 편집됐을 수 있다. 기본값을 가정하면 안 된다 |
| 같은 Day 이전 2회 | 추이 판단 |
| **최근 러닝 3회** | 하체 정체가 무게 탓인지 전날 7km 탓인지 구분한다 |
| **주차 · 디로드 여부** | 6주차면 조언이 달라진다 |
| 사용자 상황 + 진행 규칙 | 이중 점진, 실패까지 안 감, 감량기 |
| 답변 형식 | 항목당 한 줄, 마크다운 금지 |

**러닝과 주차는 지금 프롬프트에 없다.** 러닝이 빠져 있으면 하체 정체의 원인을
영영 무게 문제로만 보게 된다.

### 파싱 규칙 — 옛 텍스트도 계속 읽혀야 한다

기존 파서는 `종목명: NNkg x NN, ...` 한 줄 문법만 안다.
**이 문법을 깨지 말 것.** 새 정보는 전부 `·` 뒤에 덧붙인다.

| 줄 | 파서 동작 |
|---|---|
| `[날짜] Day N · 제목` | Day 갱신 |
| `이름: NNkg x NN, ...` | 기록으로 읽음. `·` 뒤는 무시해도 되고 읽어도 된다 |
| `메모:` / `웜업:` / `러닝` | 콜론은 있지만 `kg x` 패턴이 없어 **자동으로 건너뛴다** |

v1 텍스트(헤더 없음, `·` 없음)도 그대로 읽힌다. 하위 호환이 유지된다.

**복원 시 `date`에 `"복원"`을 넣지 말 것.** 헤더의 실제 날짜를 쓰고,
헤더가 없는 누적 섹션이면 그 필드를 비워둔다(`null`). 날짜 자리에 날짜가 아닌 값이
들어가면 그대로 `index.json`에 실려 GitHub까지 올라간다.

---

## 스키마 변경 규칙

`schema` 번호를 올리고, 앱 시작 시 마이그레이션을 돌린다.
**옛 데이터를 못 읽는 변경은 금지.** 필드 추가는 자유, 삭제·의미 변경은 번호를 올린다.

### 마이그레이션은 이렇게 돈다

번호만 정해두고 절차가 없으면, 정작 올려야 할 때 그 자리에서 즉흥적으로 짜게 된다.

```
앱 시작 → 각 키의 schema 읽기 → 현재 버전보다 낮으면:
  1. 원본을 백업 키로 복사      wk:2026-08  →  wk:2026-08.bak1
  2. 변환 함수를 순서대로 적용   1→2, 2→3 …  (건너뛰지 않는다)
  3. schema 번호를 올려 저장
  4. 실패하면 원본을 되돌리고 배너를 띄운다
```

| 규칙 | 이유 |
|---|---|
| **변환 전에 반드시 백업 키에 복사** | 변환이 실패하면 되돌릴 것이 있어야 한다 |
| 백업 키는 **한 단계만** 보관 | 무한정 쌓이면 저장 용량을 먹는다 |
| 변환은 **한 단계씩 순서대로** | 1→3 직행 함수를 따로 만들면 조합이 폭발한다 |
| 실패하면 **조용히 넘어가지 않는다** | CLAUDE.md 원칙 3. 화면에 표시하고 내보내기를 권한다 |
| 마이그레이션 전에 **내보내기를 권하는 배너** | 사용자가 스스로 백업할 기회를 준다 |

**필드 추가는 마이그레이션이 아니다.** 없으면 기본값으로 읽으면 된다.
번호를 올리는 건 필드를 **없애거나 의미를 바꿀 때**뿐이다.

5단계(프로그램 편집)가 들어가면 구조가 흔들릴 가능성이 있으니,
**4단계에 들어가기 전에 이 절차의 자리(함수 껍데기)를 만들어 둔다.**
