# 다음 세션이 할 일

**이 파일은 붙여넣기용이 아니다. 세션 시작 때 읽는 문서다.**
`CLAUDE.md` 가 자동으로 읽히고 거기서 이 파일을 가리킨다 — 사용자는
"NEXT-SESSION.md 읽고 이어서 하자" 한 마디만 하면 된다.

반드시 `C:/Users/hr222/projects_hr/workout` 안에서 `claude` 를 실행할 것
(상위 폴더에서 열면 `workout/CLAUDE.md` 가 자동으로 안 읽힌다).

---

이 저장소는 아이폰 전용 오프라인 PWA 운동 기록 앱이다. 이미 배포돼 실사용 중이다.
서비스 https://leranunculus.github.io/workout/ · 리포 LeRanunculus/workout (public, main)

**지난 세션(10회차)에 6단계 러닝 탭을 구현해 `v11` 로 배포했다.**
SPEC 0~6단계가 전부 끝났고 코칭 프롬프트 계약 7/7 이 채워졌다.
남은 것은 **4-9 와 7단계** 둘이다.

## 0. 먼저 읽어라 — 추측하지 말고 파일을 실제로 열 것

  1. CLAUDE.md   — 절대 원칙 (외부 의존성 0 · 빌드 도구 없음 · 기록 손실 금지 ·
                   손가락으로 쓰는 앱 44px · 시각은 로컬 기준)
  2. HANDOFF.md  — 현재 상태 · 「10회차」 절이 오늘의 출발점 · 되살리지 말 것
  3. SCHEMA.md   — 데이터 계약
  4. SPEC.md     — 4-9 와 7단계가 남아 있다
  5. index.html  — 앱 본체 단일 파일 (약 2650줄). sw.js, manifest.json 도 있다

## 1. 가장 먼저 — 폰에서 `v12` 의 RIR 칩

**11회차에 실사용에서 잡힌 첫 기능 결함을 고쳐 배포했다. 확인이 안 됐다.**
한 종목의 세트를 전부 ✓ 했을 때 —

- **칩 3개(「여유있었음 / 딱맞았음 / 한계였음」)가 눈에 보이는가**
- 흐리지 않고, 휴식 바에 안 깔리고, 화면이 다음 종목으로 도망가지 않는가
- **칩을 고르면** 그때 다음 종목으로 넘어가는가

원인과 수정은 HANDOFF 「11회차」에 있다. ⚠ **마크업은 v8 부터 줄곧 옳았다** —
흐려지고·가려지고·스크롤로 밀려나서 안 보였을 뿐이라 **하네스가 못 잡는 종류**였다.

그 다음이 **러닝 탭 390px 레이아웃**이다 (아직 미실측 · 체크리스트 11~16번).
브라우저 창을 390×844 로 줄이면 렌더러가 죽어서 PC 로는 못 본다.
`[ 러닝 ]` → `+ 오늘 러닝 기록` 을 눌러 **가로 스크롤이 없는지**부터 볼 것.

**먼저 묻느라 멈추지는 말 것** — 말이 없으면 아래 작업을 진행하면 된다.

## 2. 다음 순위 — SPEC 4-9 (되돌리기 · 비정상 값 경고)

**러닝이 들어오면서 급해졌다.** 러닝은 주 5회 입력이라 오타 하나가 주간 누적을
영구히 틀리게 만든다 — `5000` 을 잘못 치면 50.00km 가 그대로 굳는다.
지금은 저장한 러닝을 고치거나 지울 길이 **아예 없다** (10회차에 의도적으로 미룬 것이다).

세션 되돌리기와 **같이** 설계할 것. 둘을 따로 만들면 조작 규칙이 갈라진다.
SPEC 4-9 를 기준으로 하되, 러닝까지 덮도록 범위를 넓혀 먼저 보고할 것.

## 3. 그 다음 — 7단계 (GitHub 동기화)

⚠ **착수 전에 공개 범위를 반드시 다시 정할 것.** 지금까지는 기록이 폰 안에만 있어
"공개돼도 새는 정보가 없다"가 성립했는데, 7단계가 돌면 날짜별 기록 전체가 public 으로
올라간다. **secret gist 가 유력하다** (SCHEMA 「기록을 어디에 올릴 것인가」).
첫 작업은 **토큰 확인**이다 — gist API 가 fine-grained PAT 로 되는지부터.

## 4. 이 프로젝트의 작업 규약

- 고치기 전에 무엇을 바꿀지 먼저 보고할 것. 승인받고 손댄다
- PC 에서 검증 가능한 것은 반드시 검증하고 넘어간다. "코드는 짰지만 확인 안 함"은 완료가 아니다
  · node --check 로 인라인 스크립트 문법 검사 (<script>~</script> 를 떼어내서)
  · 부모 폴더에서 python -m http.server 8765 를 띄우면 /workout/ 서브경로까지 재현된다
    (localhost 는 보안 컨텍스트라 서비스워커도 등록된다)
  · 브라우저에서 실제로 눌러보고 저장된 JSON 을 SCHEMA 와 대조할 것
  · 테스트 중 confirm() 을 띄우면 브라우저 자동화가 멈춘다. window.confirm 을 스텁으로
- 배포할 때마다 sw.js 의 CACHE 숫자를 하나 올릴 것. 안 올리면 폰에 옛 화면이 뜬다
- 한 번에 여러 배포를 쌓지 말 것. 6회차에 여섯 개를 쌓았다가 폰 검수가 통째로 밀렸다
- ⭐ **검수 하네스가 이미 있다. 새로 만들지 마라.**
      cd C:/Users/hr222/projects_hr/workout-tests && node run.js
  372건이 한 번에 돈다 (tests 104 · tests2 61 · regress 20 · migrate 41 · runtests 146).
  `node run.js runtests` 처럼 하나만 돌릴 수도 있다.
  index.html 의 인라인 스크립트를 떼어내 vm 위에서 돌리므로 **앱은 손대지 않는다.**
  ⚠ **리포 밖 옆 폴더에 있다 — 일부러 그렇다.** 원칙 2(프로젝트에 빌드 도구 없음)를
  지키면서 스크래치패드가 세션마다 날아가는 문제를 피한 것이다.
  **workout 리포 안에 테스트 파일을 만들지 말 것.**
  기능을 더하면 그 폴더에 검사를 **덧붙여라** — 지우지 말 것.
  원리는 HANDOFF 「7회차」 절에 적혀 있다

지금까지 데인 것들 — 같은 함정에 다시 빠지지 말 것:
- 폰 입력칸에 inputmode="numeric" 을 걸면 iOS 숫자 키패드에는 하이픈도 콜론도 없다.
  "8-12" 같은 값은 칸을 나눠서 받아야 한다. PC 브라우저에서는 그냥 타이핑돼서 100% 통과한다.
  (이 성질을 피하지 말고 이용하는 쪽으로 갔다 — 러닝 입력은 기호를 아예 안 받고
   숫자만 받아 앱이 넣어준다. numeric 을 쓰지 말라는 뜻이 아니다)
- 기존 flex 줄에 새 요소를 넣을 때는 그 줄의 align-items 를 먼저 확인할 것.
  기본값 stretch 라서 44px 버튼 하나가 옆의 작은 태그들을 44px 로 늘려놨다
- 종목 목록은 D().ex 가 아니라 todayEx() 다 (오늘 추가한 종목이 뒤에 붙는다).
  ⚠ 그 배열의 인덱스를 키로 쓰는 세션 상태는 **다섯**이다 —
  entries · swaps · sessionSets · sessionSkip · rirs.
  목록 중간에 무언가를 끼우거나 한 자리를 갈아치울 때 하나라도 빠뜨리면
  입력값이 옆 종목에 붙는다. 7회차(promoteAdds)와 10회차(applyPick)에 각각 한 번씩
  같은 실수가 나왔다 — **자리를 바꾸거나 지우는 코드를 쓸 때마다 다섯을 셀 것**
- 세션 텍스트에 줄을 더할 때는 "NNkg x NN" 패턴이 없어야 한다. 파서가 그 패턴만 본다
- 종목 이름은 사용자 입력이다. 화면에 뿌릴 때는 반드시 esc() 를 통과시킬 것
- 저장 경로와 복원 경로는 짝이다. 한쪽 규칙만 고치면 다른 쪽이 조용히 틀린다 —
  allTop 이 저장은 >=, 복원은 === 인 채로 넉 달 갈 뻔했다 (7회차에 발견)
- "실측했다"고 적힌 것도 의심할 것. 「전 카드가 태그 줄 한 줄에 들어간다」가
  HANDOFF 에 실측으로 적혀 있었는데 실제로는 매 Day 첫 카드가 두 줄이었다.
  폭을 이분 탐색해 임계값을 숫자로 뽑으면 바로 드러난다
- 코드의 DAYS 를 고쳐도 폰의 wk:program 은 안 바뀐다. 8회차에 밤 러닝 run 필드를
  패치에서 빠뜨렸는데, DAYS 를 직접 읽는 Node 테스트는 통과하고 브라우저에서만 잡혔다.
  폰의 저장된 상태를 localStorage 에 심고 로드해봐야 보이는 종류다
- 하네스로 드래프트를 심고 같은 탭에서 리로드하면 옛 페이지의 pagehide 플러시가
  그 드래프트를 먼저 지운다. 앱의 정상 동작이니 탭을 새로 열어 검증할 것
- 텍스트 계약에 새 줄을 더할 때는 반드시 파서에 먹여볼 것. 러닝 줄은 `kg x` 가 없어
  안전하다고 판단했는데 **사용자 메모가 그 패턴을 담을 수 있었다.**
  "내가 만드는 줄"만 보면 놓친다 — 사용자 입력이 섞이는 자리를 볼 것
- 소수 초를 다룰 때 나머지를 먼저 반올림하면 59.7초가 "5:60" 이 된다.
  총초를 먼저 정수로 만들 것
- 브라우저 자동화에서 물리 클릭 좌표가 조용히 빗나갈 수 있다. document.activeElement 로
  확인하고, 요소를 JS 로 focus() 한 뒤 실제 키 입력을 보내면 입력 경로는 진짜로 검증된다
- 브라우저 창 크기를 바꾸다 렌더러가 죽으면 그 뒤로 Chrome 이 localhost 에 아예
  접속하지 못할 수 있다(curl 은 200). 포트를 바꿔도 같으면 더 파지 말고 미실측으로 적을 것
- ⭐ **「마크업이 나온다」와 「눈에 보인다」는 다른 말이다.** RIR 칩은 v8 부터 렌더가
  옳았는데 흐려지고(부모 opacity)·가려지고(fixed 바)·스크롤로 밀려나 폰에서 안 보였다.
  하네스는 innerHTML 만 봐서 세 배포를 통과시켰다. **새 UI 를 넣을 때는 뜨는 순간
  화면에서 무슨 일이 같이 벌어지는지**(스크롤·고정 요소·상태 클래스) 를 같이 볼 것
- opacity 를 부모에 걸면 자식이 벗어날 수 없다. 일부만 진하게 두려면 **선택자를 바꿔야** 한다
- `body` 의 `padding-bottom` 은 **문서 끝** 여백이라 문서 중간이 fixed 요소에 덮이는 것을
  못 막는다. 그건 `scroll-margin-bottom` 의 일이다
- 자동화 탭은 `visibilityState:"hidden"` 이라 `behavior:"smooth"` 가 생략되고 CSS 전환이
  얼어붙는다. **스크롤은 `scrollIntoView` 를 가로채 「어디로 갈지 결정했는가」를 보고,
  위치는 `behavior:"auto"` 로 재현해 재면 된다** (11회차에 이 방법으로 판정했다)

- 작업이 끝나면 HANDOFF.md 를 갱신하고 NEXT-SESSION.md 도 다음 작업에 맞게 고칠 것

## 5. 지금 상태 (2026-08-17 · 11회차 종료 시점)

SPEC 0~6단계 완료. 캐시 workout-v12. localStorage 키는
wk:index / wk:YYYY-MM / wk:program / wk:draft 네 개다.
코드 레벨 검수 395건 전부 통과 (하네스는 `../workout-tests/` 에 있다 — 위 4절).

⭐ 11회차에 고친 것 (`v12`) — 되돌리지 말 것:
  - **RIR 칩이 화면에 안 보이던 것.** 마크업은 옳았다. `.ex.done` 이 카드를 통째로
    흐리고, 휴식 바(fixed)가 덮고, 260ms 뒤 다음 카드로 스크롤해서 밀려났다.
    흐림을 직계 자식에만 걸고 칩을 뺐다 · 스크롤 목적지를 칩으로 미뤘다 ·
    `.rir` 에 `scroll-margin-bottom` 을 줬다. **실사용에서 나온 첫 기능 결함이다**
  - **바퀴 = 누적 세션 ÷ Day 수 (`cyclesDone()`).** `rotation.cycles` 는 마지막 Day 를
    해야만 올라서 Day 를 골라 하면 영영 0 이었다 — 디로드 권고가 안 떴다.
    필드는 SCHEMA 계약이라 그대로 두고 **표시·판정만** 유도값을 쓴다
  - `COACH-BRIEF.md` rev 2 — 0절(델타 표) 신설 · 케이던스 서술 정정 · 백업 문법 추가

⭐ 10회차에 러닝 탭이 들어갔다 (SPEC 6단계 완료):
  - 축 전환 [ 웨이트 ][ 러닝 ]. 앱을 열면 언제나 웨이트다 — 선택을 저장하지 않는다
  - 거리·시간 모두 숫자만 친다. 533 → 5.33 · 2631 → 26:31. 둘 다 inputmode="numeric"
  - 저장은 wk:YYYY-MM 의 runs[]. idx.counts.runs 와 idx.recentRunWeeks 를 갱신한다
  - 주는 ISO 8601 (월~일). 코드는 (getDay()+6)%7 로 월=0 을 만든다
  - 코칭 프롬프트에 [최근 러닝 3회] · [이번 주 러닝] 이 들어가 계약 7/7 이 됐다
  - 백업 내보내기에도 러닝이 담긴다 (한 방향 — 파서는 되읽지 않는다)
  - 주 목표 25km 는 코드 상수 RUN_WEEK_TARGET 이다. 편집 화면은 없다
  - 저장한 러닝을 고치거나 지울 길은 없다 — 4-9 의 영역이다 (위 2절)

⭐ 8회차의 PROGRAM_REV 개정 장치 — 이걸 모르면 다음 프로그램 변경이 폰에 반영되지 않는다.
  DAYS 를 고쳐 배포해도 폰의 wk:program 은 안 따라온다 (진실이 그쪽이다).
  프로그램을 바꿀 때는 반드시 PROGRAM_REV 를 올리고 PROGRAM_PATCHES 에 블록을 더할 것.
  패치는 조건을 보고 움직이고(멱등), 정의는 fromDAYS() 로 DAYS 에서 가져오고,
  이름이 바뀌면 moveRecord() 로 기록 키를 옮기고, 구성이 바뀐 Day 의 드래프트는 비운다

⭐ 8회차에 바뀐 프로그램 (한 바퀴 31종목 87세트) — 되살리지 말 것:
  카프레이즈 전면 제거(종아리 볼륨 0은 의도다) · Day 1 #6 오버헤드 덤벨 트라이셉스 익스텐션 ·
  Day 2 힙 어브덕션 머신 · Day 4 는 6종목 16세트 · 밤 러닝 5km 고정(Day 2·4 만 이지).
  근거는 PROGRAM.md 「설계 근거」에 있다.

7·10회차에 고친 것 (되돌리지 말 것 — 전부 재현 후 수정했다):
  - 인덱스를 키로 쓰는 세션 상태는 다섯이다 — entries · swaps · sessionSets ·
    sessionSkip · rirs. promoteAdds 와 applyPick 이 둘 다 rirs 를 빠뜨려 있었다
  - parseBackup 의 allTop 이 >= 다 (저장 경로와 같은 규칙). === 로 되돌리지 말 것
  - parseBackup 이 `[` 로 시작하는 줄을 건너뛴다 — 러닝 메모의 "5kg x 10" 방어
  - parseBackup 이 rir 을 되읽는다 (`· 한계였음`)
  - migrateKey 는 변환 함수가 없으면 false 를 반환해 배너를 띄운다
  - pruneSwapMemory() 가 고아 대체 기억을 지운다
  - 태그 줄을 한 줄로 만들려고 건너뛰기를 아이콘화하고 target 을 `·` 앞만 표시한다

바뀌지 않는 규칙들:
  - Day 순환의 단일 진실은 wk:index 의 rotation 이다. history 에서 유도하지 말 것
  - 운동 프로그램의 진실은 wk:program 이다. 코드의 DAYS 상수는 되돌리기용 원본일 뿐이다
  - 퀵수정의 규칙은 하나다 — 카드에서 하는 것은 언제나 오늘만,
    ⚙ 시트에서 하는 것은 언제나 계획(wk:program). 「다음부터도 적용」 체크박스는 없다.
    자리가 곧 범위다
  - 시각은 어디에도 저장하지 않는다. 날짜 · durationSec · rev 정수뿐이다

## 6. 되살리지 말 것 — 전부 검토 후 내린 결정이다

알림음·진동 (iOS 무음 스위치에서 안 울리고 사용자가 필요 없다고 확인했다),
앱 안에서 AI API 호출, 애플 건강 연동, 체중·식단 기록, index.html 프레임워크 재작성,
백엔드·계정·푸시 알림, 전체 기록 편집기, 퀵수정의 「다음부터도 적용」 체크박스,
롱프레스 제스처, 휴식 바에 조작 얹기, 편집 모드 토글.
자세한 이유는 HANDOFF.md 「되살리지 말 것」과 「검토했으나 채택하지 않은 것」에 있다.

---

## 이 파일을 갱신하는 법

작업이 끝나면 **1~3절(오늘 할 일과 다음 순위) · 5절(지금 상태)** 을
다음 작업에 맞게 고친다. 나머지(읽는 순서 · 작업 규약 · 되살리지 말 것)는 그대로 둔다.
**「데인 것들」에는 이번에 새로 배운 것만 덧붙인다** — 지우지 말 것.
