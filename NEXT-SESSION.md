# 다음 세션이 할 일

**이 파일은 붙여넣기용이 아니다. 세션 시작 때 읽는 문서다.**
`CLAUDE.md` 가 자동으로 읽히고 거기서 이 파일을 가리킨다 — 사용자는
"NEXT-SESSION.md 읽고 이어서 하자" 한 마디만 하면 된다.

반드시 `C:/Users/hr222/projects_hr/workout` 안에서 `claude` 를 실행할 것
(상위 폴더에서 열면 `workout/CLAUDE.md` 가 자동으로 안 읽힌다).

---

이 저장소는 아이폰 전용 오프라인 PWA 운동 기록 앱이다. 이미 배포돼 실사용 중이다.
서비스 https://leranunculus.github.io/workout/ · 리포 LeRanunculus/workout (public, main)

지난 세션(9회차)에 6단계 러닝 탭 설계를 확정했다. 코드는 아직 한 줄도 안 짰다.
오늘 할 일은 그 구현이다 — v11 한 배포로 묶는다.

## 0. 먼저 읽어라 — 추측하지 말고 파일을 실제로 열 것

  1. CLAUDE.md   — 절대 원칙 (외부 의존성 0 · 빌드 도구 없음 · 기록 손실 금지 ·
                   손가락으로 쓰는 앱 44px · 시각은 로컬 기준)
  2. HANDOFF.md  — 현재 상태 · 「9회차」 절이 오늘의 출발점 · 되살리지 말 것
  3. SCHEMA.md   — 데이터 계약. 「러닝」과 「입력 형식」을 반드시
  4. SPEC.md     — 6단계에 확정안이 전부 적혀 있다
  5. index.html  — 앱 본체 단일 파일 (약 2090줄). sw.js, manifest.json 도 있다

## 1. 6단계 러닝 탭 — 설계는 이미 확정됐다

SPEC.md 6단계와 SCHEMA.md 「러닝」·「입력 형식」에 확정안이 전부 적혀 있다.
시안 실물 — https://claude.ai/code/artifact/7e919a59-339c-4c4c-a7cc-66a9c0cfd9b4
**시안과 문서가 어긋나면 문서가 기준이다.** 물어보지 말고 문서대로 구현해라.

핵심만 다시 적으면:

   - 축 전환 [ 웨이트 ] [ 러닝 ] 을 상단에. 웨이트 화면 탭 수는 1탭도 늘리지 마라
   - 거리·시간 모두 숫자만 친다. 소수점도 콜론도 안 친다
       533 → 5.33 (최대 99.99) · 2631 → 26:31 (최대 999:59)
       둘 다 inputmode="numeric". decimal 이 아니다 — 기호 키가 필요 없어진다
       기호를 눌러도 무시하고, 백스페이스는 한 자리씩 밀고, 다 지우면 빈칸이다
   - 시간은 분:초로만 받는다. 1시간이 넘으면 분으로 이어 친다 (6218 → 62:18).
     표시할 때만 1:02:18 로 푼다. 초가 60 이상이면 저장을 막고 사유를 보여준다
   - 거리 프리필은 5.00 고정이고 회색(ghost)이다. SPEC 원안의 「직전 값」이 아니다 —
     5km 고정이 된 뒤로 롱런 다음 날 10.9 가 떠서 매번 지워야 했다
   - 페이스는 입력받지도 저장하지도 않는다. 계산해서 표시만 한다
   - 주는 ISO 8601 — 월요일 시작, 일요일 끝. getDay()(일요일=0)를 그대로 쓰면 어긋난다
     주 범위를 날짜로 적어라 (08.17 월 ~ 08.23 일). 목록도 주 경계에서 가른다
   - 종류 칩 넷 다 (이지/평소/롱런/인터벌). 심박·케이던스·메모는 접어둔다
   - 입력 규칙 안내(힌트) 줄은 두지 마라. 혼자 쓰는 앱이다
   - 애플 건강 연동은 하지 않는다. 검토 후 내린 결정이니 되살리지 말 것

## 2. 저장 자리와 마무리

   - 러닝은 wk:YYYY-MM 의 runs[] 에 넣는다. 자리는 이미 있다 (blankMonth)
   - 웨이트는 Day 순환이지만 러닝은 날짜별 독립 기록이다. 같은 날 둘 다 있을 수 있다
   - idx.counts.runs 와 idx.recentRunWeeks 를 갱신한다
   - ★ 코칭 프롬프트에 「최근 러닝 3회」를 더해 계약 7항목을 완성해라. 이게 6단계의 목적이다
   - COACH-BRIEF.md 3절 「러닝 데이터는 아직 안 온다」를 같이 고쳐라
   - sw.js 를 v11 로. 한 배포로만 묶는다

## 3. 같이 처리할 낮은 순위 3건

HANDOFF 7회차 「고치지 않은 것」이다. 배포를 따로 쌓지 말고 이번에 묶어라.

   - 백업 복원 시 rir 이 손실된다 (텍스트엔 있는데 파서가 안 읽는다)
   - 마이그레이션 껍데기가 조용히 넘어간다 (MIGRATIONS[v+1] 이 없으면 schema 를 안 올림)
   - swapMemory 고아 키 (자리 교체·이름 변경·종목 제거 후에도 남는다)

그 다음 순위는 4-9(되돌리기·비정상 값 경고), 그 다음이 7단계(GitHub 동기화)다.
7단계는 착수 전에 반드시 공개 범위를 다시 정할 것 — secret gist 유력.

## 3-b. 폰 검수는 여전히 밀려 있다 (v3~v10, 여덟 배포치)

HANDOFF 「검수 체크리스트」의 「코드」 칸은 자동 검수로 차 있고 「폰」 칸이 비어 있다.
내가 헬스장 다녀와서 뭔가 말하면 그게 새 기능보다 1순위다. 먼저 묻지는 마라 —
말 안 하면 구현을 진행하면 된다. 결과를 받으면 「폰」 칸에 적어라
(통과 ✅ / 실패 ❌ 와 증상 한 줄 / 못 해본 것 ⬜). 코드 결과로 폰 칸을 채우지 마라.

## 4. 이 프로젝트의 작업 규약

- 고치기 전에 무엇을 바꿀지 먼저 보고할 것. 승인받고 손댄다
- PC 에서 검증 가능한 것은 반드시 검증하고 넘어간다. "코드는 짰지만 확인 안 함"은 완료가 아니다
  · node --check 로 인라인 스크립트 문법 검사 (<script>~</script> 를 떼어내서)
  · 부모 폴더에서 python -m http.server 8765 를 띄우면 /workout/ 서브경로까지 재현된다
    (localhost 는 보안 컨텍스트라 서비스워커도 등록된다)
  · 브라우저에서 실제로 눌러보고 저장된 JSON 을 SCHEMA 와 대조할 것
  · 테스트 중 confirm() 을 띄우면 브라우저 자동화가 멈춘다. window.confirm 을 스텁으로
- 배포할 때마다 sw.js 의 CACHE 숫자를 하나 올릴 것. 안 올리면 폰에 옛 화면이 뜬다
- 한 번에 여러 배포를 쌓지 말 것. 6회차에 여섯 개를 쌓았다가 폰 검수가 통째로 밀렸다
- 테스트 하네스를 만들어 쓸 거면 스크래치패드에서만 해라. 프로젝트에 남기지 말 것
  (원칙 2 — 빌드 도구 없음). 만드는 법은 HANDOFF 「7회차」 절에 적혀 있다

지금까지 데인 것들 — 같은 함정에 다시 빠지지 말 것:
- 폰 입력칸에 inputmode="numeric" 을 걸면 iOS 숫자 키패드에는 하이픈도 콜론도 없다.
  "8-12" 같은 값은 칸을 나눠서 받아야 한다. PC 브라우저에서는 그냥 타이핑돼서 100% 통과한다.
  (이 성질을 피하지 말고 이용하는 쪽으로 갔다 — 러닝 입력은 기호를 아예 안 받고
   숫자만 받아 앱이 넣어준다. numeric 을 쓰지 말라는 뜻이 아니다)
- 기존 flex 줄에 새 요소를 넣을 때는 그 줄의 align-items 를 먼저 확인할 것.
  기본값 stretch 라서 44px 버튼 하나가 옆의 작은 태그들을 44px 로 늘려놨다
- 종목 목록은 D().ex 가 아니라 todayEx() 다 (오늘 추가한 종목이 뒤에 붙는다).
  entries·swaps·sessionSets·sessionSkip 은 전부 그 배열의 인덱스를 키로 쓴다 —
  목록 중간에 무언가를 끼우면 입력값이 옆 종목으로 옮겨 붙는다
- 세션 텍스트에 줄을 더할 때는 "NNkg x NN" 패턴이 없어야 한다. 파서가 그 패턴만 본다
- 종목 이름은 사용자 입력이다. 화면에 뿌릴 때는 반드시 esc() 를 통과시킬 것
- 저장 경로와 복원 경로는 짝이다. 한쪽 규칙만 고치면 다른 쪽이 조용히 틀린다 —
  allTop 이 저장은 >=, 복원은 === 인 채로 넉 달 갈 뻔했다 (7회차에 발견)
- "실측했다"고 적힌 것도 의심할 것. 「전 카드가 태그 줄 한 줄에 들어간다」가
  HANDOFF 에 실측으로 적혀 있었는데 실제로는 매 Day 첫 카드가 두 줄이었다.
  폭을 이분 탐색해 임계값을 숫자로 뽑으면 바로 드러난다
- 코드의 DAYS 를 고쳐도 폰의 wk:program 은 안 바뀐다. 8회차에 밤 러닝 run 필드를
  패치에서 빠뜨렸는데, DAYS 를 직접 읽는 Node 테스트는 통과하고 브라우저에서만 잡혔다.
  폰의 저장된 상태를 localStorage 에 심고 로드해봐야 보이는 종류다
- 하네스로 드래프트를 심고 같은 탭에서 리로드하면 옛 페이지의 pagehide 플러시가
  그 드래프트를 먼저 지운다. 앱의 정상 동작이니 탭을 새로 열어 검증할 것

- 작업이 끝나면 HANDOFF.md 를 갱신하고 NEXT-SESSION.md 도 다음 작업에 맞게 고칠 것

## 5. 지금 상태 (2026-08-17 기준)

SPEC 0~5단계 완료. 캐시 workout-v10. localStorage 키는
wk:index / wk:YYYY-MM / wk:program / wk:draft 네 개다.
9회차에 6단계 설계만 확정했고 코드는 아직 없다 — 배포도 없었다.

⭐ 8회차에 프로그램이 바뀌었다 (한 바퀴 31종목 87세트):
  - 카프레이즈 전면 제거 (Day 2·4). 종아리 볼륨 0은 의도다
  - Day 1 #6 이 「오버헤드 덤벨 트라이셉스 익스텐션」 (머신이 헬스장에 없다)
  - Day 2 에 「힙 어브덕션 머신」 (중둔근). Day 4 는 6종목 16세트로 줄어든 게 의도다
  - 밤 러닝 5km 고정. Day 2·4 만 이지 페이스
  근거는 PROGRAM.md 「설계 근거」에 있다. 되살리지 말 것

⭐ PROGRAM_REV 개정 장치가 생겼다 — 이걸 모르면 다음 프로그램 변경이 폰에 반영되지 않는다.
  DAYS 를 고쳐 배포해도 폰의 wk:program 은 안 따라온다 (진실이 그쪽이다).
  프로그램을 바꿀 때는 반드시 PROGRAM_REV 를 올리고 PROGRAM_PATCHES 에 블록을 더할 것.
  패치는 조건을 보고 움직이고(멱등), 정의는 fromDAYS() 로 DAYS 에서 가져오고,
  이름이 바뀌면 moveRecord() 로 기록 키를 옮기고, 구성이 바뀐 Day 의 드래프트는 비운다

7회차에 고친 것 셋 (되돌리지 말 것 — 전부 재현 후 수정했다):
  - promoteAdds 가 rirs 까지 재매핑한다. 인덱스를 키로 쓰는 세션 상태는 다섯이다 —
    entries · swaps · sessionSets · sessionSkip · rirs. 하나라도 빠뜨리면 옆 종목에 붙는다
  - parseBackup 의 allTop 이 >= 다 (저장 경로와 같은 규칙). === 로 되돌리지 말 것
  - 태그 줄을 한 줄로 만들려고 건너뛰기를 아이콘화하고 target 을 `·` 앞만 표시한다.
    wk:program 의 target 원본은 그대로다 — 화면 태그에서만 줄인다

바뀌지 않는 규칙들:
  - Day 순환의 단일 진실은 wk:index 의 rotation 이다. history 에서 유도하지 말 것
  - 운동 프로그램의 진실은 wk:program 이다. 코드의 DAYS 상수는 되돌리기용 원본일 뿐이다
  - 퀵수정의 규칙은 하나다 — 카드에서 하는 것은 언제나 오늘만,
    ⚙ 시트에서 하는 것은 언제나 계획(wk:program). 「다음부터도 적용」 체크박스는 없다.
    자리가 곧 범위다
  - 시각은 어디에도 저장하지 않는다. 날짜 · durationSec · rev 정수뿐이다

## 6. 되살리지 말 것 — 전부 검토 후 내린 결정이다

알림음·진동 (iOS 무음 스위치에서 안 울리고 사용자가 필요 없다고 확인했다),
앱 안에서 AI API 호출, 애플 건강 연동, 체중·식단 기록, index.html 프레임워크 재작성,
백엔드·계정·푸시 알림, 전체 기록 편집기, 퀵수정의 「다음부터도 적용」 체크박스,
롱프레스 제스처, 휴식 바에 조작 얹기, 편집 모드 토글.
자세한 이유는 HANDOFF.md 「되살리지 말 것」과 「검토했으나 채택하지 않은 것」에 있다.

---

## 이 파일을 갱신하는 법

작업이 끝나면 **1~3절(오늘 할 일과 다음 순위) · 5절(지금 상태)** 을
다음 작업에 맞게 고친다. 나머지(읽는 순서 · 작업 규약 · 되살리지 말 것)는 그대로 둔다.
**「데인 것들」에는 이번에 새로 배운 것만 덧붙인다** — 지우지 말 것.
