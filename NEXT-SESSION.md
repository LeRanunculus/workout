# 다음 세션 첫 마디

아래 블록을 **통째로 복사해서** 새 Claude Code 세션에 붙여넣는다.
반드시 `C:/Users/hr222/projects_hr/workout` 안에서 `claude` 를 실행할 것
(상위 폴더에서 열면 `workout/CLAUDE.md` 가 자동으로 안 읽힌다).

---

```
이 저장소는 아이폰 전용 오프라인 PWA 운동 기록 앱이다. 이미 배포돼 실사용 중이다.
https://leranunculus.github.io/workout/

먼저 이 순서로 읽어줘. 추측하지 말고 파일을 실제로 읽을 것:
  1. CLAUDE.md   — 절대 원칙 (외부 의존성 0, 빌드 도구 없음, 기록 손실 금지, 로컬 시각)
  2. SCHEMA.md   — 데이터 계약. 구현 전에 반드시
  3. SPEC.md     — 0~7단계 구현 명세
  4. HANDOFF.md  — 현재 상태 · 내린 결정과 근거 · 되살리지 말 것
  5. index.html  — 앱 본체 단일 파일 (약 1530줄). sw.js, manifest.json 도 있다

## 지금 상태 (2026-08-12 기준)

SPEC 0~3단계 + 5단계 완료. 배포됨. 아이폰 실기기 9항목 통과. 캐시는 workout-v4.
v2 개선(코드 리뷰 10건, 휴식 타이머 Date.now() 기준, 코칭 프롬프트와 전체 백업 분리,
세트별 무게 보존, 저장 후 초기화 버튼)과 5단계(프로그램 편집 화면)가 올라가 있다.

localStorage 키는 wk:index / wk:YYYY-MM / wk:program / wk:draft 네 개다.
Day 순환의 단일 진실은 wk:index 의 rotation 이다. history 에서 유도하지 말 것.
운동 프로그램의 진실은 wk:program 이다. 코드의 DAYS 상수는 되돌리기용 원본일 뿐이다.

## 이번에 할 일 — 정해져 있지 않다. 나에게 물어봐줘

사용자가 첫 실사용에서 요구한 것은 전부 반영됐다. 남은 SPEC 항목 중 무엇이 먼저인지는
헬스장에서 몇 번 더 써봐야 안다 — 실제로 첫 세션 만에 나온 피드백 4건 중 어느 것도
SPEC 의 4단계 순서와 맞지 않았다. 그래서 순서를 미리 정해두지 않았다.

후보: 4-1(RIR 칩) · 4-8(대체 종목 기억) · 4-9(되돌리기 · 비정상 값 경고) ·
6단계(러닝 탭) · 7단계(GitHub 동기화).

굳이 하나 고르라면 4-9 의 비정상 값 경고다 — 잘못 저장된 값이 프리필과 증량 제안을
계속 오염시키는데 지금은 고칠 방법이 아예 없다. 5단계로도 안 된다 (프로그램은 고쳐지지만
기록은 못 고친다). 자세한 근거는 HANDOFF.md 「다음 세션이 먼저 할 것」에 있다.

7단계에 착수하게 되면 그 전에 반드시 공개 범위를 다시 정할 것 —
기록이 public 리포에 올라가면 "새는 정보 없음" 전제가 깨진다. secret gist 가 유력하다.

## 작업 방식 (이 프로젝트의 규약)

- 고치기 전에 무엇을 바꿀지 먼저 보고할 것. 승인받고 손댄다.
- PC 에서 검증 가능한 것은 반드시 검증하고 넘어간다.
  "코드는 짰지만 확인 안 함"은 완료가 아니다.
  · node --check 로 인라인 스크립트 문법 검사
  · 부모 폴더에서 python -m http.server 를 띄우면 /workout/ 서브경로까지 재현된다
    (localhost 는 보안 컨텍스트라 서비스워커도 등록된다)
  · 브라우저에서 실제로 눌러보고 저장된 JSON 을 SCHEMA 와 대조할 것
  · 테스트 중 confirm() 을 띄우면 브라우저 자동화가 멈춘다. window.confirm 을 스텁으로
- 배포할 때마다 sw.js 의 CACHE 숫자를 하나 올릴 것. 안 올리면 폰에 옛 화면이 뜬다.
- 종목 이름은 이제 사용자 입력이다. 화면에 뿌릴 때는 esc() 를 통과시킬 것.
- 폰 입력칸에 inputmode="numeric" 을 걸면 iOS 숫자 키패드에는 하이픈도 콜론도 없다.
  "8-12" 같은 값은 칸을 나눠서 받을 것.
- 작업이 끝나면 HANDOFF.md 를 갱신하고 NEXT-SESSION.md 도 다음 작업에 맞게 고칠 것.

## 먼저 나에게 물어볼 것

폰에서 아직 확인하지 못한 것이 둘 있다. 새 작업을 시작하기 전에 물어봐줘.
  1. 날짜 경계: 다음 날 앱을 열었을 때 지난 중량이 프리필됐는지
     (1차 완료 기준의 마지막 미증명 항목. 하루를 넘겨야만 확인된다)
  2. 5단계 편집 화면을 폰에서 써봤는지. 표시줄이 ● workout-v4 인지부터
문제가 있으면 새 작업보다 그것부터 고친다.

그리고 다음에 무엇을 할지도 물어봐줘 — 위 「이번에 할 일」 참조.

## 되살리지 말 것 (전부 검토 후 내린 결정이다)

알림음·진동 (iOS 무음 스위치에서 안 울린다. 사용자가 필요 없다고 확인했다),
앱 안에서 AI API 호출, 애플 건강 연동, 체중·식단 기록,
index.html 프레임워크 재작성, 백엔드·계정·푸시, 전체 기록 편집기.
자세한 이유는 HANDOFF.md 「되살리지 말 것」에 있다.
```

---

## 이 파일을 갱신하는 법

작업이 끝나면 위 블록의 **「지금 상태」와 「이번에 할 일」과 「먼저 물어볼 것」** 세 절을
다음 작업에 맞게 고친다. 나머지(읽는 순서 · 작업 방식 · 되살리지 말 것)는 그대로 둔다.
