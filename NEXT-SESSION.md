# 다음 세션 첫 마디

아래 블록을 **통째로 복사해서** 새 Claude Code 세션에 붙여넣는다.
반드시 `C:/Users/hr222/projects_hr/workout` 안에서 `claude` 를 실행할 것
(상위 폴더에서 열면 `workout/CLAUDE.md` 가 자동으로 안 읽힌다).

---

```
이 저장소는 아이폰 전용 오프라인 PWA 운동 기록 앱이다. 이미 배포돼 실사용 중이다.
https://leranunculus.github.io/workout/

먼저 이 순서로 읽어줘. 추측하지 말고 파일을 실제로 읽을 것:
  1. CLAUDE.md   — 절대 원칙 (외부 의존성 0, 빌드 도구 없음, 기록 손실 금지, 로컬 시각)
  2. SCHEMA.md   — 데이터 계약. 구현 전에 반드시
  3. SPEC.md     — 0~7단계 구현 명세
  4. HANDOFF.md  — 현재 상태 · 내린 결정과 근거 · 되살리지 말 것
  5. index.html  — 앱 본체 단일 파일 (약 2070줄). sw.js, manifest.json 도 있다

## 지금 상태 (2026-08-12 기준)

SPEC 0~5단계 완료. 배포됨. 캐시는 workout-v8.
올라가 있는 것 — 세션 기록·자동 저장·오프라인 PWA · 프로그램 편집(5단계) ·
세션 중 퀵수정(세트 증감·건너뛰기·⚙ 시트·자리 교체·오늘 종목 추가) ·
RIR 칩 · 노트 · 웜업 기록 · 소요 시간 · 대체 종목 기억 · 주차 표시 · 지난 세션 보기.

⚠ 폰에서 실제로 써본 것은 v2 까지다. v3~v8 여섯 배포는 PC 검증만 됐다.

퀵수정의 규칙은 하나다 — 카드에서 하는 것은 언제나 오늘만, ⚙ 시트에서 하는 것은
언제나 계획(wk:program). 「다음부터도 적용」 체크박스는 없다. 자리가 곧 범위다.

localStorage 키는 wk:index / wk:YYYY-MM / wk:program / wk:draft 네 개다.
Day 순환의 단일 진실은 wk:index 의 rotation 이다. history 에서 유도하지 말 것.
운동 프로그램의 진실은 wk:program 이다. 코드의 DAYS 상수는 되돌리기용 원본일 뿐이다.

## 이번에 할 일 — ① 폰 검수 결과부터 받는다

**새 기능을 얹기 전에 밀린 검수부터 닫는다.** 폰에서 실제로 써본 것은 v2 까지고
v3~v8 여섯 배포는 PC 검증만 됐다. 사용자가 Day 3 세션에서 검수하기로 했으니
결과를 먼저 물어보고, HANDOFF.md 「검수 체크리스트」 10줄의 결과 칸을 채울 것.

문제가 나오면 그것부터 고친다. 새 단계는 그 다음이다.
6회차에 나온 문제 셋(반복 범위 입력 불가 · 태그 늘어남 · 대체 기록 사라짐)은
전부 폰에서만 드러났고 PC 테스트로는 하나도 안 잡혔다.

## 그 다음 — 남은 SPEC 세 개

SPEC 0~5단계는 전부 끝났다. 남은 것은 셋이다.

  1순위 6단계 러닝 탭 — 코칭 프롬프트 계약 7항목 중 마지막 하나가 「최근 러닝 3회」다.
        6단계가 없으면 데이터 자체가 없고, 그러면 하체 정체의 원인을 영영 무게 탓으로만
        보게 된다. 사용자는 밤에 주 5회 이상 5~7km 를 뛴다 — 실제로 가장 큰 교란 변수다.
  2순위 4-9 되돌리기 · 비정상 값 경고 — 잘못 저장된 값이 프리필과 증량 제안을 계속
        오염시키는데 지금은 고칠 방법이 아예 없다. 5단계로도 안 된다 (프로그램은
        고쳐지지만 기록은 못 고친다).
  3순위 7단계 GitHub 동기화 — 착수 전에 반드시 공개 범위를 다시 정할 것.
        기록이 public 리포에 올라가면 "새는 정보 없음" 전제가 깨진다. secret gist 가
        유력하고 gh 토큰에 gist 스코프가 있는 것은 확인돼 있다.

다만 순서는 권고일 뿐이다 — 헬스장에서 써보고 나온 피드백이 늘 SPEC 순서를 이겼다.
사용자에게 무엇이 거슬리는지 먼저 물어볼 것.

## 작업 방식 (이 프로젝트의 규약)

- 고치기 전에 무엇을 바꿀지 먼저 보고할 것. 승인받고 손댄다.
- PC 에서 검증 가능한 것은 반드시 검증하고 넘어간다.
  "코드는 짰지만 확인 안 함"은 완료가 아니다.
  · node --check 로 인라인 스크립트 문법 검사
  · 부모 폴더에서 python -m http.server 를 띄우면 /workout/ 서브경로까지 재현된다
    (localhost 는 보안 컨텍스트라 서비스워커도 등록된다)
  · 브라우저에서 실제로 눌러보고 저장된 JSON 을 SCHEMA 와 대조할 것
  · 테스트 중 confirm() 을 띄우면 브라우저 자동화가 멈춘다. window.confirm 을 스텁으로
- 배포할 때마다 sw.js 의 CACHE 숫자를 하나 올릴 것. 안 올리면 폰에 옛 화면이 뜬다.
- 종목 이름은 이제 사용자 입력이다. 화면에 뿌릴 때는 esc() 를 통과시킬 것.
- 종목 목록은 D().ex 가 아니라 todayEx() 다 (오늘 추가한 종목이 뒤에 붙는다).
  entries·swaps·sessionSets·sessionSkip 은 전부 그 배열의 인덱스를 키로 쓴다 —
  목록 중간에 무언가를 끼우면 입력값이 옆 종목으로 옮겨 붙는다.
- 폰 입력칸에 inputmode="numeric" 을 걸면 iOS 숫자 키패드에는 하이픈도 콜론도 없다.
  "8-12" 같은 값은 칸을 나눠서 받을 것. PC 브라우저에서는 그냥 타이핑돼서 100% 통과한다.
- 기존 flex 줄에 새 요소를 넣을 때는 그 줄의 align-items 를 먼저 확인할 것.
  기본값 stretch 라서 44px 버튼 하나가 옆의 작은 태그들을 44px 로 늘려놨다.
- 세션 텍스트에 줄을 더할 때는 "NNkg x NN" 패턴이 없어야 한다. 파서가 그 패턴만 본다.
- 작업이 끝나면 HANDOFF.md 를 갱신하고 NEXT-SESSION.md 도 다음 작업에 맞게 고칠 것.

## 먼저 나에게 물어볼 것

  1. Day 3 세션에서 검수해봤는지. HANDOFF.md 「검수 체크리스트」 10줄을 그대로 물어볼 것.
     특히 6번 — 대체 종목이 다음에 미리 선택되는가. 6회차에 터진 버그를 고친 자리다.
  2. 날짜 경계: 다음 날 앱을 열었을 때 지난 중량이 프리필됐는지
     (1차 완료 기준의 마지막 미증명 항목. 하루를 넘겨야만 확인된다)
  3. 그래서 다음에 뭘 할지 — 위 「그 다음」 참조.

문제가 있으면 새 작업보다 그것부터 고친다.

## 되살리지 말 것 (전부 검토 후 내린 결정이다)

알림음·진동 (iOS 무음 스위치에서 안 울린다. 사용자가 필요 없다고 확인했다),
앱 안에서 AI API 호출, 애플 건강 연동, 체중·식단 기록,
index.html 프레임워크 재작성, 백엔드·계정·푸시, 전체 기록 편집기.
퀵수정의 「다음부터도 적용」 체크박스 (자리로 가른다), 롱프레스 제스처,
휴식 바에 조작 얹기, 편집 모드 토글.
자세한 이유는 HANDOFF.md 「되살리지 말 것」과 「검토했으나 채택하지 않은 것」에 있다.
```

---

## 이 파일을 갱신하는 법

작업이 끝나면 위 블록의 **「지금 상태」와 「이번에 할 일」과 「먼저 물어볼 것」** 세 절을
다음 작업에 맞게 고친다. 나머지(읽는 순서 · 작업 방식 · 되살리지 말 것)는 그대로 둔다.
