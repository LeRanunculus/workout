# 세션 인계

마지막 갱신 — **2026-08-12**

---

## 지금 어디까지 왔나

**문서 4개 갱신 완료. 코드는 한 줄도 건드리지 않았다.**
다음 세션은 **SPEC.md의 0단계부터** 시작하면 된다.

| | 상태 |
|---|---|
| CLAUDE.md · SCHEMA.md · SPEC.md · PROGRAM.md | 갱신 완료. 미결 항목 0 |
| `index.html` | **손대지 않음.** 813줄 원본 그대로 |
| `manifest.json` · `sw.js` | 없음 (2단계에서 생성) |
| `icons/` | 4개 존재. **검증 완료** — 아래 참조 |
| git 저장소 | 아직 아님. `git init` 필요 |
| 아이폰 실기기 | **아무것도 확인하지 못했다.** 배포 자체를 안 했다 |

---

## 이번에 바꾼 것

### CLAUDE.md
- "이미 완성된 앱" 문장에 단서 추가 — 완성이 버그 없음은 아니다
- **원칙 1에 Claude API 직접 호출 금지**를 명문화
- **원칙 5 「시각은 언제나 로컬 기준」** 신설
- "하지 말 것"에 체중·식단 추가 (범위 밖임을 명시)
- 프로그램 변경 금지가 **AI에게 하는 말**임을 분명히 함 — 사용자는 앱에서 편집한다

### SCHEMA.md
- **「시각 규칙」** 신설 — `localDate()` / `localStamp()` 구현 포함. ISO 주(월요일 시작)
- `index.json`에 `programStart` 추가 + 주차 계산식
- `lastByExercise`에 **`was`** (이름 변경 이력) 추가
- **「대체 종목 프리필 — 읽기 전용 규칙」** 신설
- **「프로그램 — wk:program」** 신설 (편집 가능한 프로그램)
- **「클립보드 텍스트」** 신설 — 백업 내보내기 / 코칭 프롬프트 두 계약

### SPEC.md
- 단계를 **0~7로 재구성**. 배포를 2단계로 당기고 3단계를 관문으로
- **0단계 신설** — 알려진 버그 4건을 줄 번호·재현 방법과 함께
- **6단계 신설** — 프로그램 편집
- 7단계에 **"복붙이 필요 없어지는 지점"** 명시 (리포 raw URL)
- 검증 체크리스트에 회귀 항목 다수 추가
- 함정 ②를 "이미 해결됨"으로 변경

### PROGRAM.md
- 디로드 항목에 앱 동작 명시 (주차 표시만, 세트 자동 감소 없음)
- 프로그램 편집 시 이 문서를 다시 맞추는 방법 추가

---

## 아이폰 실기기에서 확인한 것

**없다.** 배포를 하지 않았으므로 확인할 수 없었다.
SPEC.md 검증 체크리스트의 "아이폰" 절은 **전부 미확인**이다.

## PC에서 확인한 것

| 확인 | 결과 |
|---|---|
| 아이콘 4개 실제 치수 | 180 / 192 / 512 / 512 모두 정확 |
| apple-touch-icon 모서리 알파 | **불투명 `#2B2836`** — iOS 회색 아이콘 문제 없음 |
| maskable 아이콘 배경 | 불투명 — 안전영역 잘림 없음 |
| `DAYS` 배열 vs PROGRAM.md | 세트 수까지 일치 (17/16/17/19/21) |
| `probeStorage` 정의 | **파일 전체에 없음** — 호출 1건이 전부 |
| `storageOK` 대입 | **어디에도 없음** — 선언 후 영원히 `null` |
| `ask()`의 API 호출 | 키 없이 `api.anthropic.com` 호출. Pages에서 100% 실패 |

---

## 다음 세션이 먼저 할 것

### 0단계 — 버그 4건 (파일 하나, 20~40줄)

| # | 무엇 | 위치 |
|---|---|---|
| 0-1 | `today()`를 로컬 날짜로 교체. `toISOString()` 전체 검색 | `:360` |
| 0-2 | `probeStorage()` 신설 + `storageOK` 세팅 | `:808` 호출부 |
| 0-3 | 반복 비면 ✓ 막고 반복 칸으로 포커스 | `:657`, `:557` |
| 0-4 | 드래프트 자동 저장 (1단계와 함께) | `:359`, `:371` |

**0-1을 먼저 할 것.** 이걸 안 고치고 저장을 살리면 잘못된 날짜로 데이터가 쌓인다.

재현 방법 — PC 시계를 오전 8시로 맞추고 저장하면 화면 상단 날짜(로컬)와
저장된 날짜(UTC)가 하루 다르다.

### 그 다음

SPEC.md의 1 → 2 → 3단계. **3단계를 통과하기 전에 4단계로 넘어가지 말 것.**

---

## 이번 세션에서 내린 결정과 근거

기록해두지 않으면 다음 세션이 다시 검토하게 되는 것들이다.

| 결정 | 근거 |
|---|---|
| 앱 안에서 Claude API를 부르지 않는다 | 키가 public 리포에 노출된다. 서버는 금지. 요금도 따로 나간다 |
| 세션 코치 → **프롬프트 복사** / 대체종목 AI 버튼 → **삭제** | 오프라인 동작. 대체 종목은 이미 종목당 2개 하드코딩 |
| 이름 변경 시 **키 이동 + `was`** (고유 ID 아님) | ID는 스키마를 2로 올리는 대공사. 이름 변경은 드문 이벤트 |
| 폴백 프리필은 **읽기 전용** | 쓰기까지 폴백하면 원래 종목 기록이 오염된다 |
| 폴백 기록으로 **증량 판정 안 함** | 다른 종목에서 목표를 채운 건 이 종목의 근거가 아니다 |
| sw는 **network-first + 2초 타임아웃** | 신호가 없는 것보다 약한 것이 나쁘다. 지하에서 10초 흰 화면 |
| 주는 **ISO 8601 월요일 시작** | `2026-W33` 표기가 ISO다. 다른 선택은 표기와 모순 |
| 디로드는 **주차 표시만**, 세트 자동 감소 없음 | 화면과 PROGRAM.md가 달라지면 어느 쪽이 맞는지 모른다 |
| 내보내기에 **전체 이력 + 프로그램** 포함 | 7단계 전까지 유일한 복구 수단이다 |
| 체중·식단은 **범위 밖** | PROGRAM.md에 규칙은 있지만 이 앱은 세션 기록만 다룬다 |

---

## 참고

전체 흐름 시안 (사용자·데이터·실패·네트워크 네 시점 + 화면 시안)
https://claude.ai/code/artifact/91483fc3-05a1-4193-83c9-ccf20f2f7335

시안과 문서가 어긋나면 **문서가 기준**이다.

---

## 새 세션을 시작할 때

작업 디렉토리가 `projects_hr`이면 `workout/CLAUDE.md`가 자동으로 안 읽힐 수 있다.
첫 마디에 이렇게 말하면 된다.

> workout 디렉토리의 CLAUDE.md · SCHEMA.md · SPEC.md · HANDOFF.md 읽고
> SPEC 0단계부터 시작해줘
