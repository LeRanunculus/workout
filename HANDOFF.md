# 세션 인계

마지막 갱신 — **2026-08-12**
저장소 — https://github.com/LeRanunculus/workout (public, `main`)

---

## 지금 어디까지 왔나

**0단계와 1단계가 끝났다. 0-1~0-4 전부 해소됐다.**
다음 세션은 **SPEC.md의 2단계(PWA 껍데기 + 배포)부터** 시작한다.

| | 상태 |
|---|---|
| CLAUDE.md · SCHEMA.md · SPEC.md · PROGRAM.md · HANDOFF.md | 확정. **미결 항목 0** |
| `index.html` | **0·1단계 완료.** 저장 계층 · SCHEMA 구조 · 자동 저장 |
| `manifest.json` · `sw.js` | **없음 — 2단계의 전부다** |
| `icons/` | 4개 존재. **실측 검증 완료.** 다시 만들지 말 것 |
| git | 원격 push 완료. **`git init`·`gh repo create` 다시 하지 말 것** |
| GitHub Pages | **아직 꺼져 있다.** 2단계에서 켠다 |
| 아이폰 실기기 | **아무것도 확인하지 못했다.** 배포 자체를 안 했다 |

### 0·1단계 항목별

| # | 무엇 | 상태 |
|---|---|---|
| **0-1** | `today()` UTC → 로컬 | ✅ |
| — | `parseBackup`의 `date:"복원"` 제거 | ✅ |
| **0-3** | 반복 없는 세트가 조용히 사라짐 | ✅ 브라우저 실조작 |
| **0-2** | `probeStorage` 신설 · `storageOK` 대입 | ✅ **저장 실패 배너가 처음으로 동작한다** |
| **0-4** | 드래프트 자동 저장 · 복원 | ✅ 새로고침 왕복 검증 |
| **1-1** | `DB` 어댑터 (`window.storage` → localStorage) | ✅ |
| **1-2** | `wk:index` · `wk:YYYY-MM` · `rotation` | ✅ 월 경계 검증 |
| **1-3** | 1초 디바운스 자동 저장 · `beforeunload` 제거 | ✅ |
| **1-4** | `wk:program` (편집 UI 는 5단계) | ✅ |
| — | 마이그레이션 함수 껍데기 | ✅ SCHEMA 요구 |
| — | `ask()` · AI 버튼 2개 제거 (SPEC 4-3 당김) | ✅ |

---

## 이번 세션에 한 일 (3회차 — 1단계)

### 구조

`DAYS`의 종목이 위치 배열(`e[0]`~`e[12]`)이었다. **스크립트로 변환**하고
변환 전후를 정규형으로 deep-compare 해서 **9033바이트 완전 일치**를 확인했다.
Day 레벨은 원래부터 이름 필드였다. 세트 합계 `17/16/17/19/21`도 그대로다.

| 키 | 내용 |
|---|---|
| `wk:program` | 첫 실행 때 `DAYS`를 복사. **이후로는 이 키가 진실이다.** 코드 상수는 되돌리기용 |
| `wk:index` | `rotation` · `lastByExercise` · `months` · `counts` |
| `wk:2026-08` | 그 달 세션. `exercises`는 **배열**, `planned`에 원래 종목 |
| `wk:draft` | 진행 중 세션. 1초 디바운스 + `visibilitychange`/`pagehide` 즉시 flush |

**`rotation.byDay`를 추가했다** (SCHEMA에 없는 필드). 브리핑의 "마지막 수행 · n일 전"에
Day별 마지막 날짜가 필요한데, 월 파일을 열지 않으려면 index에 있어야 한다.
SCHEMA가 "index만 보고 오늘 할 일에 답할 수 있어야 한다"고 한 원칙 그대로다. 필드 추가는 자유.

### 검증 중에 발견해서 고친 것 ← 코드를 짜면서는 안 보였던 것들

**① 저장한 세션이 드래프트로 되살아났다** — 가장 큰 것.
`finish`가 저장 후 `clearDraft()`는 했지만 `entries`는 메모리에 남겼다.
그 다음 `pagehide`가 그 값으로 드래프트를 **다시 만들었다.**
실사용에서는 이렇게 된다 — **세션을 저장하고 앱을 닫았다가 열면 방금 끝낸 Day가
"진행 중"으로 뜨고 rotation을 가로챈다.** 저장에 성공하면 메모리 상태도 비우고
`curDay`를 다음으로 옮기도록 고쳤다. 실패했을 때는 절대 안 비운다 — 그게 유일하게 남은 기록이다.

**② 대체 종목만 골라도 "진행 중인 세션"이 됐다.**
`hasDraftContent`가 `swaps`를 세고 있었다. Day를 둘러보다 대체를 한 번 눌러본 것만으로
드래프트가 남아 다음 실행 때 Day가 고정된다. 근거는 **실제로 기록한 것**(세트 입력·완료,
웜업/마무리 체크)뿐으로 좁혔다. `swaps`는 드래프트에 같이 실려 복원되지만 단독 근거는 아니다.

**③ 복원 개수가 부풀고, 아는 날짜가 `null`로 덮였다.**
내보내기에 「기록」과 「누적」 두 섹션이 생기면서 같은 종목이 두 번 세어졌다(4개인데 "7개 복원").
줄이 아니라 종목을 센다. 그리고 뒤에 오는 「누적」 섹션(헤더 없음 → `date=null`)이
앞에서 읽은 실제 날짜를 덮어썼다. **아는 날짜를 모르는 값으로 낮추지 않도록** 고쳤다.

### 검증 (PC 한정 — 아이폰은 전부 미검증)

크롬 `localhost`에서 실제로 조작해 확인했다.

- 첫 실행에 `wk:program` 생성 · 환경 표시줄 **「● 기록 저장」** (0-2)
- 세트 입력 → 새로고침 → **값·완료상태·대체종목 전부 유지** (0-4)
- 저장된 JSON이 SCHEMA와 일치 · **시각 흔적 0건** (정규식 5종 전수)
- 대체 종목의 `planned`에 원래 종목 보존
- **폴백 프리필** — 출처 `(다른 종목)` 표시, **증량 배지 안 뜸**, 프리필도 증량 미적용
- **월 경계** — 8/31 Day 3 완료 상태에서 9월 파일 없이 재실행 → **Day 4 유지**
- 마지막 Day 완주 → `cycles` +1, 다음 Day 1
- 내보내기 왕복 — 지우고 붙여넣어 `lastByExercise` **전 종목 값 일치**
- **v1 옛 텍스트**(헤더·`·` 없음)도 그대로 읽힘
- 옛 `workout-log` 이사 — 월 파일 분배 · rotation 유도 · `"복원"` 문자열 소거 · 옛 키 삭제
- 저장 실패 배너 — `localStorage`가 던지게 만들어 `○ 기록 저장` + 원인 + 버튼 문구 전환 확인
- **콘솔 에러 0건**

---

## 2회차에 한 일 (0단계 수정)

`index.html` 한 파일. 30줄 추가 / 7줄 삭제.

### 0-1. 날짜를 로컬 기준으로

`toISOString()`은 **파일 전체에 1곳뿐이었다** (`today()`). `Date.now()`는 0곳,
시각을 저장하는 필드도 0곳. SCHEMA의 `localDate()`로 교체하고 `today()` 이름은 유지했다
(호출부 2곳의 구조를 건드리지 않으려고). `localDate(d)`는 인자를 받는 형태로 넣어뒀다 —
1단계에서 월 키(`wk:2026-08`)를 만들 때 바로 쓴다.

실측 — KST 기준으로 옛 코드가 틀리는 창은 **문서가 말한 "오전 9시 이전"과 일치**한다.

| KST | `localDate()` | 옛 `toISOString()` |
|---|---|---|
| 00:30 · 08:00 | 2026-08-11 | **2026-08-10** ✗ |
| 12:00 · 23:30 | 2026-08-11 | 2026-08-11 |

**건드리지 않기로 판단한 두 곳** (다음 세션이 다시 검토하지 않도록 남긴다)
- 화면 상단 날짜 — 원래 로컬이라 맞는 쪽이었다. 어긋난 게 아니라 **재현 수단**이었다
- 경과일 계산 `new Date(today())-new Date(prev.date)` — `"YYYY-MM-DD"`는 양쪽 다
  UTC 자정으로 파싱돼 차이가 정확히 일수다. 손대면 오히려 위험하다

### `parseBackup`의 `date:"복원"` 제거

SCHEMA 「파싱 규칙」이 금지하는 것. 방치하면 1단계에서 `lastByExercise`에 실려
7단계에 GitHub까지 올라간다. 헤더 정규식이 버리던 날짜를 캡처하도록 바꿨다.

- 헤더 `[2026-08-11] Day 1` → 그 날짜를 쓴다
- 구분선(`---`)을 지나면 날짜 문맥을 끊는다 → **헤더 없는 「누적 기록」 섹션은 `null`**
- 대괄호 안이 날짜 형식이 아니면 `null`
- **v1 옛 텍스트(헤더 없음)도 그대로 읽힌다** — 하위 호환 확인함

`index.html`에서 함수 본문을 그대로 떼어내 세 가지 입력으로 돌려 확인했다(사본 아님).

### 0-3. 반복 없는 세트가 조용히 사라지던 것

`.chk` 핸들러에서 **켜지는 순간** 막고 반복 칸으로 포커스를 옮긴다.
알람색 테두리(`.need`)가 1.2초 뜬다. 저장 시점 경고는 45분 뒤라 늦다는 SPEC 판단 그대로다.

- **`0`도 막는다.** SPEC은 "비어 있으면"이라 썼지만 저장 필터가 `x.r`(falsy)이라
  `0`도 똑같이 조용히 버려진다. 같은 버그이고 조건 한 글자 차이다
- **끄는 동작은 막지 않는다.** 안 그러면 잘못 켠 세트를 되돌릴 수 없다
- 저장 시점 필터(`filter(x=>x.done&&x.r)`)는 **방어선으로 남겨뒀다**
- 고스트 프리필은 실제 `value`가 있어 통과한다 — 마찰이 늘지 않는다

크롬 `localhost`에서 직접 눌러 확인 — 반복 비움/`0`은 막히고, `12`는 완료되고,
해제는 동작하고, 종목 완주 후 **"완료됐는데 반복 없는 세트 0건"** · 진행률과 눌린 체크 수 일치.

### 콘솔 상태

에러 **1건뿐 — `probeStorage is not defined`**. 0-2이고 1단계에서 사라진다.
이번 수정으로 생긴 새 에러는 없다.

---

## 1회차에 한 일 — 문서 확정

### 발견 — 코드를 읽고 확인한 것

문서에는 "완성된 앱"이라 적혀 있었으나 실제로는 버그 4건과 죽은 기능 2개가 있었다.
전부 파일에서 직접 확인했다.

| 확인 | 결과 |
|---|---|
| `today()` | `toISOString()` — UTC라서 오전 9시 이전 세션이 전날로 기록됨 |
| `probeStorage` 정의 | **파일 전체에 없음.** 호출 1건이 전부 → ReferenceError |
| `storageOK` 대입 | **어디에도 없음.** 선언 후 영원히 `null` → 저장 실패 배너가 뜰 수 없음 |
| 반복 없는 세트 | 저장 시 조용히 버려짐. 진행률은 완료로 셈 |
| `DRAFT` 상수 | 선언만 있고 한 번도 안 쓰임 → 세션 중 앱이 죽으면 전부 소실 |
| `ask()` | 키 없이 `api.anthropic.com` 호출. Pages에서 100% 실패 |
| `setInterval` 타이머 | 백그라운드에서 멈춤. 고치면 알람 조건도 함께 깨짐 |
| 아이콘 4개 | 치수 정확, apple-touch-icon 모서리 **불투명 `#2B2836`** → 함정 ② 해결됨 |
| `DAYS` vs PROGRAM.md | 세트 수까지 일치 (17/16/17/19/21) |

### 문서 변경

**CLAUDE.md**
- 원칙 1에 **Claude API 직접 호출 금지** 명문화
- **원칙 5 「시각은 언제나 로컬 기준」** 신설
- Pages가 선택이 아니라 전제임을 명시 (HTTPS 없으면 서비스워커 없음)
- **7단계 전 공개 범위 재판단** 항목 신설
- "하지 말 것"에 체중·식단 추가. 프로그램 변경 금지가 AI 대상임을 분명히 함

**SCHEMA.md**
- **「시각 규칙」** 신설 — `localDate()`/`newId()` 구현 포함. ISO 주(월요일 시작)
- **「시각을 저장하지 않는다」** — `durationSec` · id 4자리 무작위 · `rev` 정수
- **「원격에서 직접 고치기」** — 기록 편집기를 대신한다. `rev`를 올려야 한다
- **「기록을 어디에 올릴 것인가」** — secret gist 후보와 확인 사항
- **「디로드는 사이클 기준이다」** — `rotation.cycles`. 달력 주차가 아니다
- `lastByExercise`에 **`was`** (이름 변경 이력)
- **「대체 종목 프리필 — 읽기 전용 규칙」**
- **「프로그램 — wk:program」** — 편집 가능한 프로그램
- **「클립보드 텍스트」** — 백업 내보내기 / 코칭 프롬프트 두 계약
- **「마이그레이션은 이렇게 돈다」** — 백업 키, 단계별 변환, 실패 시 복구

**SPEC.md**
- 단계를 **0~7로 재구성.** 배포를 2단계로 당기고 3단계를 관문으로
- **0단계 신설** — 버그 4건을 줄 번호·재현 방법과 함께
- **5단계 프로그램 편집 / 6단계 러닝 탭** (교체됨)
- **4-8 대체 종목 기억** 신설
- **4-9 기록 되돌리기 + 비정상 값 경고** 신설
- 3단계 뒤 **"여기서 멈추고 한동안 써볼 것"**
- 7단계에 **복붙이 필요 없어지는 지점** 명시 (리포 raw URL)
- 검증 체크리스트에 회귀 항목 다수 추가. 함정 ②는 "이미 해결됨"

**PROGRAM.md**
- 디로드를 **6바퀴·12바퀴**로 정정 (달력 주차 아님)
- 프로그램 편집 후 이 문서를 다시 맞추는 방법

---

## 다음 세션이 먼저 할 것

### 2단계 — PWA 껍데기와 배포 ← 여기서 배포한다

**완성한 뒤에 배포하지 않는다.** 3단계의 아이폰 검증을 시작하려면 HTTPS 주소가 먼저 필요하다.
SPEC.md 2단계에 `manifest.json` 내용과 `<head>` 태그가 그대로 적혀 있다.

| # | 무엇 | 함정 |
|---|---|---|
| 2-1 | `manifest.json` | `start_url`·`scope`를 **상대경로 `./`**로 (함정 ①) |
| 2-2 | `<head>` 태그 · `<title>`을 `운동 세션`으로 | `apple-touch-icon` 180 **반드시** (함정 ②는 아이콘만 해결된 상태) |
| 2-3 | `sw.js` — network-first + **2초 타임아웃** → 캐시 | `CACHE="workout-v1"`, 배포마다 올린다 (함정 ③) |
| 2-3 | 캐시 버전을 환경 표시줄에 표시 | 함정 ③을 폰에서 눈으로 확인하려고 |
| 2-4 | push → **Pages 켜기** → 서브경로 200 확인 | `register("sw.js")` 상대경로 (함정 ①) |

**저장소는 이미 있다.** `git init`·`gh repo create` 금지. Pages만 켜면 된다.
켠 직후 `https://leranunculus.github.io/workout/manifest.json`이 200인지부터 볼 것.

### 그 다음 — 3단계가 관문

**3단계를 통과하기 전에 4단계로 넘어가지 말 것.** 통과하면 거기서 멈추고 **몇 주 써본다.**

1단계 수정의 회귀 테스트가 3단계에 걸려 있다. 특히 이 셋은 **폰에서만 증명된다.**
- 오전 8시에 저장해도 오늘 날짜인가 (0-1)
- 세트 입력 → 앱 완전 종료 → 재실행 → 남아 있는가 (0-4)
- 앱을 나갔다 30초 뒤 돌아와도 타이머가 맞는가 (4-4, 아직 안 고침)

### 4단계로 넘어갈 때 알아둘 것

1단계에서 자리만 잡아둔 것들이다. 새로 설계하지 말고 채우기만 하면 된다.

- 세션의 `rir`은 `null`로 저장되고 있다 → 4-1이 칩을 붙인다
- `durationSec`·`note`·`warmup`·`finisher`는 **아직 저장하지 않는다** → 4-2
- `ask()`와 AI 버튼 2개는 **이미 지웠다.** 4-3은 「코칭 프롬프트 복사」를 만들기만 하면 된다.
  `CONTEXT` 상수는 그때 쓰려고 남겨뒀다
- 내보내기 텍스트에 **프로그램이 이미 포함**돼 있다 (SCHEMA 「백업 내보내기」)

### 작업 방식 — 계속 지킬 것

**고치기 전에 무엇을 바꿀지 먼저 보고한다.** 0단계에서 이 방식으로
"안 건드리는 게 맞는 곳" 두 곳을 걸러냈다(화면 날짜·경과일 계산).
1단계는 함수 7개가 연쇄로 걸려 있어 더 필요하다.

**PC에서 검증 가능한 것은 반드시 검증하고 넘어간다.** 0단계는 전부 실제로 돌려서 확인했다 —
`node --check`, 소스에서 함수를 떼어낸 파서 테스트, 크롬 실조작.
"짰지만 확인 안 함"은 완료가 아니다 (CLAUDE.md).

---

## 내린 결정과 근거 (누적)

기록해두지 않으면 다음 세션이 처음부터 다시 검토하게 된다.

### 3회차 (1단계)

| 결정 | 근거 |
|---|---|
| **`DAYS` 변환을 손이 아니라 스크립트로** | 종목 32개 × 필드 13개다. 손으로 옮기면 오타가 조용히 섞인다. 변환 전후 deep-compare 로 동일성을 증명했다 |
| `rotation.byDay` **필드 추가** | 브리핑의 "마지막 수행"에 Day별 마지막 날짜가 필요하다. 월 파일을 열지 않으려면 index 에 있어야 한다. SCHEMA 「index.json」의 취지 그대로이고 필드 추가는 마이그레이션이 아니다 |
| 저장 성공 시 **메모리 세션 상태도 비운다** | 안 비우면 `pagehide` 가 방금 저장한 값으로 드래프트를 다시 만들어, 다시 열었을 때 끝낸 Day 가 되살아난다. **실패 시엔 절대 안 비운다** — 유일하게 남은 기록이다 |
| 드래프트 판정에서 **`swaps` 제외** | 대체를 한 번 눌러본 것만으로 Day 가 고정되면 안 된다. 근거는 실제로 기록한 것뿐이다 |
| `parseBackup` 이 **아는 날짜를 `null` 로 낮추지 않는다** | 「누적」 섹션이 「기록」 섹션 뒤에 와서 헤더 날짜를 덮어썼다. 둘 다 SCHEMA 대로지만 결과가 손해다 |
| 복원 개수를 **줄이 아니라 종목으로** | 두 섹션에 같은 종목이 나와 4개가 "7개"로 보고됐다 |
| 내보내기에 **프로그램 포함** (5단계 아님) | SCHEMA 「백업 내보내기」가 요구한다. `wk:program` 자리를 1단계에서 잡았으니 지금 넣는 게 맞다. 커스텀 프로그램의 유일한 복구 수단이다 |
| `setRetry` 의 **재시도 간격을 800→300ms** | localStorage 는 네트워크가 아니다. 실패하면 대개 즉시 실패다 |

### 2회차 (0단계)

| 결정 | 근거 |
|---|---|
| `today()` **이름을 유지**하고 내부만 `localDate()`로 | 호출부 2곳의 구조를 건드리지 않는다. 0단계가 필요 이상으로 번지지 않게 |
| 화면 상단 날짜와 경과일 계산은 **안 건드린다** | 둘 다 원래 맞는 코드였다. 화면 날짜는 버그가 아니라 재현 수단이었고, `"YYYY-MM-DD"` 차이 계산은 양쪽 다 UTC 자정이라 정확하다 |
| 0-3에서 **반복 `0`도 막는다** | 저장 필터가 `x.r`(falsy)이라 `0`도 똑같이 조용히 버려진다. SPEC은 "비어 있으면"이라 썼지만 같은 버그다 |
| 0-3에서 **끄는 동작은 막지 않는다** | 막으면 잘못 켠 세트를 되돌릴 수 없다 |
| 저장 시점 필터를 **남겨둔다** | 입력 단계에서 막아도 방어선은 두 겹이 낫다 |
| `parseBackup`이 헤더 날짜를 **캡처**하도록 | `date:"복원"`을 없애려면 넣을 실제 값이 있어야 한다. 없으면 `null` |
| 0-2·0-4는 **1단계로** | SPEC 지정. 저장 어댑터와 같은 코드라 지금 만들면 곧 다시 뜯는다 |

### 1회차 (문서 확정)

| 결정 | 근거 |
|---|---|
| **시각을 저장하지 않는다** — `durationSec` · id에 4자리 무작위 · `rev` 정수 | 날짜만 남는 것과 날짜+시각이 남는 것은 위험도가 다르다. 세 곳을 다 막아야 의미가 있다. 데이터 0건인 지금 바꾸면 공짜다 |
| 병합 기준을 `updatedAt` → **`rev` 정수** | 노출 때문만이 아니다. 원격 JSON을 손으로 고쳤을 때 앱이 덮어쓰지 않으려면 **사람이 올릴 수 있는 값**이어야 한다 |
| 기록 편집기 대신 **원격 JSON 직접 수정** | 7단계 후 웹 에디터가 곧 편집기다. 앱에 화면을 만들 이유가 없어진다 |
| 비정상 값 검사에 **반복 범위**도 추가 | 3배 룰은 `5→50`을 잡지만 `12→2`를 못 잡는다. 목표 범위는 프로그램에 이미 있다 |
| 반응형 디로드는 **RIR 기준** (중량 하락 아님) | 중량은 대체 종목·이중 점진에 오염된다. 같은 Day는 2주에 한 번 와서 감지도 늦다. RIR은 종목이 바뀌어도 유효하고 2세션이면 사흘이다 |
| 기록 위치는 **secret gist가 유력**, 7단계에서 확정 | Claude가 raw URL로 읽으면서 검색·프로필에는 안 뜨는 유일한 지점. 단 토큰 종류 확인 필요 |
| **디로드는 바퀴 수 기준** (달력 주차 아님) | PROGRAM.md의 "주 5회 = 정확히 1주 1바퀴"에 비춰 "6주차"는 원래 6바퀴를 뜻했다. 누적 부하는 세션 수를 따라간다 |
| **5단계 = 프로그램 편집, 6단계 = 러닝** | 없는 기구를 매 세션 다시 고르는 부담이 먼저다. 러닝은 소급 입력이 된다 |
| **대체 종목 기억을 4단계에** | 없는 기구 문제의 대부분을 몇 줄로 해결한다. 5단계를 기다릴 이유가 없다 |
| **되돌리기 + 비정상 값 경고**만. 전체 기록 편집기는 안 만듦 | 오타는 그 자리에서 알아챈다. 두 겹으로 대부분 막힌다 |
| 이름 변경 시 **키 이동 + `was`** (고유 ID 아님) | ID는 스키마를 2로 올리는 대공사. 이름 변경은 드문 이벤트 |
| 폴백 프리필은 **읽기 전용**, 증량 판정 안 함 | 쓰기까지 폴백하면 원래 종목 기록이 오염된다. 다른 종목에서 목표를 채운 건 이 종목의 근거가 아니다 |
| 앱 안에서 **Claude API를 부르지 않는다** | 키가 public 리포에 노출된다. 서버는 금지. 요금도 따로 나간다 |
| 세션 코치 → **프롬프트 복사** / 대체종목 AI 버튼 → **삭제** | 오프라인 동작. 대체 종목은 이미 종목당 2개 하드코딩 |
| sw는 **network-first + 2초 타임아웃** | 신호가 없는 것보다 약한 것이 나쁘다. 지하에서 10초 흰 화면 |
| 주는 **ISO 8601 월요일 시작** | `2026-W33` 표기가 ISO다. 다른 선택은 표기와 모순 |
| 배지는 권고만. **세트 자동 감소 없음** | 화면과 PROGRAM.md가 달라지면 어느 쪽이 맞는지 모른다 |
| 내보내기에 **전체 이력 + 프로그램** 포함 | 7단계 전까지 유일한 복구 수단이다 |
| **7단계 전에 공개 범위 재판단** | 기록이 올라가면 "새는 정보 없음" 전제가 깨진다 |
| 체중·식단·애플 건강은 **범위 밖** | PROGRAM.md에 규칙은 있지만 이 앱은 세션 기록만 다룬다 |

---

## 아직 정해지지 않은 것

**없다.** 착수를 막는 미결 항목은 0이다.
다만 문서로 확인할 수 없는 것이 남아 있고, 이건 성격이 다르다 — **iOS 실기기 동작**이다.
클립보드 제스처 제약, 백그라운드 타이머, 홈 화면 앱 저장소, 약한 신호에서의 sw 타임아웃.
넷 다 PC에서 아무리 테스트해도 증명되지 않는다. 그래서 3단계가 관문이다.

---

## 참고

전체 흐름 시안 — 사용자·데이터·실패·네트워크 네 시점 + 화면 시안
https://claude.ai/code/artifact/91483fc3-05a1-4193-83c9-ccf20f2f7335

**시안과 문서가 어긋나면 문서가 기준이다.**

---

## 새 세션을 시작하는 법

### 여는 위치

```bash
cd C:/Users/hr222/projects_hr/workout
claude
```

**반드시 `workout` 안에서 열 것.** 상위 폴더에서 열면 `workout/CLAUDE.md`가
자동으로 안 읽힌다.

### 첫 마디

```
SCHEMA.md · SPEC.md · HANDOFF.md 읽고 SPEC 2단계부터 시작해줘.
0·1단계는 끝났다. 배포까지 하고 나서 3단계 체크리스트를 뽑아줘.
```

### 제대로 읽었는지 확인하는 질문

문서를 읽고 나면 이 셋을 알고 있어야 한다. 어긋나면 덜 읽은 것이다.

- **0단계가 왜 1단계보다 먼저인가** → 잘못된 날짜로 데이터가 쌓이기 전에 막으려고
- **3단계가 왜 관문인가** → 실기기 검증 전에는 오프라인·저장이 전부 미검증
- **`probeStorage`가 왜 문제인가** → 함수가 없어 참조 오류 → `envCheck` 중단 →
  저장 실패 배너가 뜰 수 없음

특히 세 번째를 모르고 "저장 배너는 이미 구현돼 있다"고 하면
옛 명세의 잘못된 전제를 믿고 있는 것이다. 이 파일을 다시 읽힐 것.
