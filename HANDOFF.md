# 세션 인계

마지막 갱신 — **2026-08-17** (11회차)
저장소 — https://github.com/LeRanunculus/workout (public, `main`)
서비스 — **https://leranunculus.github.io/workout/**

---

## 지금 어디까지 왔나

**SPEC 0~6단계 완료. 코칭 프롬프트 계약 7/7 이 채워졌다.**
사용자가 실사용에서 요구한 것은 **전부 반영됐다.** 남은 것은 **4-9 와 7단계** 둘뿐이다.

| | 상태 |
|---|---|
| CLAUDE.md · SCHEMA.md · SPEC.md · PROGRAM.md | 확정. **미결 항목 0** |
| `COACH-BRIEF.md` | 기록을 읽는 **Claude 채팅방용** 브리프. 개발 문서가 아니다. **세션 텍스트 형식이나 프로그램 규칙을 바꾸면 반드시 같이 갱신할 것** |
| `index.html` | 0~6단계 + 퀵수정. 단일 파일 약 2765줄 |
| `manifest.json` · `sw.js` | 있음. **캐시 `workout-v13`** |
| `icons/` | 4개. 실측 검증 완료. **다시 만들지 말 것** |
| GitHub Pages | **켜져 있다** (`main` / root, HTTPS 강제). **끄지 말 것** — 끄면 HTTPS 가 없어져 서비스워커가 죽고 PWA 자체가 성립하지 않는다 |
| 아이폰 실기기 | **9항목 통과.** 아래 표 |

### 아이폰에서 실제로 확인한 것 ← 추정 아님

| 항목 | 결과 |
|---|---|
| 홈 화면 설치 · 아이콘 · 전체화면 | ✅ |
| **Wake Lock** (타이머 도는 동안 화면 유지) | ✅ (iOS 18.4+ 인 셈) |
| **비행기 모드에서 열림** | ✅ |
| 세트 입력 → **앱 완전 종료** → 재실행 → 값 생존 | ✅ (0-4) |
| **오전에 저장 → 날짜가 오늘** | ✅ (0-1) |
| 저장 후 닫았다 열면 다음 Day | ✅ |
| 숫자 칸 → 숫자 키패드 | ✅ |
| 내보내기 → 복사 → 메모 앱 | ✅ |
| **타이머 — 앱 밖 30초 뒤 복귀 시 남은 시간 정확** | ✅ (4-4 `Date.now()` 방식 실기기 확인) |
| 배포 반영 (표시줄 `● workout-v2` → **`● workout-v8`**) | ✅ |
| **`v10` 프로그램 개정(`PROGRAM_REV`)이 폰에 적용됨** | ✅ (11회차 · 08-17 세션 기록에 Day 1 #6 이 새 이름으로 찍혔다) |
| **`v11` 이 폰에 반영됨** | ✅ (11회차 · 코칭 프롬프트에 러닝 두 블록이 들어 있었다) |

### ⚠ 폰 검수가 열한 배포치 밀려 있다

**폰에서 실제로 써본 것은 v2 까지다.** `v3`~`v13` 은 전부 **PC 검증만** 됐다.
11회차에 `v11` 을 실사용한 프롬프트가 들어와 몇 칸이 채워졌고, **거기서 첫 기능 결함이 나왔다**
(RIR 칩 · 아래 「11회차」). 밀린 검수가 왜 리스크인지의 실물 사례다.

이게 지금 가장 큰 리스크다 — 6회차에 나온 문제 셋(**반복 범위 입력 불가 · 태그 늘어남 ·
대체 기록 사라짐**)은 **전부 폰에서 써봐야 나온 것들**이었고, 당시 PC 브라우저 테스트로는
하나도 잡히지 않았다.

다만 7회차에 **코드 레벨 검수**를 돌려 10줄 중 9줄을 PC 에서 판정했다 (아래 표).
**남은 것은 폰에서만 증명되는 성질의 것들**이다 — iOS 키패드 실물, Wake Lock,
클립보드 제스처, 홈 화면 설치, 비행기 모드, 그리고 실제 iOS 폰트 폭.

#### 검수 체크리스트 — 「폰」 칸은 여전히 사용자가 채운다

**코드** 칸은 7회차 자동 검수 결과다. **폰** 칸을 코드 결과로 채우지 말 것 —
6회차의 세 문제는 전부 코드가 맞는데 폰에서 틀린 것들이었다.

| # | 확인할 것 | 통과 기준 | 코드 | 폰 |
|---|---|---|---|---|
| 1 | 종목 카드 맨 윗줄 | 태그가 **작은 알약**, 오른쪽에 `건너뛰기`·`⚙` 가 **한 줄**로 | ✅ (v9 에서 고침) | ⬜ |
| 2 | `+ 세트 추가` | 세트 줄 +1, 진행률 분모 +1, 프로그램은 안 바뀜 | ✅ | **✅ 11회차** — 벤치프레스 4세트 + `· 오늘 4세트 수행` |
| 3 | 한 종목 세트 전부 ✓ | 아래에 **RIR 칩 3개**가 뜬다 | ✅ | ❌ **11회차 · 폰에서 실패 → `v12` 에서 고침.** 재확인 필요 |
| 4 | `⚙` → 반복 범위 | 숫자 키패드로 **두 칸** 입력 가능 | ✅ 두 칸·순수 숫자 | ⬜ **키패드 실물** |
| 5 | 저장 직후 | **「초기화하고 다음 Day 로」가 화면에 보인다** | ✅ 표시·위치 | ⬜ **스크롤 동작** |
| 6 | **대체 → 저장 → 앱 껐다 켜기 → 그 Day 열기** | 대체 종목이 **미리 선택**되고 지난 기록·프리필이 따라온다. `지난 기록 없음` 이면 **실패** | ✅✅ 리로드까지 | ⬜ |
| 7 | `⚙` → 「이 자리를 다른 종목으로 교체」 | **순서가 그 자리 그대로.** 맨 뒤로 가면 실패 | ✅ | ⬜ |
| 8 | `☐ 건너뛰기` | 카드가 접히고 **진행률 분모에서 빠진다** (다 하면 100%) | ✅ | ⬜ |
| 9 | 메모 + 웜업 체크 → 저장 → 프롬프트 복사 | 프롬프트에 `웜업:` `메모:` `· NN분` 줄이 있다 | ✅ | **부분 ✅ 11회차** — `메모:` 와 `· 49분` 확인. **`웜업:` 은 체크를 안 해 미확인** |
| 10 | 터치감 · 스크롤 길이 | 땀 묻은 손으로 눌러지는가. 카드가 길어진 게 거슬리는가 | ✅ 44px 전수 | ⬜ **감각** |
| 11 | **`[ 러닝 ]` 탭 → 폼 열기** | 한 화면에 다 들어가고 **가로 스크롤이 없다** | ⚠ **390px 미실측** | ⬜ **여기부터 볼 것** |
| 12 | 거리 칸에 `487` 치기 | 숫자 키패드가 뜨고 **`4.87` 로 자동 변환**. 소수점 키를 찾을 일이 없다 | ✅ 실제 키 입력 | ⬜ **키패드 실물** |
| 13 | 시간 칸에 `2631` 치기 | `26:31`. `2675` 면 저장이 막히고 사유가 뜬다 | ✅ | ⬜ |
| 14 | 러닝 저장 → 앱 껐다 켜기 → 러닝 탭 | 기록이 남아 있고 주간 누적이 맞다 | ✅ 리로드까지 | ⬜ |
| 15 | **일요일 밤에 뛴 것** | 목록에서 **그 주**에 들어간다 (다음 주 아님) | ✅ | ⬜ |
| 16 | 세션 저장 → 프롬프트 복사 | `[최근 러닝 3회]` 와 `[이번 주 러닝]` 이 들어 있다 | ✅ | ⬜ |

**11번이 이번 배포의 유일한 미실측 항목이다.** 창을 390px 로 줄이는 순간 브라우저가
죽어서 확인을 못 했다 (10회차 「확인하지 못한 것」). 폰을 열면 **여기부터** 볼 것.

**v9 에서 건너뛰기가 글자 없는 44px 원형 버튼이 됐다.** 태그 줄을 한 줄로 되돌리려고
줄인 것인데, **눌러볼 생각이 드는지**가 10번의 새 관전 포인트다. 헷갈리면 글자를
되살리는 대신 `target` 태그를 아예 빼는 쪽이 있다 (아래 「7회차」 참조).

#### 기다려야만 확인되는 것

**날짜 경계** — 다음 날 앱을 열었을 때 지난 중량이 회색으로 프리필되는가.
**7회차에 시계를 하루·30일 앞당겨 코드 레벨로 증명했다** — 세트별 무게가 각 세트에
맞게 ghost 로 들어오고, 달을 넘겨도(월별 파일 분기) 이어지고, 브리핑이 「1일 전」으로
바뀌는 것까지. 폰에서의 최종 확인만 남았다.

---

## 6회차에 만든 것 — 기능 지도

하루에 `v3`~`v8` 여섯 번을 배포했다. 무엇이 어디에 있는지부터.

| 배포 | 무엇 | 계기 |
|---|---|---|
| `v3` | **5단계 프로그램 편집** (총정리 아래 접힌 편집기) | *"Day별 운동종목을 내가 추가하거나 수정할 수 없네"* |
| `v4` | 반복 범위를 **칸 두 개**로 | *"8회에서 12회 같이 설정할 수가 없는데 숫자패드만 떠서"* |
| `v5` | **세션 중 퀵수정** (세트 증감 · 건너뛰기 · ⚙ 시트 · 오늘 종목 추가) | *"운동 세션 중간에 수정할 수 있게"* · *"퀵수정을 다 적용"* |
| `v6` | 태그 줄 정렬 · **자리 교체** · 증량 단위를 시트에서 뺌 | *"동그란 겉에 레이어가 너무 비대"* · *"바벨 백스쿼트를 아예 다른 운동으로"* |
| `v7` | **4-8 대체 종목 기억** · 4-5 주차 · 지난 세션 보기 · 코칭 프롬프트 정리 | *"대체운동 선택한 게 기록이 사라져서"* + 프롬프트 실물 감사 |
| `v8` | **4-1 RIR 칩** · **4-2 노트 · 웜업 기록 · 소요 시간** | SCHEMA 코칭 프롬프트 계약 미충족 3건 중 2건 |

**여섯 중 다섯이 실사용 피드백에서 나왔다.** SPEC 의 4단계 순서와 맞은 것은 마지막 하나뿐이다.

시안 비교와 설계 근거 — https://claude.ai/code/artifact/c2c902b9-85d7-433c-b88d-1f2c9f60de97

### 설계의 출발점 — 세션 중 실제로 바뀌는 것의 빈도

퀵수정에 6개 필드를 다 올리려다 멈추고 먼저 세어봤다. **이 표가 배치의 근거 전부다.**

| 무엇 | 빈도 | 범위 | 어디에 뒀나 |
|---|---|---|---|
| 세트 증감 | **잦음** | 오늘만 | 카드 (1탭) |
| 기구가 차 있음 | **잦음** | 오늘만 | 카드 → `⚙` 시트 (대체 종목) |
| 휴식이 짧다/길다 | **잦음** | 그 회차 | 이미 있던 `+30s` — 손대지 않음 |
| 종목 건너뛰기 | 가끔 | 오늘만 | 카드 체크박스 (1탭) |
| 종목 추가 | 가끔 | 오늘만 | 목록 맨 아래 |
| 자리 교체 | 가끔 | 계획 | `⚙` 시트 |
| 반복 범위 · 휴식 기본값 | 드묾 | 계획 | `⚙` 시트 |
| **증량 단위** | **드묾** | 계획 | **시트에서 뺐다** — 5단계 편집기에만 |

**빈도에 탭 수를 맞춘다.** 잦은 것은 눈에 보이는 자리에서 1탭, 드문 것은 한 단계 뒤로.

### ⭐ 자리가 곧 범위다 — 이 규칙 하나가 전부다

> **카드에서 하는 것은 언제나 오늘만. `⚙` 시트에서 하는 것은 언제나 계획.**

**「다음부터도 적용」 체크박스는 두지 않는다.** 1차 설계에 있었는데 모순이었다 —
체크는 시트 안에 있고 세트 증감은 카드에서 하니, 카드에서 누른 것에 시트의 체크가
소급 적용되느냐는 물음에 답이 없었다. 자리로 가르면 확인할 상태 자체가 없어진다.

| 어디 | 무엇 | 범위 |
|---|---|---|
| 카드 · 세트 목록 아래 | `+ 세트 추가` / `−` | 오늘만 (`sessionSets`) |
| 카드 · 태그 줄 | `☐ 건너뛰기` · `⚙` | 오늘만 (`sessionSkip`) |
| `⚙` 시트 · 오늘만 구역 | 대체 종목 · **다른 종목으로(오늘만)** · 되돌리기 | 오늘만 (`swaps`) |
| `⚙` 시트 · 프로그램 구역 | 계획 세트 · 반복 범위 · 휴식 · **이 자리를 다른 종목으로 교체** | **즉시 `wk:program`** |
| 종목 목록 맨 아래 | `+ 오늘 종목 추가` | 오늘만 (`sessionAdds`) |

### 구현하며 정한 것

| 결정 | 근거 |
|---|---|
| **`arr.length === e.sets` → `>=`** | 계획 3세트인데 오늘 4세트를 하면 `4===3` 이 거짓이라 **네 세트 전부 목표 상단을 채워도 증량 제안이 사라졌다.** 더 했는데 벌을 받는 셈이다. `>=` 면 줄였을 때는 여전히 거짓이라 "계획만큼 채워야 증량"이라는 원래 의도가 지켜진다. 비교 대상은 언제나 **계획** 세트 수다 |
| `sessionAdds` 는 **언제나 목록 맨 뒤** | `entries`·`swaps` 가 배열 인덱스 키다. 중간에 끼우면 이미 입력한 값이 옆 종목으로 옮겨 붙는다 |
| 「이 Day 프로그램에도 넣기」는 **저장 시점에** 반영 | 세션 중에 `D().ex` 에 밀어 넣으면 `todayEx()` 뒷부분 순서가 바뀐다. 저장 뒤 `promoteAdds()` 가 옮기고 **같은 객체 참조로 인덱스를 다시 매핑**한다 |
| 휴식은 스테퍼가 아니라 **칩** | 실제로 쓰는 값이 몇 개뿐인데 스테퍼면 여러 번 눌러야 한다 |
| **증량 단위는 이 시트에 두지 않는다** | 이중 점진의 증가폭이라 필요한 값이지만 **세션 중에 고칠 일이 없다.** 5단계 편집기에 있다 |
| **자리를 지키는 교체**를 따로 둔다 | 「오늘 종목 추가」로는 안 된다 — 맨 뒤에 붙고 원래 것을 따로 건너뛰어야 해서 **순서가 어긋나고 손이 두 번 간다.** 실사용에서 나온 지적이다. 교체는 그 자리에 그대로 들어가고, 그 자리의 `entries`·`swaps`·오늘 값은 전부 지운다 (다른 종목이 들어왔으니 의미가 없다). **지난 기록은 어느 쪽도 지우지 않는다** — 이름 변경이 아니라 다른 종목이라 `was` 도 남기지 않는다 |
| `.tagline` 에 **`align-items:center`** | 기본값이 `stretch` 다. 44px 짜리 건너뛰기·⚙ 가 같은 줄에 들어오면서 **옆의 작은 태그들이 44px 로 늘어나** 글자에 비해 알약이 뚱뚱해 보였다. 실사용에서 나온 지적이다 |
| 건너뛰기·⚙ 를 **한 덩어리(`.tagctl`)로 묶는다** | 따로 두면 줄바꿈될 때 ⚙ 만 다음 줄로 내려가 태그 줄이 3줄(94px)이 됐다. 묶으니 3줄이 2줄로 줄었다. ⚠ **여기 「전 카드가 한 줄에 들어간다」고 적어두었던 것은 틀렸다** — 7회차 실측에서 매 Day 첫 카드가 아이폰 폭에서 두 줄이었다. 한 줄은 v9 에서야 됐다 (아래 「7회차」) |
| 종목 추가는 **고르기가 먼저**, 직접 입력은 뒤 | 추가하는 종목은 대개 이미 아는 종목이다. 고르면 키보드가 안 뜨고 **지난 중량까지 따라온다** |
| 직접 입력한 종목의 `target` 은 **빈 문자열** | `"오늘 추가"` 를 넣었더니 같은 글자의 태그가 두 번 떴다 |
| 건너뛴 종목은 `exercises` 에 **넣지 않는다** | 안 한 것은 기록이 아니다. 대신 코칭 프롬프트에 「오늘 건너뛴 종목」 줄로 넘긴다 — 볼륨이 왜 줄었는지 알아야 조언이 맞는다 |
| 오늘 추가한 종목은 `planned: null` | 계획에 없던 종목이다. 대체(`planned` = 원래 종목)와 구분된다 |
| **카드 세로 길이 +9px** | 대체 종목 버튼(−59px)이 시트로 들어간 대신 세트 증감 줄(+47px)이 붙고 태그 줄이 23→44px 로 커졌다. 아이폰 폭에서 **전 카드가 태그 줄 한 줄**로 들어가는 것을 실측했다 |
| 롱프레스는 쓰지 않는다 | iOS 가 텍스트 선택과 돋보기를 먼저 붙인다. 무엇보다 눌러볼 생각을 하지 못한다 |
| **4-8 대체 종목 기억** — 실사용에서 터진 버그 | 기록의 키는 **실제 수행 종목**이다. 바벨 백스쿼트 자리에서 고블릿 스쿼트를 하면 기록은 「고블릿 스쿼트」에 남는데, 다음에 Day 2 를 열면 화면은 바벨 백스쿼트를 보여주며 **"지난 기록 없음"** 이라고 한다 — 한 운동이 통째로 사라진 것처럼 보인다. `idx.swapMemory[계획종목]=실제종목` 을 저장에 기록하고 그 Day 에 들어올 때 미리 골라둔다. **되돌리면 기억도 지운다** — 안 지우면 원래 종목으로 돌아갈 길이 없다 |
| 총정리 「기록 N」 배지도 **대체분을 센다** | 프로그램 이름으로만 세면 같은 이유로 한 운동이 없어 보인다 |
| **지난 세션 펼쳐보기** (브리핑의 「마지막 수행」 줄) | 앱을 다시 열면 그날 한 것을 볼 방법이 **아예 없었다.** 읽기 전용이다 — 지난 기록을 화면에서 고치는 길은 만들지 않는다 |
| 저장 후 스크롤은 **`#saved`** 로 | 코칭 상자로 스크롤하면 그 48px 위의 「초기화」 버튼이 화면 밖으로 밀려 "초기화 버튼이 어디 갔냐" 가 된다 |
| **RIR 칩은 종목을 마쳤을 때만** 뜨고 같은 칩을 다시 누르면 해제된다 (4-1) | 세트마다 물으면 21탭이지만 종목당 한 번이면 6~7탭이다. 잘못 눌렀을 때 되돌릴 길이 없으면 안 된다 |
| `durationSec` 의 기준점을 **드래프트에만** 둔다 | 첫 세트 완료 시각(epoch)이 필요한데 기록에는 남기면 안 된다. `wk:draft` 는 SCHEMA 가 **「동기화 대상 아님」**으로 못박은 키이고 저장하면 지워진다 — 폰 밖으로 나가는 것은 `durationSec` 뿐이다. **1분 미만은 표기하지 않는다** (`0분` 은 노이즈다) |
| `warmup` 은 SCHEMA 예시의 **고정 키 대신 `{done:[이름], min:N}`** | 예시(`foamRoll` 등)는 Day 1 기준인데 **프로그램이 편집 가능하므로 그 키를 보장할 수 없다.** 실제로 체크한 항목 이름을 담는다 |
| 세션 텍스트에 붙는 새 줄은 전부 **`kg x` 패턴이 없다** | 파서는 그 패턴만 본다 (SCHEMA 「파싱 규칙」). `웜업:` `메모:` `마무리:` 와 헤더 뒤 `· 62분` 이 모두 자동으로 건너뛰어지는 것을 왕복 검증했다 |
| RIR 이 `한계였음` 이어도 **증량 제안은 그대로** | 진행 규칙은 이중 점진이다. RIR 은 코치가 판단할 재료지 앱이 규칙을 뒤집을 근거가 아니다 (반응형 디로드는 별개 항목) |
| 코칭 프롬프트 — 건너뛴 종목은 **별도 블록**, 세트 수가 계획과 다르면 **명시** | 목표 목록에 붙여 두면 목표의 일부로 읽힌다. 목표는 2세트인데 기록에 3세트가 있으면 코치가 볼륨 변화를 계획 변경으로 오해한다 |
| 휴식 바는 손대지 않는다 | `+30s` 가 이미 그 일을 한다. 게다가 거기 `건너뛰기` 는 **휴식**을 건너뛴다는 뜻이라 이름이 충돌한다 |

### 검토했으나 채택하지 않은 것

| 무엇 | 왜 안 했나 |
|---|---|
| **롱프레스로 퀵수정 열기** | iOS 가 텍스트 선택과 돋보기를 먼저 붙인다. 숫자 칸이 촘촘한 카드에서 충돌이 잦고, 무엇보다 **눌러볼 생각을 하지 못한다** |
| **휴식 바에 조작 얹기** | 휴식 중에만 존재해 "오늘은 2세트만" 을 미리 못 정한다. 바가 좁아 버튼 3개가 한계고, 거기 `건너뛰기` 는 **휴식**을 건너뛴다는 뜻이라 이름이 충돌한다 |
| **편집 모드 토글** (평소엔 깨끗, 켜면 전부 인라인) | 운동 **시작 전**에는 셋 중 가장 좋았다. 그런데 그건 이미 프로그램 편집기가 하는 일이고, **세트 사이 40초에는 최악**이다 — 가장 잦은 조작에 가장 많은 탭이 든다 |
| **전부 `···` 시트로** (카드는 지금 그대로) | 카드가 안 길어지는 대신 가장 잦은 「세트 하나 더」에 3탭이 들고, 항목이 다섯이라 시트 안에서 스크롤이 난다 |
| **세션 화면에서 지난 기록 고치기** | 4-9 의 영역이다. 지금 만들면 사고가 난다. 「지난 세션 보기」는 **읽기 전용**으로만 |

### PC 에서 검증한 것

세트 증감(계획 불변) · 건너뛰기 토글과 진행률 분모 · `activeIdx` 가 건너뛴 종목에서 안 멈춤 ·
드래프트 복원(`sessionSets`·`sessionSkip`·`sessionAdds`·`rirs`·`note`·`startMark`) ·
시트 항목 전부 `wk:program` 기록(한 조작에 `rev` +1, 중복 쓰기 없음) · 반복 범위 역전 거부 ·
종목 추가 2경로와 중복 거부 · 자리 교체(순서 유지 · 입력 초기화 · 자기 자신 제외) ·
**`>=` 로 4세트 참 / 2세트 거짓** · `promoteAdds` 인덱스 재매핑 ·
**대체 기억**(저장 → 재실행 → 미리 선택 → 프리필 · 되돌리면 기억까지 삭제 · Day 재진입 후에도 안 되살아남) ·
RIR 칩(완료 후에만 표시 · 선택/해제 · 기록 기입 · 지난 RIR 표시) ·
`durationSec`·`note`·`warmup`·`finisher` 저장 · **백업 텍스트 파서 왕복**(새 줄이 종목으로 안 잡히고 세트가 동일 복원) ·
5단계 편집기와의 상호작용 · 아이폰 폭(366px) 레이아웃과 44px 터치 타깃 · 콘솔 오류 0.

**폰에서는 아직 안 눌러봤다** — 위 「검수 체크리스트」 참조.

---

## 7회차 — 코드 레벨 검수 (2026-08-16) · `v9`

폰 검수 결과를 기다리는 대신 **PC 에서 판정 가능한 것을 전부 자동으로 돌렸다.**
총 **242건** — Node 하네스 163건 + 실제 Chrome 79건.

**하네스는 프로젝트에 남기지 않았다** (원칙 2 — 빌드 도구 없음). 스크래치패드에서
`index.html` 의 인라인 스크립트를 떼어내 최소 DOM 스텁 위에 올리는 방식이고,
`innerHTML` 이 문자열로 남아 **렌더 결과까지 그대로 검사된다.** 다시 만들려면
`<script>`~`</script>` 를 추출해 `document`·`localStorage`·`navigator` 를 스텁하고,
스크립트 끝에 `globalThis.__ev=function(c){return eval(c)}` 를 덧붙여 앱 스코프의
`let`/`const` 에 접근하면 된다. 시계는 `Date` 를 갈아끼워 앞당긴다.

### 고친 것 셋

| 무엇 | 증상 | 고침 |
|---|---|---|
| **`promoteAdds` 가 `rirs` 를 재매핑하지 않았다** | 인덱스를 키로 쓰는 세션 상태는 **다섯**인데 넷만 옮기고 있었다. 오늘 종목을 2개 이상 추가하고 일부만 「프로그램에도 넣기」를 켜면 순서가 뒤집히는데, `rirs` 만 옛 자리에 남아 **재저장에서 A종목의 RIR 이 B종목에 기록됐다.** 화면 칩도 옆 종목에 붙는다 | `nr` 추가 + `rirs=nr` |
| **`parseBackup` 의 `allTop` 이 `===`** | 저장 경로는 6회차에 `>=` 로 고쳤는데 **복원 경로만 `===` 로 남아 있었다.** 폰을 새로 깔고 백업 텍스트로 복원하면 계획보다 더 한 날의 증량 제안이 사라진다 — 「더 했는데 벌을 받는」 그 문제 | `>=` |
| **태그 줄이 아이폰 폭에서 두 줄** | 매 Day **첫 카드**가 전부 해당. 한 줄에 필요한 폭이 Day1 392pt · Day3/5 424pt · **Day2/4 448pt** 였다. 아이폰은 390/393pt, Pro Max 도 430pt다 | 아래 |

**태그 줄이 왜 첫 카드에서만 터지나** — 「세션 첫 종목은 프리웨이트 복합운동」이
프로그램 설계라 **첫 카드에만 `프리웨이트` 태그(79px)가 붙고**, 그 Day 들의 `target` 이
`대퇴사두·코어` 처럼 길다. 구조적이라 어쩌다 생긴 게 아니다.

두 가지를 같이 넣어 임계폭을 **448pt → 357pt** 로 낮췄다 (390pt 에서 33pt 여유).

- **`건너뛰기` 를 글자 없는 44px 원형 버튼으로** (`.skipbox.icon`). 약 49px 절약.
  **접힌 카드(`skipcard`)의 「되돌리기」는 글자를 그대로 뒀다** — 자리가 남고,
  되돌아가는 길이라 눈에 띄어야 한다. `aria-label` 과 `title` 은 유지
- **`target` 태그를 `·` 앞만 표시** (`tagTarget()`). `대퇴사두·코어` → `대퇴사두`.
  약 30px 절약. **`wk:program` 의 `target` 원본은 건드리지 않는다** — 화면 태그만 줄인다

⚠ **글자를 뺀 대가는 발견 가능성이다.** 빈 원이 `⚙` 옆에 있는 꼴이라 「완료」 체크와
헷갈릴 여지가 있다. 폰에서 거슬리면 **글자를 되살리는 대신 `target` 태그를 아예 빼는
쪽**을 먼저 볼 것 — 그러면 여유가 더 생겨 글자를 도로 넣을 수 있다.

### 고치지 않은 것 — 알고 두는 것

| 무엇 | 왜 두었나 |
|---|---|
| 백업 왕복 시 **`rir` 손실** | 텍스트에는 `· 한계였음` 이 있는데 파서가 안 읽는다. 복원 후 「지난 기록 · 한계였음」 표시만 사라진다. 프리필·증량 판정은 멀쩡하다. `mode` 도 손실되지만 **읽는 코드가 없다**(전수 확인) |
| **마이그레이션 껍데기가 조용히 넘어간다** | `MIGRATIONS[v+1]` 이 없으면 `break` 후 `schema` 를 안 올린 채 `true` 를 반환하고 배너도 없다. SCHEMA 「조용히 넘어가지 않는다」와 어긋나지만 **앱이 `schema 0` 을 쓴 적이 없어 오늘은 도달 불가**다. 1→2 변환을 만들 때 같이 손볼 것 |
| **`swapMemory` 고아 키** | 자리 교체·이름 변경·종목 제거 후에도 옛 이름의 대체 기억이 남는다. 화면엔 안 새지만 **같은 이름을 다시 프로그램에 넣으면 옛 대체가 되살아난다**(재현 확인). 4-9 를 할 때 같이 볼 것 |

### 검수로 증명된 것 (폰 검수 전에 이미 아는 것)

`rotation` 5바퀴 순환과 `cycles`·`byDay` · 재저장 시 같은 id `rev+1` 과 이중 카운트 방지 ·
드래프트가 rotation 을 가로채지 않음 · 용량 부족 시 실패 표시와 래치 해제 ·
Day 삭제 시 `byDay` 당기기 · 이름 변경의 `was` 와 키 이동 · **날짜/월 경계 프리필** ·
사용자 입력 종목명 `esc()` (카드·시트·백업·프롬프트 전부) · 코칭 프롬프트 계약 6/7 항목과
길이 상한(48줄 고정) · 백업 텍스트 왕복 · 세션 화면 터치 타깃 41개 전부 44px↑ ·
서비스워커가 `/workout/` 서브경로에 등록되고 캐시 7파일 전부 상대경로 · 콘솔 오류 0.

### 자동 검수로는 끝까지 못 본 것

- **`behavior:"smooth"` 스크롤** — 자동화 탭이 `visibilityState:"hidden"` 이라 Chrome 이
  애니메이션을 통째로 생략한다 (`auto` 는 정상 동작 확인). 저장 후 「초기화」로 스크롤되는지는
  **앱 결함 여부를 판정할 수 없어 미검증으로 남긴다**
- **실제 iOS 폰트 폭** — 위 임계폭은 Windows 폴백 폰트 기준이다. `프리웨이트` 실측 51px 대
  Apple SD Gothic Neo 가정 50px 로 오차는 ±2% 라 결론은 안 바뀌지만, 숫자를 그대로 믿지는 말 것

---

## 8회차 — 프로그램 데이터 변경 (2026-08-17) · `v10`

사용자가 프로그램 3건을 바꿨다. **근거는 PROGRAM.md 「설계 근거」에 있다 — 되살리지 말 것.**

| 무엇 | 결과 |
|---|---|
| **카프레이즈 전면 제거** (Day 2·4) | 종아리 발달을 원하지 않는다. 러닝이 이미 종아리를 쓰고 **카프 강화의 러닝 부상 예방 근거도 강하지 않다.** 「러너에게 카프가 필요하다」로 되살리지 말 것 |
| **Day 1 #6 종목 교체** | `트라이셉스 익스텐션 머신` → `오버헤드 덤벨 트라이셉스 익스텐션`. 헬스장에 머신이 없다. `inc` 5→2.5, `equip` 머신→프리 |
| **Day 2 에 `힙 어브덕션 머신`** | 중둔근이 프로그램 전체에서 비어 있던 자리다. 바이셉스 컬 **앞자리**에 넣어 Day 2 는 6종목 16세트를 유지 |
| **밤 러닝 5km 고정** | Day1/3/5 `5km 평소 페이스`. Day2/4 는 거리 같고 **이지 페이스** 유지. `CONTEXT` 문자열과 문서의 주간 총량도 **주 25km** 로 |

**한 바퀴 32종목 90세트 → 31종목 87세트.** Day 4 는 7종목 19세트 → 6종목 16세트.
**Day 4 에 대체 종목을 넣지 않은 것은 의도다** — 세션이 길어 뒤쪽이 잘렸다. 채우지 말 것.

### ⭐ 프로그램 개정 장치 (`PROGRAM_REV`) — 이번에 새로 만든 것

**`DAYS` 를 고쳐 배포해도 폰의 `wk:program` 은 따라오지 않는다.** 진실이 그쪽이기 때문이다.
지금까지 반영할 길은 「기본값으로 되돌리기」뿐이었는데 그건 **사용자 편집을 통째로 날리고,
종목 이름이 바뀌었을 때 `lastByExercise` 키를 옮기지 않아 기록이 끊긴다** (`edReset()` 은
`moveRecord()` 를 부르지 않는다). 구조적 구멍이라 장치를 만들었다.

```
PROGRAM_REV = 2   ·   program.progRev 가 낮으면 PROGRAM_PATCHES[n] 을 순서대로 적용
```

| 규칙 | 왜 |
|---|---|
| **조건을 보고 움직인다** | 이미 사용자가 뺐거나 이름을 바꾼 종목은 안 건드린다. 두 번 돌아도 결과가 같다(멱등) |
| **정의는 `fromDAYS()` 로 `DAYS` 에서 가져온다** | 패치에 다시 적으면 둘이 어긋난다. 단일 진실 유지 |
| **`moveRecord()` 를 재사용** | 기록 키 이동 + `was` 를 편집기와 똑같이 처리한다. **프로그램에서 옛 이름이 사라진 뒤에** 불러야 한다 (다른 Day 에 남아 있으면 안 옮기는 규칙이 있다) |
| **`blankProgram()` 이 `progRev` 를 최신으로 박는다** | 새로 설치한 폰은 `DAYS` 를 그대로 받으므로 개정이 아예 안 돈다 |
| **`run` 은 `DAYS` 를 따라간다** | 편집기에서 못 고치는 값이라 덮어써도 잃을 편집이 없다. 종목 목록을 안 건드리므로 `touched` 에 넣지 않는다 |
| **구성이 바뀐 Day 의 드래프트는 비우고 배너로 알린다** | `entries`·`swaps` 가 **배열 인덱스**를 키로 잡는다. 그대로 두면 입력값이 옆 종목에 붙는다 (6회차에 데인 그 문제). 조용히 넘기지 않는다 |

**다음에 프로그램을 또 바꿀 때는 `PROGRAM_REV` 를 올리고 `PROGRAM_PATCHES` 에 블록을 하나 더한다.**
`DAYS` 만 고치고 끝내면 폰에 반영되지 않는다 — 이게 이 장치가 존재하는 이유다.

### 검증

**Node 226건 + 브라우저.** 개정 전용 41건을 따로 만들었다 — 사용자 편집(세트·반복·추가 종목·
Day 제목) 보존 · 멱등성(3회 부팅) · 이미 손댄 종목 비간섭 · 기록 키 이동과 `was` ·
드래프트 안전장치(바뀐 Day 는 비우고, 안 바뀐 Day 3 은 그대로 복원).

실제 브라우저에서 **v9 폰 상태를 localStorage 에 심고 로드**해 전 항목을 확인했다.
`run` 이 폰에 반영되지 않던 것은 **브라우저 검증에서만 잡혔다** — Node 쪽은 `DAYS` 를 직접
읽어 통과했기 때문이다. 폰의 저장된 상태를 재현해봐야 보이는 종류다.

⚠ **하네스로 드래프트를 심고 같은 탭에서 리로드하면 옛 페이지의 `pagehide` 플러시가
그 드래프트를 먼저 지운다.** 앱의 정상 동작이다. 탭을 새로 열어 검증할 것.

---

## 9회차 — 6단계 러닝 탭 설계 확정 (2026-08-17) · 배포 없음

**코드는 아직 한 줄도 안 짰다.** 시안을 만들어 폰에서 만져보고 **설계만 확정**했다.
확정 내용은 **`SPEC.md` 6단계와 `SCHEMA.md` 「러닝」에 이미 반영돼 있다** — 거기가 기준이다.

시안(실물 프로토타입) — https://claude.ai/code/artifact/7e919a59-339c-4c4c-a7cc-66a9c0cfd9b4
**시안과 문서가 어긋나면 문서가 기준이다.**

### 정해진 것

| | 결정 | 근거 |
|---|---|---|
| `km` | **소수 2자리** (SCHEMA 변경) | 시계에 `4.87` 이 찍히는데 반올림할 이유가 없다. 5.0 으로 뭉개면 페이스가 8초 틀린다. `schema` 번호는 안 올린다 — 정밀도만 넓히는 것이라 옛 데이터도 읽힌다 |
| **입력** | 거리·시간 모두 **숫자만.** `533`→5.33 · `2631`→26:31 | **소수점·콜론 키를 찾게 하지 않는다.** 6회차 하이픈과 같은 종류의 함정을 아예 없앤다. `inputmode="numeric"` 이면 키패드가 무엇이든 상관없다 |
| **시간** | 분:초로만 받고 **표시만** `1:02:18` | 1시간이 넘으면 분으로 이어 친다(`6218`→62:18). 입력에 자릿수 규칙을 두 개 두지 않는다 |
| **프리필** | ⚠ 「직전 값」이 아니라 **`5.00` 고정** | 밤 러닝 5km 고정(8회차) 때문에 SPEC 원안이 어긋났다. 롱런 다음 날 10.9 가 떠서 매번 지워야 한다. 회색(ghost)으로 띄운다 |
| **주** | **월요일 ~ 일요일** (ISO). 범위를 **날짜로 표시** | `2026-W34` 만으로는 어디부터인지 안 보인다. 목록도 주 경계에서 가른다 |
| **종류 칩** | 이지 / 평소 / 롱런 / 인터벌 **넷 다** | 롱런을 실제로 뛴다 |
| **힌트 줄** | **두지 않는다** | 혼자 쓰는 앱이라 몇 번이면 손에 붙는다 |
| 페이스 | 입력도 저장도 안 한다 | SCHEMA 「러닝」 — 계산해서 표시만 |

### 시안을 만들며 배운 것

- **아티팩트 주소(`claude.ai/...`)는 폰에서 로그인이 필요하다.** 폰 확인용 시안을 만들 때는
  그것부터 확인할 것. 안 되면 **사용자 Pages 에 임시 페이지로 올리는 길**이 있다
  (리포가 public 이라 공개된다는 점은 미리 알릴 것)
- 로컬 `python -m http.server` 는 **charset 을 안 보낸다.** 한글이 깨져서 문자열 비교가
  전부 실패한다. 테스트할 때는 `<meta charset>` 을 붙인 사본으로 서빙할 것
- 시안은 **앱의 토큰을 그대로 썼다.** 러닝 축은 `--slate`, 웨이트 축은 `--brass` —
  앱의 `.runline` 이 이미 slate 를 쓰고 있어 새로 만든 게 아니라 있는 규칙을 이었다

### 이 설계로 구현했다 → 10회차

---

## 10회차 — 6단계 러닝 탭 구현 (2026-08-17) · `v11`

**9회차에 확정한 설계를 그대로 구현했다. 문서와 다른 곳은 없다.**
`SPEC.md` 6단계에 완료 표시와 「구현하면서 정한 것」을 남겼다.

### 바꾼 것

| 무엇 | 자리 |
|---|---|
| **축 전환** `[ 웨이트 ] [ 러닝 ]` | `.axis` · `setAxis()`. 웨이트 15개 요소를 `#wtaxis` 로 **감싸기만** 했다 — id·순서·핸들러 무변경 |
| **러닝 화면** | `#runaxis` · `renderRun()` · `runCard()`. 주간 누적 · 입력 폼 · 주 경계로 가른 목록 |
| **숫자만 입력** | `shiftFmt()` · `bindDigits()`. `533`→5.33 · `2631`→26:31. 둘 다 `inputmode="numeric"` |
| **저장** | `saveRun()` → `wk:YYYY-MM` 의 `runs[]` · `idx.counts.runs` · `idx.recentRunWeeks` |
| **코칭 프롬프트** | `[최근 러닝 3회]` + `[이번 주 러닝]` → **계약 7/7 완성** |
| **백업 내보내기** | `runLine()`. 세션과 날짜순으로 섞는다 |
| `COACH-BRIEF.md` 3절 | 「러닝 데이터는 아직 안 온다」 → 「이제 같이 온다」 + 읽는 법 |
| `sw.js` | `v10` → **`v11`** |

### 같이 고친 것 — 7회차 「고치지 않은 것」 3건 + 새로 찾은 2건

| 무엇 | 어떻게 |
|---|---|
| 백업 왕복 시 `rir` 손실 | 파서가 `· 한계였음` 을 읽어 되살린다 (`RIRTXT` 역인덱스) |
| 마이그레이션 껍데기가 조용히 넘어감 | `MIGRATIONS[v+1]` 이 없으면 `false` 를 반환해 배너를 띄운다 |
| `swapMemory` 고아 키 | `pruneSwapMemory()` — `load()` · `afterEdit()` · `sheetSaved()` 에서 돈다 |
| ★ **`applyPick` 이 `rirs` 를 안 지웠다** (새로 발견) | 자리를 교체하면 옛 종목의 RIR 이 새 종목에 그대로 붙어 저장됐다. `promoteAdds` 와 **같은 종류**다 — 인덱스를 키로 쓰는 세션 상태는 언제나 **다섯**이다 |
| ★ **파서가 `[` 로 시작하는 줄을 안 건너뛰었다** (새로 발견) | 러닝 메모에 `5kg x 10` 이 들어가면 `[날짜] 러닝` 이라는 엉뚱한 종목이 생긴다. `line.startsWith("[")` 로 끊는다 |

### 검증 — 372건 (Node 372 · 브라우저 별도)

⭐ **하네스를 리포 밖에 옮겨 보존했다 — 다음 세션은 새로 만들지 말 것.**

```bash
cd C:/Users/hr222/projects_hr/workout-tests && node run.js
```

`workout-tests/` 는 **리포 밖 옆 폴더**다. 일부러 그렇게 뒀다 — 원칙 2(프로젝트에
빌드 도구 없음)를 지키면서, 스크래치패드가 세션마다 날아가 매번 하네스를 다시
만들던 문제를 없앤 것이다. 커밋되지 않고 Pages 에도 올라가지 않는다.
`run.js` 가 index.html 에서 인라인 스크립트를 떼어내 `--check` 후 5개 스위트를 돌린다.

Node 하네스 **372건 전부 통과** (기존 226 + 러닝 146).
러닝 146건은 주 계산(ISO 8601 · 달·해 넘김) · 입력 형식 · 저장 필드의 SCHEMA 대조 ·
저장 실패 롤백 · 프리필 · 목록과 주 경계 · 페이지 나눔 · 프롬프트 계약 · 백업 파서 안전 ·
축 전환 · 옛 폰 호환을 덮는다.

브라우저(`localhost:8765/workout/`)에서 실제로 확인한 것 —
**실제 키보드로 `487` → `4.87`** · 소수점 키를 눌러도 무시 · `2631` → `26:31` ·
`2675` 에서 저장 차단과 사유 표시 · 저장된 JSON 이 SCHEMA 「러닝」과 정확히 일치
(`pace` 없음 · 시각 필드 없음) · **일요일(08.16)이 지난주 묶음으로 감** ·
이번 주 4.9km / 지난주 15.9km · 메모 `<가벼움>` 이 `esc()` 통과 ·
프롬프트 7항목 · 백업에 러닝 줄이 담기고 되읽어도 엉뚱한 종목이 안 생김.

### 확인하지 못한 것 — 그대로 적는다

- **390px 폭(아이폰) 레이아웃을 실측하지 못했다.** 브라우저 창을 390×844 로 줄이는
  순간 렌더러가 죽었고, 그 뒤로 Chrome 이 localhost 에 접속하지 못했다(curl 은 200).
  포트를 바꾸고 탭을 새로 열어도 같아서 더 파지 않고 멈췄다.
  데스크톱 폭에서는 가로 스크롤 0 · 터치 타깃 전부 44px↑ 를 확인했다.
  **산술로는 들어간다** — 390px 에서 내용 폭은 336px 이고 칩 4개가 `4×70 + 3×7 = 301px`
  다. 다만 **이건 계산이지 실측이 아니다.** 폰에서 볼 때 이 줄부터 확인할 것
- 폰 실기기 검수는 여전히 밀려 있다 (`v3`~`v11`, 아홉 배포치 · 11회차에 `v12` 가 더해졌다)

### 구현하며 배운 것 — 다음 세션이 반복하지 말 것

- **인덱스를 키로 쓰는 세션 상태는 다섯이다** (`entries` · `swaps` · `sessionSets` ·
  `sessionSkip` · `rirs`). 7회차에 `promoteAdds` 에서 한 번, 10회차에 `applyPick` 에서
  또 한 번 같은 실수가 나왔다. **자리를 바꾸거나 지우는 코드를 쓸 때마다 다섯을 셀 것**
- **텍스트 계약에 새 줄을 더할 때는 파서에 먹여보고 확인할 것.** 러닝 줄은 `kg x` 가
  없어 안전하다고 판단했는데, **사용자 메모가 그 패턴을 담을 수 있었다.**
  "내가 만드는 줄"만 보면 놓친다 — 사용자 입력이 섞이는 자리를 볼 것
- **나머지를 먼저 반올림하면 `5:60` 이 나온다.** 페이스처럼 소수 초를 다룰 때는
  총초를 먼저 정수로 만들 것 (`mmss` 주석에 남겼다)
- 브라우저 자동화에서 **물리 클릭 좌표가 안 맞을 수 있다.** 스크린샷 좌표계와 뷰포트가
  어긋나면 클릭이 조용히 빗나간다. `document.activeElement` 로 먼저 확인하고,
  요소는 JS 로 `focus()` 한 뒤 **실제 키 입력**을 보내면 입력 경로는 진짜로 검증된다

---

## 11회차 — RIR 칩 · 바퀴 계산 · 「오늘 마무리」 (2026-08-17) · `v12`·`v13`

문서 갱신으로 시작했는데 **실사용 프롬프트를 검사하다 기능 결함이 나와** 배포까지 갔다.

### `COACH-BRIEF.md` rev 2 — 코드와 대조해 고친 것

챗 대화방에 다시 올리려고 갱신하면서 **코드와 실제로 대조했다.** 셋이 어긋나 있었다.

| 무엇 | 어떻게 |
|---|---|
| **0절 신설 — 「지난 브리프 이후 달라진 것」** | 대화방이 이미 옛 버전을 읽었다. 전문을 다시 읽히는 대신 `v8`·`v10`·`v11` 델타를 표 하나로 주고 해당 절로 보낸다. **다음에 브리프를 다시 넘길 때 이 절을 갱신할 것** |
| ⚠ **케이던스 서술이 틀렸다** | 「심박·케이던스는 없을 수 있다」고 적혀 있었는데 **`coachText()` 는 `r.hr` 만 넣는다.** 케이던스와 러닝 메모는 `runLine()`(백업)에만 담긴다. 코치가 없는 값을 기다리게 되는 서술이었다 |
| **백업 내보내기 문법이 통째로 빠져 있었다** | 「두 종류가 온다」고 해놓고 프롬프트 문법만 있었다. `WORKOUT-EXPORT v2` 헤더 · 프로그램 줄(`3x8-12 \| RIR 2 \| 휴식 90 \| +5kg`) · `[날짜] 러닝:` 줄 · 날짜순 혼합 · 「누적 기록」 섹션 없음 |
| 프롬프트 블록 3개 누락 | `[사용자 상황]` · `[진행 규칙]` · `[답변 형식]`. `[진행 상황]` 의 `· 디로드 권고 구간` 조건도 |
| 9절 강화 | 되돌리기 부재가 코칭에 미치는 영향 — 이상한 값은 지적 말고 **물어볼 것**, 고칠 길은 백업 텍스트 수정뿐 |

### 실사용 프롬프트로 확인한 것 ← 폰 검수의 첫 실물 증거

사용자가 08-17 Day 1 세션의 코칭 프롬프트를 그대로 가져왔다. **"웨이트 쪽은 문제 없었다."**
프롬프트 자체가 폰 상태의 증거라 위 체크리스트 몇 칸을 채웠다.

- **`v11` 이 폰에 있다** — `[최근 러닝 3회]`·`[이번 주 러닝]` 블록은 v11 에만 있다
- **`v10` 의 `PROGRAM_REV` 개정이 폰에서 실제로 돌았다** — Day 1 #6 이
  `오버헤드 덤벨 트라이셉스 익스텐션` 으로 찍혔다. 8회차 장치의 첫 실전 검증이다.
  ⚠ 옛 세션 기록에는 `트라이셉스 익스텐션 머신` 이 그대로 남아 있다 — **의도된 동작**이다
  (`moveRecord()` 는 `lastByExercise` 키를 옮기지 기록의 이름을 소급하지 않는다)
- **세트 추가**(체크리스트 2) · **`durationSec` 49분** · **메모** · **`· 대체(...)` 두 건** 정상
- 벤치프레스 자리를 `덤벨 벤치프레스` → `벤치프레스` 로 대체했다. **`alts` 에 없는 이름**이라
  직접 입력 경로다. `swapMemory` 에 남았으므로 **다음 Day 1 에 벤치프레스가 미리 선택된다**

### ⭐ 물어본 결과 — RIR 칩이 **안 떴다.** 실사용에서 나온 첫 기능 결함

「안 눌렀나, 안 떴나」를 물었더니 **안 떴다**고 했다. 코드를 파보니 **마크업은 v8 부터
줄곧 옳았다** — 뜨자마자 안 보이게 되는 것이 문제였고, 셋이 한꺼번에 겹쳤다.

| # | 마지막 세트에 ✓ 를 누르면 | 어디 |
|---|---|---|
| 1 | 카드 전체가 `opacity:.45` 로 흐려진다. **방금 생긴 칩도 같이** | `.ex.done` |
| 2 | 휴식 바가 `position:fixed;bottom:0` 로 화면 아래를 덮는다. **칩은 카드 맨 아래**라 그 자리다 | `.rest` |
| 3 | 260ms 뒤 **다음 카드로 스크롤**한다. 칩이 위로 밀려 나간다 | 완료 핸들러 |

**흐려지고 → 가려지고 → 사라진다.** 마지막 종목은 3번이 없지만 1·2번은 그대로라
6종목 전부 못 본 것이 설명된다.

⚠ **`innerHTML` 만 보는 검사로는 절대 못 잡는 종류다.** 하네스가 v8 부터 이 칩을
통과시켜 왔다. **렌더 결과가 옳아도 화면에 안 보일 수 있다** — 앞으로 새 UI 를 넣을 때
「마크업이 나온다」와 「눈에 보인다」를 같은 것으로 치지 말 것.

### 고친 것 (`v12`)

| 무엇 | 어떻게 |
|---|---|
| **흐림에서 칩만 뺀다** | `.ex.done{opacity}` → `.ex.done > *:not(.rir):not(.rirlab){opacity:.45}`. opacity 는 부모에 걸면 자식이 못 벗어나서 **선택자를 바꿔야만** 된다 |
| **스크롤 목적지를 미룬다** | 마쳤는데 RIR 을 아직 안 골랐으면 다음 카드가 아니라 **칩**으로 스크롤한다(`block:"end"`). **칩을 고르면 그때** 다음 카드로 넘어간다. 안 고르고 지나가도 막지 않는다 — 「선택」이다 |
| **칩이 휴식 바에 안 깔린다** | `.rir` 에 `scroll-margin-bottom:calc(150px + env(safe-area-inset-bottom))`. ⚠ `body` 의 `padding-bottom:170px` 은 **문서 끝** 여백이라 문서 중간의 칩에는 아무 소용이 없다 — 처음에 이걸 대책으로 적었다가 정정했다 |
| **해제·되돌아오기에는 안 움직인다** | 칩을 다시 눌러 해제하면 스크롤하지 않는다. 다음 종목이 **이미 끝나 있으면**(나중에 되돌아와 고치는 경우다) 화면을 낚아채지 않는다 |

### 같이 고친 것 — 바퀴 계산 (`cyclesDone()`)

`rotation.cycles` 는 **마지막 Day 를 해야만** 오르는데 총정리에서 Day 를 직접 골라
할 수 있다. Day 5 를 건너뛰면 **바퀴가 영영 0 이고 디로드 권고(6바퀴)가 절대 안 뜬다.**
실사용 프롬프트의 `0바퀴 · 누적 5세션` 이 그 증거였다.

> **바퀴 = 누적 세션 ÷ Day 수 (내림).**

HANDOFF 「디로드는 바퀴 기준」의 원래 근거가 *"누적 부하는 세션 수를 따라간다"* 였으니
의도에 맞다. Day 1 을 두 번 해도 몸이 받은 세션은 쌓인다.
**`rotation.cycles` 필드는 SCHEMA 계약이라 그대로 두고 표시·판정만 유도값을 쓴다.**

### 검증 — Node 395건 + 브라우저

하네스에 **23건을 덧붙였다** (`regress.js` ④⑤ · 372 → **395건 전부 통과**).
④ 는 **CSS 와 스크롤 목적지를 직접 본다** — 마크업 검사가 놓친 종류라서 그렇게 짰다.

브라우저(`localhost:8765/workout/`)에서 실측 —
칩 실효 불투명도 **1.0**(나머지 0.45) · 마친 직후 스크롤 목적지가 **`.rir`**(다음 카드 아님) ·
칩 하단이 휴식 바 최종 상단보다 **53px 위** · 칩을 고르면 `ex-1` 로 이동 ·
해제하면 스크롤 없음 · 이미 끝난 다음 종목이면 안 낚아챔 ·
프롬프트가 `5세션 → 1바퀴` · `30세션 → 6바퀴 · 디로드 권고 구간` (그때도 `rotation.cycles` 는 0) ·
콘솔 오류 0 · 표시줄 `workout-v12`.

⚠ **53px 여유는 데스크톱 뷰포트(1294px)에서 잰 값이다.** 폰(844px)에서도 같은 값이
나오도록 `scroll-margin-bottom` 을 잡았지만 **그건 산술이지 실측이 아니다.**
자동화 탭에서는 `behavior:"smooth"` 가 생략되고 CSS 전환이 얼어붙어서(7회차), 스크롤은
목적지 결정을 가로채 확인하고 위치는 `behavior:"auto"` 로 재현해 쟀다.

### ⭐ 이어서 — 「오늘 마무리」 프롬프트 (`v13`)

사용자 지적: **"헬스 끝나고 세션 저장을 누르는데 그때 러닝은 당연히 안 했으니
기록이 없다. 그거에 의문을 가져서."** 하루가 안 끝났는데 보고하는 꼴이라는 것이다.

먼저 사실을 정리했다 — **저장은 이미 따로다.** 버튼도(`#finish` / `#runsave`),
저장 위치도(`sessions[]` / `runs[]`) 갈려 있다. **문제는 프롬프트의 시점**이었다.

⚠ **웨이트 프롬프트에서 러닝을 빼는 선택지는 버렸다.** 시간 순서를 보면 갈린다 —
**오늘 밤 러닝은 오늘 세션의 원인이 아니다**(세션 뒤에 온다). 반대로 **어제까지 쌓인
러닝은 오늘 세션의 원인**이다. 빼면 10회차에 그 블록을 넣은 이유가 사라지고 계약이
7/7 → 6/7 로 후퇴한다. **되살리지 말 것.**

| 무엇 | 어떻게 |
|---|---|
| **세션 프롬프트에 시점을 적는다** | 오늘 러닝이 없으면 `러닝은 밤에 한다 — …오늘 밤 러닝은 아직 없다.` 한 줄. **이미 뛰었으면 안 붙인다** |
| **`주 N일차`** | 주 초반엔 누적이 언제나 목표 미달이라 그 줄만 보면 「미달」로 읽힌다. 월요일 아침이면 언제나 `0.0km · 0회` 다 |
| **`finishText()` 신설** | 러닝을 저장하면 뜬다. 그날 세션 전부(오래된 것부터) + 오늘 러닝 + `[오늘 이전 러닝 3회]`(오늘 것은 뺀다) + 주간 + 진행 상황 |
| **러닝만 한 날** | 세션 블록·`[진행 규칙]`·`[진행 상황]` 을 빼고 `[답변 형식]` 을 「최대 2줄 · 웨이트 조언 금지」로 바꾼다 |
| **`skipped` 필드 추가** | 건너뛴 종목을 **기록에** 남긴다. 예전엔 `sessionSkip`(세션 상태)에서만 읽어서 「초기화」를 누르면 사라졌다 — 밤에 만드는 마무리가 이걸 통째로 잃었다. 필드 추가지 마이그레이션이 아니다(옛 기록은 `[]`) |
| **강조는 웨이트를 한 날만** | 러닝은 주 5회다. 매번 눈에 띄게 하면 붙여넣기를 강요하는 꼴이 된다. 러닝만 한 날은 기본 버튼 + 「보낼 것이 없으면 그냥 두세요」 |

**둘 중 하나만 보내면 된다.** 앱은 어느 쪽도 강요하지 않는다 — 사용자가 직접 고른 규칙이다.

검증 — **하네스 395 → 420건 전부 통과** (`runtests.js` Z절 25건).
브라우저 실측 — `주 1일차` 표기 · 「밤에 한다」 줄이 붙고 러닝 저장 뒤엔 안 붙음 ·
`502`→5.02 / `2631`→26:31 / 페이스 5:17 · 마무리 블록 11개 순서 ·
`[오늘 이전 러닝 3회]` 에 오늘 것 없음 · 초기화 뒤에도 건너뛴 종목이 살아 있음 ·
러닝만 한 날 경로 · **마무리 텍스트를 파서에 먹여도 종목 오염 0** ·
폼을 다시 열면 옛 프롬프트가 치워짐 · 터치 타깃 44px 미만 0 · 가로 스크롤 0 · 콘솔 오류 0.

⚠ **하네스에서 데인 것** — `runtests.js` 의 `const pass = …` 가 **검사 절들 위에** 있었다.
새 절을 그 아래 붙였더니 집계에서 통째로 빠졌다. 요약은 반드시 맨 끝에서 셀 것.

### 확인하지 못한 것 · 사용자에게 물은 것

- ⭐ **`v12`·`v13` 이 폰에서 하나도 확인되지 않았다.** 다음 세션이 먼저 볼 것 —
  **한 종목을 마치면 칩 3개가 눈에 보이는가**(체크리스트 3), 그리고 **칩을 고르면
  다음 종목으로 넘어가는가.** 폰에서만 증명된다
- **러닝 탭을 아직 안 열었다** (`러닝 기록 없음`). 390px 미실측이 그대로다 (체크리스트 11).
  ⚠ **`v13` 의 「오늘 마무리」도 러닝을 저장해야 보인다** — 러닝 입력이 선행 조건이다
- 웜업 체크를 안 해 `웜업:` 줄은 미확인

### 알고 두는 것 — 고치지 않았다

- **`[오늘 · 2026-08-17 Day 1 상체 밀기 · 49분]`** — `Day 1` 과 제목 사이에 `·` 가 없다.
  `sessionLines()` 는 `Day 1 · 상체 밀기` 인데 `coachText()` 의 오늘 헤더만 다르다.
  파싱에 영향 없고 코치도 읽는다. 다음에 그 줄을 손댈 때 같이 맞출 것
- **저장한 러닝을 고칠 길은 여전히 없다** — 4-9 의 영역이다. `v12` 는 손대지 않았다

---

## 남은 SPEC 항목

**무엇이 먼저인지는 헬스장에서 몇 번 더 써봐야 안다.**
실제로 첫 세션 만에 나온 피드백 4건 중 어느 것도 SPEC 의 4단계 순서와 맞지 않았다.
SPEC 이 순서를 권고로만 둔 이유다. **사용자에게 무엇이 거슬리는지 먼저 물어볼 것.**

**SPEC 4단계와 6단계가 끝났다** — 4-1(RIR) · 4-2(노트·웜업·소요시간) · 4-3 · 4-4 ·
4-5(주차) · 4-6 · 4-7 · 4-8(대체 기억) · **6(러닝 탭)** 전부.
남은 것은 **4-9 와 7단계** 둘이다.

### 다음 순위는 4-9 다 — 러닝이 그 이유를 키웠다

**4-9(되돌리기 · 비정상 값 경고).** 원래도 남아 있던 항목인데 러닝이 들어오면서
급해졌다. 러닝은 **주 5회 입력**이라 오타 하나가 주간 누적을 영구히 틀리게 만든다 —
`5000` 을 잘못 치면 50.00km 가 그대로 굳는다. 지금은 고칠 길이 없다.
세션 되돌리기와 **같이** 설계할 것.

그 다음이 **7단계(GitHub 동기화)** 다. 착수 전에 **공개 범위를 반드시 다시 정할 것** —
secret gist 가 유력하다 (SCHEMA 「기록을 어디에 올릴 것인가」).

### 코칭 프롬프트 — 계약 7/7 충족 ✅

`[최근 러닝 3회]` 와 `[이번 주 러닝]` 이 들어가 **마지막 한 칸이 채워졌다** (10회차).
하체 정체의 원인을 무게로만 보게 되던 구멍이 메워졌다.

그 다음이 **4-9 의 비정상 값 경고**다. 잘못 저장된 값이 프리필과 증량 제안을 계속
오염시키는데 지금은 고칠 방법이 아예 없다. **5단계로도 이건 못 고친다** —
프로그램은 고쳐지지만 **기록**은 못 고친다.

**7단계는 착수 전에 공개 범위를 다시 정해야 한다** (secret gist 유력).

### 5단계에서 하지 않은 것 — 의도적이다

- **드래그 순서 변경** — ↑↓ 버튼으로 갈음했다. 땀 묻은 손과 iOS 스크롤이 충돌한다
- **웜업 · 마무리 유산소 · 큐 · 대체 종목 목록 편집** — SPEC 이 요구한 6개 항목 밖이다.
  필요하면 백업 텍스트를 손으로 고쳐 복원하는 경로가 있다
- **근육군별 볼륨 표시** — 총 세트 수 변화만 보여준다 (SPEC 5단계 명시)
- **저장된 세션의 Day 번호 소급 정정** — Day 를 삭제해도 이미 저장된 기록의 `day` 는
  그대로다. 이름과 같은 이유다 — 그날은 실제로 그 Day 였다

---

## 반드시 지킬 것

### ⚠ 배포할 때마다 `sw.js` 의 `CACHE` 를 올린다

`workout-v2` → `v3` → … **안 올리면 고쳐도 폰에 옛 화면이 그대로 뜬다.**
하단 표시줄에 그 값이 그대로 나오므로 폰에서 눈으로 확인한다. 이게 함정 ③이다.

### 폰에 반영하는 방법 (사용자에게 안내할 때)

1. **멀티태스킹에서 위로 스와이프해 완전 종료** — 백그라운드로 갔다 오는 건 안 된다
2. **온라인 상태로** 열 것 — network-first 라 오프라인이면 캐시의 옛 버전이 뜬다
3. 표시줄이 안 바뀌면 **한 번 더** — 첫 실행은 새 워커 설치, 두 번째부터 그 워커가 답한다

### 검증 방식

- **PC 에서 되는 것은 PC 에서 끝낸다.** `localhost` 는 보안 컨텍스트라 **서비스워커가 등록된다.**
  CLAUDE.md 의 "로컬 검증 불가"는 `http://192.168.x.x` 얘기다
- **부모 폴더에서 서빙하면 `/workout/` 서브경로까지 재현된다** — 함정 ① 검증에 쓴다
  `cd .. && python -m http.server 8765` → `http://127.0.0.1:8765/workout/`
- `node --check` 로 인라인 스크립트 문법 검사 (`<script>`~`</script>` 를 떼어내서)
- **⚠ 테스트 중 `confirm()` 을 띄우면 브라우저 자동화가 멈춘다.** `window.confirm` 을 스텁으로 바꿔서 검증할 것
- **폰에서만 증명되는 것**: 홈 화면 설치 · 아이콘 · Wake Lock · iOS 저장소 분리 ·
  백그라운드 타이머 · 클립보드 제스처

### 작업 방식

- **고치기 전에 무엇을 바꿀지 먼저 보고한다.** 이 방식으로 "안 건드리는 게 맞는 곳"을 여러 번 걸러냈다
- **"코드는 짰지만 확인 안 함"은 완료가 아니다** (CLAUDE.md). 못 한 검증은 못 했다고 적는다

---

## 내린 결정과 근거 (누적)

**기록해두지 않으면 다음 세션이 처음부터 다시 검토하거나, 이미 내린 결정을 되돌린다.**
실제로 3회차 결정 하나가 5회차에 실사용 피드백으로 뒤집혔다 — 아래 ⚠ 표시.

### 데이터 · 스키마

| 결정 | 근거 |
|---|---|
| **시각을 저장하지 않는다** — `durationSec` · id 에 4자리 무작위 · `rev` 정수 | 날짜만 남는 것과 날짜+시각이 남는 것은 위험도가 다르다. 세 곳을 다 막아야 의미가 있다 |
| 병합 기준을 `updatedAt` → **`rev` 정수** | 노출 때문만이 아니다. 원격 JSON 을 손으로 고쳤을 때 **사람이 올릴 수 있는 값**이어야 앱이 덮어쓰지 않는다 |
| **`rotation` 이 Day 순환의 단일 진실** | `history[0]` 에서 유도하면 월별 파일로 쪼개진 뒤 월초에 항상 Day 1 로 리셋된다 |
| `rotation.byDay` **필드 추가** (SCHEMA 에 없음) | 브리핑의 "마지막 수행" 에 Day별 마지막 날짜가 필요한데 월 파일을 열지 않으려면 index 에 있어야 한다. 필드 추가는 마이그레이션이 아니다 |
| `lastByExercise.sets` **필드 추가** | 무게를 하나만 담으면 30/35/40kg 로 올려간 날이 "40kg 로 10회" 라는 거짓이 된다. `w`·`reps` 는 하위 호환용으로 남긴다 |
| **디로드는 바퀴 수 기준** (달력 주차 아님) | PROGRAM.md 의 "주 5회 = 정확히 1주 1바퀴" 에 비춰 "6주차" 는 원래 6바퀴를 뜻했다. 누적 부하는 세션 수를 따라간다 |
| 반응형 디로드는 **RIR 기준** (중량 하락 아님) | 중량은 대체 종목·이중 점진에 오염된다. 같은 Day 는 2주에 한 번 와서 감지도 늦다 |
| 이름 변경 시 **키 이동 + `was`** (고유 ID 아님) | ID 는 스키마를 2로 올리는 대공사. 이름 변경은 드문 이벤트 |
| 폴백 프리필은 **읽기 전용**, 증량 판정 안 함 | 쓰기까지 폴백하면 원래 종목 기록이 오염된다. 다른 종목에서 목표를 채운 건 이 종목의 근거가 아니다 |
| 무게 `0` = **머신 기본 무게 / 빈 봉**. 화면에는 「기본」 | 실사용에서 나온 질문. 증량 판정에서 `w>0` 조건을 뺐다 — 빈 봉으로 목표를 채웠으면 다음은 첫 원판 차례다. **맨몸 종목만 제외** |

### 저장 · 실패 처리

| 결정 | 근거 |
|---|---|
| ⚠ **저장 성공 시 화면을 비우지 않는다** (3회차 결정을 5회차에 뒤집음) | 3회차엔 드래프트 되살아남을 막으려 `entries` 를 비웠는데, **실사용에서 "저장하면 기록을 다시 못 본다"** 가 됐다. 지금은 `sessionSaved` 로 드래프트만 막고 화면은 남기며, 비우는 것은 **「초기화」 버튼**이 맡는다 |
| 재저장은 **같은 id 를 `rev+1` 로 갱신** | id 를 매번 새로 뽑으면 중복 제거가 죽은 코드가 되어 재시도마다 세션이 쌓이고 `counts`·`cycles` 가 두 번씩 오른다 |
| **쓰기에 성공하면 `storageOK` 래치를 푼다** | 한 번 실패하면 되돌아오지 않아, 용량을 확보하고 다시 눌러도 쓰기를 시도조차 안 했다 |
| 옛 키 삭제는 **새 키 쓰기 전부 성공 후에만** | 사파리 프라이빗·용량 부족에서는 `setItem` 만 던지고 `removeItem` 은 성공한다. 순서를 뒤집으면 원본이 사라진다 |
| 드래프트 판정에서 **`swaps` 제외** | 대체를 한 번 눌러본 것만으로 Day 가 고정되면 안 된다. 근거는 실제로 기록한 것뿐이다 |
| 경고는 **화면 맨 위 `#banner`** | `#saved` 는 두 화면 아래인 데다 Day 행을 탭하면 지워져 마이그레이션 실패 경고가 사라졌다 |
| 저장 시점 필터(`x.done&&x.r`)는 **방어선으로 남긴다** | 입력 단계(0-3)에서 막아도 두 겹이 낫다 |

### 클립보드 텍스트

| 결정 | 근거 |
|---|---|
| **코칭 프롬프트와 백업을 분리** | SCHEMA 의 두 계약을 하나가 겸하고 있었다. 세션마다 쓰는 텍스트가 이력에 비례해 길어져 **2세션 만에 60줄**이 됐다. 지금은 저장 직후 = 코칭 프롬프트(길이 일정), 전체 백업은 접힌 별도 버튼 |
| **「누적 기록」 섹션 제거** | 「기록」과 중복이라 길이만 두 배였고, 종목당 무게를 하나만 담아 세트별 무게가 뭉개졌다 |
| 파서는 **first-wins** | 「기록」이 최신순이라 나중 것이 이기면 가장 오래된 세션의 값과 Day 가 최종으로 남는다. v1 텍스트는 종목당 한 줄이라 영향 없다 |
| 내보내기에 **프로그램 포함** | 커스텀 프로그램의 유일한 복구 수단이다 (SCHEMA 요구) |
| 복원 시 `date` 에 **`"복원"` 같은 문자열 금지** | 날짜 자리에 날짜가 아닌 값이 들어가면 그대로 `index.json` 에 실려 GitHub 까지 올라간다. 모르면 `null` |

### 프로그램 편집 (5단계)

| 결정 | 근거 |
|---|---|
| **화면에 열려 있는 Day 를 고치면 그 Day 의 입력을 비운다** | `entries`·`swaps` 가 종목의 **배열 인덱스**로 키를 잡는다. 종목을 지우거나 순서를 바꾸면 입력값이 엉뚱한 종목에 가서 붙는다. 저장 안 된 세트가 있으면 먼저 물어본다 |
| 이름 변경 시 **옛 이름이 다른 Day 에 남아 있으면 키를 옮기지 않는다** | SCHEMA 에 없던 경우다. 같은 종목이 두 Day 에 걸친 경우가 **실제로 5개** 있다 (`케이블 로프 해머컬` Day 3·4 등). 옮기면 그쪽 프리필이 끊긴다 |
| 이름 변경 시 **새 이름에 이미 기록이 있으면 합치지 않는다** | 어느 쪽이 맞는지 알 방법이 없다. 둘 다 그대로 두고 화면에 알린다. 밀려난 기록은 백업 텍스트의 「세션 기록이 없는 종목」에 남는다 |
| **직접 추가한 종목은 「대체 종목」 버튼을 감춘다** | `alts` 가 비어 있어 눌러도 빈 칸만 나온다 |
| Day 삭제 시 **`rotation.byDay` 를 같이 당긴다** | Day 번호를 키로 쓰는 필드다. 안 당기면 브리핑의 "마지막 수행" 날짜가 엉뚱한 Day 에 붙는다. `lastDay`·`nextDay` 도 같이 보정한다 |
| **반복 범위는 칸 두 개로 받는다** (`8` ~ `12`) | 한 칸에 `"8-12"` 를 치게 했더니 **폰에서 입력 자체가 불가능**했다. `inputmode="numeric"` 이 띄우는 iOS 숫자 키패드에는 **하이픈 키가 없다.** 저장 형식은 `reps` 문자열 그대로 유지하고(`lo===hi` 면 `"10"`) `repsRange()` 로 왕복시킨다 |
| **`esc()` 를 도입해 화면 출력을 전부 이스케이프** | 5단계부터 종목 이름이 **사용자 입력**이다. 그전에는 `DAYS` 상수만 화면에 들어가 필요 없었다. 따옴표 하나로 속성이 깨지고 `</textarea>` 로 백업 텍스트가 잘려나간다 |
| 되돌리기는 **프로그램만** 되돌린다 | 기록은 건드리지 않는다. 이름을 바꿔 옮겨둔 `lastByExercise` 키도 그대로다 — 되돌린 프로그램의 옛 이름에는 기록이 없는 상태가 될 수 있다. 기록을 지우는 것보다 낫다 |

### 앱 구조 · 배포

| 결정 | 근거 |
|---|---|
| `DAYS` 종목을 **위치 배열 → 이름 객체**. 변환은 스크립트로 | 32종목 × 13필드다. 손으로 옮기면 오타가 조용히 섞인다. 변환 전후 deep-compare 로 동일성을 증명했다 |
| 앱 안에서 **Claude API 를 부르지 않는다** | 키가 public 리포에 노출된다. 서버는 금지. 요금도 따로 나간다 |
| sw 는 **network-first + 2초 타임아웃** | 신호가 없는 것보다 약한 것이 나쁘다. 지하에서 10초 흰 화면 |
| **`navigator.onLine===false` 면 네트워크를 건너뛴다** | SPEC 의 "오프라인이면 fetch 가 즉시 실패한다" 는 전제가 실측과 달랐다. **2026ms → 29ms** |
| `install` 에서 **`allSettled`** | 하나가 실패해도 나머지는 캐싱한다. `cache.addAll` 은 all-or-nothing 이라 설치 자체가 실패한다 |
| 캐시 버전을 **`MessageChannel`** 로 물어봄 | 페이지에 하드코딩하면 sw 와 어긋난다. 물어보면 언제나 실제 값이다 |
| Pages 는 **계속 켜둔다** | 끄면 HTTPS 가 없어지고 서비스워커가 죽는다. 선택이 아니라 전제다 |
| **7단계 전에 공개 범위 재판단** | 기록이 올라가면 "새는 정보 없음" 전제가 깨진다. secret gist 가 유력 |

---

## 되살리지 말 것 — 전부 검토 후 내린 결정이다

| 하지 말 것 | 이유 |
|---|---|
| **알림음 / 진동** | iOS 는 무음 스위치가 켜져 있으면 Web Audio 를 재생하지 않고 `navigator.vibrate` 도 미지원이다. 헬스장에서 폰은 대개 무음이라 어차피 안 울린다. HTML5 `<audio>` 로 뚫는 알려진 해법이 있지만 **사용자가 "알림 소리는 안 나도 상관없다" 고 확인했다.** "무음에서 알람이 안 울린다" 를 버그로 보고하지 말 것 |
| 앱 안에서 **AI API 호출** | 원칙 1. 프롬프트 복사로 대체돼 있다 |
| **애플 건강 연동** | 걷기 거리와 종일 심박이 섞여 데이터 품질이 떨어진다. 러닝은 손으로 입력한다 |
| **체중·식단 기록** | PROGRAM.md 에 규칙은 있지만 이 앱은 세션 기록만 다룬다 |
| `index.html` 을 **프레임워크로 재작성** | 단일 파일 자급자족이 이 앱의 성질이다 |
| **백엔드 · 계정 · 푸시 알림** | 기록은 폰 안에만 있으면 된다 |
| 운동 프로그램 내용을 **AI 판단으로** 변경 | 근거 기반 설계다. **사용자는 앱에서 바꿀 수 있다** (5단계). 이 금지는 AI 에게 하는 말 |
| **전체 기록 편집기** | 되돌리기 + 비정상 값 경고로 대부분 막히고, 7단계 후에는 원격 JSON 이 곧 편집기다 |

---

## 지나온 경위 (요약)

상세는 커밋 메시지에 있다 — `git log --oneline` 으로 5개 작업 커밋을 볼 것.

| 회차 | 한 일 |
|---|---|
| 1 | 문서 5개 확정. 코드 읽고 버그 4건 + 죽은 기능 2개 확인 |
| 2 | **0단계** — `today()` UTC→로컬, `date:"복원"` 제거, 반복 없는 세트 소실 방지 |
| 3 | **1단계** — 저장 어댑터 · SCHEMA 구조 · rotation · 자동 저장 · `wk:program` · 마이그레이션 껍데기. `ask()` 와 AI 버튼 제거 |
| 4 | **2단계** — manifest · sw · head 태그. **Pages 켜고 배포** |
| 5 | **3단계 통과** + **v2** — 코드 리뷰 10건, 타이머 `Date.now()`, 코칭 프롬프트 분리, 세트별 무게, 초기화 버튼 |
| 6 | **5단계 + SPEC 4단계 완결.** `v3`~`v8` 여섯 배포 — 위 「기능 지도」 참조. 타이머 실기기 확인 통과, 나머지는 폰 검수 대기 |
| 7 | **코드 레벨 검수 242건** → 결함 3건 수정 후 **`v9` 한 배포.** 위 「7회차」 참조 |
| 8 | **프로그램 데이터 3건 변경 + `PROGRAM_REV` 개정 장치.** `v10` 한 배포. 위 「8회차」 참조 |
| 9 | **6단계 러닝 탭 설계 확정.** 시안으로 폰에서 만져보고 결정. 코드는 아직 안 짰다 — 배포 없음 |
| 10 | **6단계 구현 + 낮은 순위 5건.** 검수 372건 통과. `v11` 한 배포. 위 「10회차」 참조 |

**5회차 리뷰 10건은 전부 4회차에 내가 넣은 것이었다.** 데이터 손실 3 · 오프라인 불가 1 ·
저장 영구 비활성 1 · 복원 부정확 2 · 중복 저장 1 · 조용한 실패 2. 모두 실패 경로였고,
그래서 정상 사용만으로는 드러나지 않았다.

---

## 아직 정해지지 않은 것

착수를 막는 미결 항목은 **없다.**

문서로 확인할 수 없는 것만 남아 있고 성격이 다르다 — **실기기 동작**이다.
위 「아직 폰에서 확인 못 한 것」 2건.

7단계 착수 시 확정할 것 하나: 기록을 어디에 올릴 것인가 (secret gist 유력).
**`gh` 토큰에 `gist` 스코프가 이미 있는 것은 확인했다** — SCHEMA 가 먼저 확인하라던 항목.

---

## 참고

전체 흐름 시안 — https://claude.ai/code/artifact/91483fc3-05a1-4193-83c9-ccf20f2f7335
**시안과 문서가 어긋나면 문서가 기준이다.**

---

## 새 세션을 시작하는 법

```bash
cd C:/Users/hr222/projects_hr/workout
claude
```

**반드시 `workout` 안에서 열 것.** 상위 폴더에서 열면 `workout/CLAUDE.md` 가 자동으로 안 읽힌다.

첫 마디는 **`NEXT-SESSION.md 읽고 이어서 하자`** 한 줄이면 된다.
`CLAUDE.md` 가 자동으로 읽히고 거기서 그 파일을 가리킨다 — 붙여넣을 필요가 없다.

### 6회차에서 배운 것 — 다음 세션이 반복하지 말 것

1. **PC 브라우저 테스트로는 iOS 입력을 검증할 수 없다.** `inputmode="numeric"` 이 띄우는
   iOS 숫자 키패드에는 하이픈이 없어 `"8-12"` 를 칠 방법이 아예 없었다. 데스크톱에서는
   그냥 타이핑되니 100% 통과한다. **폰 입력 형식은 실기기에서만 증명된다.**
2. **CSS 기본값이 조용히 문다.** `.tagline` 에 `align-items` 를 안 줘서 기본값 `stretch` 가
   먹었고, 44px 버튼을 한 줄에 넣자 옆 태그들이 44px 로 늘어났다. 새 요소를 기존 flex 줄에
   넣을 때는 그 줄의 정렬을 먼저 확인할 것.
3. **기록의 키와 화면의 이름이 다르면 데이터가 사라진 것처럼 보인다.** 대체 종목이 그랬다.
   키를 바꾸는 기능을 만들 때는 **반대 방향 조회**가 되는지 반드시 확인할 것.
4. **한 번에 여섯 배포는 위험하다.** 폰 검증이 밀린 채 쌓이면 문제가 났을 때 어느 배포가
   원인인지 가릴 수 없다. 5회차에 리뷰 10건이 한꺼번에 나온 것과 같은 구조다.

### 제대로 읽었는지 확인하는 질문

- **왜 저장 후에 화면을 안 비우나** → 실사용 피드백. 비웠더니 기록을 다시 볼 수 없었다.
  드래프트 되살아남은 `sessionSaved` 로 따로 막는다
- **배포할 때 반드시 해야 하는 것은** → `sw.js` 의 `CACHE` 버전 올리기
- **`rotation` 이 왜 중요한가** → Day 순환의 단일 진실. `history[0]` 에서 유도하면 월초에 깨진다
- **프로그램을 고치면 왜 화면 입력이 지워지나** → `entries` 가 배열 인덱스로 키를 잡아서.
  순서를 바꾸면 값이 엉뚱한 종목에 붙는다
- **퀵수정에서 오늘만인지 계속인지 어떻게 아나** → **자리가 곧 범위다.**
  카드에서 하면 오늘만, `⚙` 시트에서 하면 계획. 체크박스는 없다
- **대체 종목을 왜 기억하나** → 기록의 키는 실제 수행 종목이라, 기억하지 않으면
  다음에 그 Day 를 열었을 때 한 운동이 통째로 사라진 것처럼 보인다

셋 중 하나라도 어긋나면 이 파일을 다시 읽힐 것.
