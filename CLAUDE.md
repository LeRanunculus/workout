# workout — 헬스 세션 기록 PWA

## 시작하기 전에 읽을 것

1. **이 파일** — 프로젝트 규약과 절대 원칙
2. **SCHEMA.md** — 데이터 계약. 앱·리포·분석이 공유한다. **구현 전에 반드시 읽을 것**
3. **SPEC.md** — 구현 명세. 0~7단계. 3단계(아이폰 검증)가 관문이다
4. **PROGRAM.md** — 운동 프로그램 데이터. `index.html`의 `DAYS` 배열과 일치해야 한다

`index.html`은 **이미 완성된 앱**이다. 프레임워크로 새로 짜지 말 것.

다만 "완성"이 "버그 없음"은 아니다. 2026-08-11 코드 검토에서 날짜·저장·타이머 버그
4건과 PWA에서 죽는 기능 2개가 확인됐다. **SPEC.md의 0단계를 먼저 읽을 것.**

---

## 이 프로젝트가 존재하는 이유

사용자는 **헬스장에 아이폰만 들고 간다.** 노트북도, 다른 기기도 없다.
그래서 이 앱의 성공 기준은 단 하나다 — **아이폰 홈 화면에서 오프라인으로 열리고, 기록이 남는가.**

원래 이 앱은 Claude 아티팩트로 만들어졌는데, 아이폰 Claude 앱에서 저장소 연결이 끊겨
(`Storage ... failed: Unexpected response type` — get/set/list/delete 전부 동일 실패)
기록이 하나도 저장되지 않았다. 그래서 PWA로 옮긴다.

**PWA로 옮기면서 실제로 얻는 것:**
- localStorage가 정상 작동 → 진짜 자동 저장
- HTTPS라 Wake Lock 작동 → 세트 사이 쉴 때 화면이 안 꺼짐
- 홈 화면 아이콘, 전체화면, 오프라인

---

## 절대 원칙

### 1. 외부 의존성 0
CDN, 웹폰트, 외부 API 호출을 **추가하지 말 것.** 헬스장 지하는 신호가 약하다.
현재 `index.html`은 단일 파일로 자급자족한다. 이 성질을 깨뜨리는 변경은 거부할 것.

**앱 안에서 Claude API를 부르지 않는다.** 아티팩트 시절 코드가 `api.anthropic.com`을
키 없이 직접 호출하는데, GitHub Pages에서는 CORS·401로 100% 실패한다. 되살리지 말 것.
AI가 필요한 자리는 **프롬프트를 클립보드에 복사**해 Claude 앱에 붙여넣는 방식으로 대체한다.
오프라인에서도 동작하고, 어차피 그게 주 사용 경로다.

### 2. 빌드 도구 없음
npm, webpack, vite 도입 금지. 정적 파일 그대로 GitHub Pages에 올린다.
`package.json`을 만들지 말 것.

### 3. 기록은 절대 잃으면 안 된다
저장 실패는 조용히 넘어가지 말고 화면에 표시한다.
localStorage가 죽어도 **내보내기 텍스트** 경로는 항상 살아 있어야 한다.
동기화는 **병합**이지 덮어쓰기가 아니다 — 폰을 재설치해도 원격 기록이 지워지면 안 된다.

### 3-1. 애플 건강 연동은 하지 않는다
걷기 거리와 종일 심박이 섞여 데이터 품질이 떨어진다. 러닝은 손으로 입력한다.
"자동화하면 편할 텐데"라는 판단으로 되살리지 말 것. 검토 후 내린 결정이다.

### 4. 손가락으로 쓰는 앱이다
터치 타깃 최소 44px, 입력 숫자 20px 이상. 땀 묻은 손과 팔 뻗은 거리를 가정한다.
데스크톱 마우스 기준으로 판단하지 말 것.

### 5. 시각은 언제나 로컬 기준
날짜에 `toISOString()`을 쓰지 말 것. UTC라서 **오전 9시 이전 세션이 전날로 기록된다.**
사용자는 아침 또는 낮에 운동한다 — 예외가 아니라 일상이다.
규칙은 SCHEMA.md의 「시각 규칙」에 있다. 새 시각 필드를 만들 때마다 그 절을 확인할 것.

---

## 개발과 검증의 분리 ← 가장 중요

**로컬 네트워크 테스트로는 PWA를 검증할 수 없다.**
PC에서 서버를 띄우고 폰에서 `http://192.168.x.x`로 접속하면 HTTPS가 아니라
**서비스워커가 등록되지 않는다.** 즉 오프라인·설치·저장이 전부 미검증 상태가 된다.

| 무엇을 | 어디서 | 방법 |
|---|---|---|
| 레이아웃, 계산 로직, 렌더링 | PC 브라우저 | `python -m http.server` 로 localhost |
| 서비스워커, 오프라인, 홈 화면 설치, 저장 | **아이폰** | 배포 후 실기기 |

그래서 **최소 구성으로 일찍 배포하고 그 위에서 고친다.** 완성한 뒤 배포하지 말 것.

---

## 배포

GitHub Pages. 리포는 **public** (무료 플랜에서 Pages는 public만 지원).
Pages가 없으면 HTTPS가 없고, HTTPS가 없으면 서비스워커가 등록되지 않아
**PWA 자체가 성립하지 않는다.** 선택이 아니라 전제다.

### 공개 범위는 7단계 전에 다시 판단한다

지금 공개되는 것은 앱 코드와 문서뿐이다. 운동 기록은 폰 안에만 있다.

**그러나 7단계(동기화)가 돌면 전제가 바뀐다.** `data/2026/08.json`에 날짜별 기록
전체가 public으로 올라간다 — 며칠 몇 시에 헬스장에 있었는지의 완전한 로그다.
"공개돼도 새는 정보가 없다"는 판단은 그때 더 이상 맞지 않는다.

**7단계에 착수하기 전에 반드시 다시 정할 것.** 선택지는 셋이다 —
그대로 public / 앱만 public이고 데이터는 별도 private 리포(단, Claude가 raw URL로
못 읽어 복붙이 다시 필요해진다) / 유료 플랜으로 private + Pages.

```
gh repo create workout --public --source=. --remote=origin --push
```
Pages 활성화 후 서비스 주소는 `https://<user>.github.io/workout/` 형태다.
**이 서브경로가 함정 ①의 원인이다. SPEC.md를 반드시 확인할 것.**

---

## 하지 말 것

- `index.html`을 프레임워크로 다시 쓰기
- 백엔드·서버·데이터베이스 추가 (기록은 폰 안에만 있으면 된다)
- 사용자 계정, 로그인
- 푸시 알림 (iOS는 설치된 PWA에서만 되고, 이 앱엔 불필요)
- 앱 안에서 AI API 호출 — 프롬프트 복사로 대체한다 (원칙 1)
- 체중·식단 기록 추가 — PROGRAM.md에 감량 속도와 단백질 규칙이 있지만
  이 앱의 범위가 아니다. 세션 기록만 다룬다
- 운동 프로그램 내용을 **네 판단으로** 바꾸기 — 근거 기반으로 설계됐다. PROGRAM.md 참조.
  **사용자는 앱에서 직접 바꿀 수 있다** (SPEC 5단계). 이 금지는 너에게 하는 말이다

---

## 세션 인계

작업을 마치면 `HANDOFF.md`에 이렇게 남긴다.
- 이번에 바꾼 것
- 아이폰 실기기에서 확인한 것 / 아직 확인 못 한 것
- 다음 세션이 먼저 해야 할 것

**"코드는 짰지만 폰에서 확인 안 함"은 완료가 아니다.** 그대로 적을 것.
